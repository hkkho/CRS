# G4-R3 - fresh G4 re-entry gate task

## Outcome

Status: BLOCKED

G4-R3 executed the fresh spatial-validation gate against the bounded admission
decision from P4-T06-G4G. The mathematical evidence role is now explicit and
usable for review, but no approved numeric tolerance, `ApprovedGolden` payload,
or external solver baseline exists. The gate therefore remains technically
blocked for production tolerance/golden approval. This is not a restart of the
completed G4 admission chain.

Effectiveness: BLOCKED

The task was effective in isolating the remaining blocker: evidence-role
admission is resolved; numeric authority and representative coverage are not.

## Execution and model evidence

- Role: root implementer; fresh gate execution
- Requested model / reasoning: GPT-5.6 Luna / high; code review (high)
- Actual model / reasoning: `UNVERIFIED`
- Execution receipt or telemetry source: root task context; same-context reviewer `01a0080f-dc22-7811-9b10-2a60a9e299d0`; receipt telemetry unavailable
- Attempts: 1 bounded gate execution; elapsed time: Unavailable
- Artifact/checkpoint status: produced
- Review disposition: PASS; final technical gate disposition BLOCKED, with no actionable review findings
- Review evidence verification: `UNVERIFIED`
- Reviewer reuse/fresh-review rationale: same reviewer context reused; no fresh reviewer and no prior-task restart

Applicable P1-T08 digest rows are `S1-R02`, `S1-R03`, `S1-R04`, `S1-R06`,
`S1-R11`, `S5-R03`, `S5-R04`, `S5-R09`, `S6-R03`, and `S6-R08`; they remain
context/methodology/candidate evidence only.

## Files created or changed

- `docs/gates/G4-R3.md` - immutable fresh gate report.
- `docs/tasks/G4-R3.md` - this task report.
- `docs/tasks/ROUND-2026-08-16-G4-BLOCK-RESOLUTION-CHAIN.md` - records the G4-R3 disposition and routes G4H.
- `docs/PROJECT_SCOPE.md` - records the narrowed remaining blocker and next task.

Pre-existing files inspected but not changed include `AGENTS.md`, the complete
implementation plan, all named P2 specifications, P1-T08 report/digest,
G4/G4-R1/G4-R2 reports, G4A-G4G reports, G4D/G4F artifacts/manifests, and the
P4-T08 benchmark. No runtime or data/golden file changed.

## Assumptions and design choices

- G4G's admission is consumed exactly as written: three mathematical cases,
  candidate/deferred status, and no tolerance authority.
- `BLOCKED` is the correct gate result because a technical PASS would require
  inventing or relabelling a numeric threshold or golden value.
- The incomplete refuelled/RRS/poison cases remain visible and are not changed
  to pass by omission.
- G4-R3 is a fresh gate report, not a corrected copy of G4-R2; G4-R2 remains
  historical evidence of the pre-G4G state.

## Validation commands and results

### G4G admission and artifact audit (T1)

```powershell
$ErrorActionPreference='Stop'; $g4f='data/comparisons/p4-t06-g4f-analytical-reference-v1.json'; $g4fManifest='data/comparisons/p4-t06-g4f-analytical-reference-v1.manifest.json'; $g4d='data/comparisons/p4-t06-g4d-independent-reproduction-v1.json'; $g4g=Get-Content -Raw docs/tasks/P4-T06-G4G.md; $f=Get-Content -Raw $g4f | ConvertFrom-Json; $m=Get-Content -Raw $g4fManifest | ConvertFrom-Json; $d=Get-Content -Raw $g4d | ConvertFrom-Json; if($f.status -ne 'candidate' -or $f.evidence_approval -ne 'Candidate' -or $f.comparison_status -ne 'Deferred' -or $f.tolerance_status -ne 'Deferred' -or $f.golden_status -ne 'NoGolden'){throw 'G4F status changed'}; if(@($f.cases).Count -ne 3 -or @($d.records).Count -ne 57 -or @($d.records | Where-Object {$_.status -ne 'Deferred' -or $_.evidence_approval -ne 'Candidate'}).Count -ne 0){throw 'admitted case/record scope changed'}; $sha=(Get-FileHash -Algorithm SHA256 -LiteralPath $g4f).Hash.ToLowerInvariant(); if($sha -ne $m.artifact_sha256 -or $sha -ne '93041fc517029fabdc18e02e48794d1b6e02b4a9d7e2d16b5b637129dd4ae584'){throw 'G4F artifact hash mismatch'}; if($g4g -notmatch 'bounded mathematical-validation input' -or $g4g -notmatch 'NoGolden' -or $g4g -notmatch 'G4-R3'){throw 'G4G decision boundary missing'}; $golden=@(Get-ChildItem -LiteralPath data/golden -File -Recurse | Where-Object {$_.Name -ne '.gitkeep'}); if($golden.Count -ne 0){throw 'golden payload exists'}; Write-Output ('G4_R3_INPUT_AUDIT_PASS g4f_cases=' + @($f.cases).Count + ' g4d_records=' + @($d.records).Count + ' g4f_sha256=' + $sha + ' golden_payload_files=' + $golden.Count)
```

Result: exit 0; 3 G4F cases, 57 deferred/candidate G4D records, matching G4F
hash, and 0 golden payload files.

### Full headless solution regression (T3)

```powershell
dotnet test .\candu\ReactorSim.sln --configuration Release --nologo
```

Result: exit 0; 111/111 tests passed, 0 failures, 0 skips.

### Gate/report routing audit (T0)

```powershell
$ErrorActionPreference='Stop'; $files=@('docs/gates/G4-R3.md','docs/tasks/G4-R3.md','docs/tasks/P4-T06-G4G.md','docs/PROJECT_SCOPE.md','docs/tasks/ROUND-2026-08-16-G4-BLOCK-RESOLUTION-CHAIN.md'); foreach($path in $files){if(-not (Test-Path -LiteralPath $path -PathType Leaf)){throw "missing $path"}; if(@(Select-String -LiteralPath $path -Pattern '\s+$').Count -ne 0){throw "trailing whitespace $path"}}; $scope=Get-Content -Raw docs/PROJECT_SCOPE.md; $round=Get-Content -Raw docs/tasks/ROUND-2026-08-16-G4-BLOCK-RESOLUTION-CHAIN.md; if($scope -match 'new owner-authorized baseline/coverage task' -or $scope -notmatch 'G4-R3' -or $round -notmatch 'G4-R3'){throw 'stale routing'}; if(@(Select-String -LiteralPath docs/gates/G4.md,docs/gates/G4-R1.md,docs/gates/G4-R2.md -Pattern 'G4-R3|G4G').Count -ne 0){throw 'historical gate modified'}; Write-Output 'G4_R3_ROUTING_AUDIT_PASS historical_gate_cross_refs=0 stale_routing=0'
```

Result: exit 0; historical gate cross-references 0 and stale routing 0.

T6 was not applicable because no external reference baseline changed.

## Numerical differences

No new numerical output was generated. G4F's preserved diagnostic maximums are
`|Δk|=5.80036019215413E-13`, max flux `|Δ|=7.9432987964978E-15`, and maximum
equation residual `8.673617379884035E-19`; these remain diagnostic only.

## Deferred validation

- `P4-T06-G4H` numeric baseline/tolerance decision - required before another
  G4 run; no threshold may be inferred from G4F.
- `P5-T10`/`G5` - remains dependent on a future G4 production disposition.
- External reference T6 - not applicable until an exact approved external
  case is supplied and regenerated.

## Blockers, risks, and follow-up

- Blocker: no approved numeric tolerance, `ApprovedGolden` payload, or
  external solver baseline exists.
- Risk trigger: fresh gate/reference-admission review required T3 and code
  review (high); no tolerance or golden status was changed.
- Accepted/deferred risks: incomplete refuelled/RRS/poison coverage remains
  explicit and visible.
- Follow-up: `P4-T06-G4H`; do not rerun G4 until its evidence basis changes.

## Next eligible task

`P4-T06-G4H` - owner-authorized numeric baseline/tolerance decision or explicit
decline of production G4 approval. Do not restart prior admission tasks.

Source: [`AGENTS.md`](../../AGENTS.md),
[`docs/Implementation_plan.md`](../Implementation_plan.md),
[`docs/gates/G4-R3.md`](../gates/G4-R3.md), and
[`docs/tasks/P4-T06-G4G.md`](P4-T06-G4G.md).
