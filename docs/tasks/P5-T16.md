# P5-T16 - restore authentication and restart reconstruction correction

## Outcome

Status: COMPLETE for the approved ReducedModel/engine-neutral Core scope.

P5-T16 addresses and closes the findings from the P5-T15 same-context review. Restore
now fails closed unless the lifecycle-authenticated accepted-inventory digest
matches the restored complete inventory and the authenticated burnup/energy
digest matches the restored state. It also validates every restored bundle's
power, snapshot, coefficient, nuclide, and retained-history binding against the
accepted lifecycle. A public reconstruction path rebuilds the complete power
snapshot from restored state, and the regression discards the pre-save snapshot,
reconstructs it after load, compares canonical bytes, and uses the reconstructed
value for subsequent burnup.

Effectiveness: SUCCESS. The final same-context G5 review returned `PASS` with
no remaining actionable findings for the bounded scope.

## Execution and model evidence

- Role: root implementer; independent code review (high) reviewer.
- Requested implementation model / reasoning: GPT-5.6 Luna / high.
- Requested review lane / reasoning: code review (high) / high effort.
- Actual root model / effort: per-task root receipt is not exposed.
- Reviewer context: `01a01217-337b-79e1-9b86-88232ccf91d1`; the same context
  must perform the final bounded rerun, with no fresh reviewer.
- Actual reviewer model / effort: final receipt verified `gpt-5.6-sol` / high;
  final turn `01a012d2-fc06-74f1-afa0-f82cf17c5497`.
- Final reviewer receipt telemetry: input `29,914,450`, cached input
  `28,962,560`, cache-write `0`, output `106,935`, reasoning output `46,380`,
  total `30,021,385`; per-turn allocation and cost were unavailable.
- Attempts: one bounded implementation correction; elapsed time unavailable.
- Artifact/checkpoint status: source, focused regressions, and report artifact
  produced.
- Review disposition: `PASS`; no remaining actionable findings within the
  approved ReducedModel/Core boundary.
- Review evidence verification: receipt-verified from the same reviewer
  context.
- Reviewer reuse/fresh-review rationale: same reviewer context is reused after
  the P5-T15 findings; no fresh reviewer is permitted.

## Files created or changed

- `src/ReactorSim.Core/Domain/LifecycleContracts.cs` - validates restored
  lifecycle-authenticated snapshot digests against the restored state and
  reconstructs the accepted snapshot contract before returning success.
- `src/ReactorSim.Core/Domain/Phase5CompleteStateContracts.cs` - adds the
  validating complete snapshot reconstruction path used after archive restore.
- `tests/ReactorSim.Core.Tests/P5T11CompleteBurnupTransitionTests.cs` - adds
  tampered-archive rejection and uses only a post-load reconstructed snapshot
  for the subsequent burnup regression.
- `docs/tasks/P5-T16.md` - this report.

Pre-existing website, PDF, Unity, Golden artifact, and manifest files were
inspected but not changed by P5-T16.

## Assumptions and design choices

- Restore treats the lifecycle-authenticated accepted inventory and
  burnup/energy digests as authoritative; it never trusts a digest supplied by
  an archive payload unless it agrees with the reconstructed restored state.
- The accepted snapshot is reconstructed from the restored inventory and
  lifecycle rather than retaining an in-memory object from before save/load.
  This makes restart validation exercise the production restored state.
- The frozen left-endpoint burnup equation and all existing units, signs,
  convergence rules, tolerances, and approved ReducedModel values remain
  unchanged.
- Applicable P1-T08 literature rows are `S1-R07`, `S4-R03`, `S4-R04`,
  `S5-R05`, and `S6-R05`; they remain context/methodology or candidate-design
  evidence, not runtime or golden authority.
- Direct CANDU/DONJON5/DRAGON5 production authority, full-core coverage,
  Unity/device/mobile, and release evidence remain outside this bounded
  ReducedModel/Core task.

## Validation commands and results

All dotnet commands below used the existing repository-pinned SDK cache at
`C:\Users\infin\AppData\Local\Temp\candu-sdk-10.0.302`; `global.json` was not
changed.

### Serialization compatibility regression (T1)

```text
dotnet test C:\Users\infin\candu\tests\ReactorSim.Core.Tests\ReactorSim.Core.Tests.csproj --configuration Release --nologo --no-restore --filter FullyQualifiedName~P3T04SerializationReplayTests --logger "console;verbosity=minimal"
```

Result: exit code 0; 5/5 tests passed, zero failures and zero skips.

### Complete-state restore and restart regression (T1)

```text
dotnet test C:\Users\infin\candu\tests\ReactorSim.Core.Tests\ReactorSim.Core.Tests.csproj --configuration Release --nologo --no-restore --filter FullyQualifiedName~P5T11 --logger "console;verbosity=minimal"
```

Result: exit code 0; 7/7 tests passed, including tampered complete-archive
rejection and post-load snapshot reconstruction before subsequent burnup.

### Phase 5 focused Core tests (T1)

```text
dotnet test C:\Users\infin\candu\tests\ReactorSim.Core.Tests\ReactorSim.Core.Tests.csproj --configuration Release --nologo --no-restore --filter FullyQualifiedName~P5T --logger "console;verbosity=minimal"
```

Result: exit code 0; 44/44 tests passed, zero failures and zero skips.

### Full Core and solution regression (T3)

```text
dotnet test C:\Users\infin\candu\tests\ReactorSim.Core.Tests\ReactorSim.Core.Tests.csproj --configuration Release --nologo --no-restore --logger "console;verbosity=minimal"
dotnet test C:\Users\infin\candu\ReactorSim.sln --configuration Release --nologo --no-restore --logger "console;verbosity=minimal"
```

Result: exit code 0; Core 110/110 and the complete solution 129/129
(Core 110/110, Golden 19/19) passed, zero failures and zero skips.

### Approved consumers and profile audit (T1)

```text
dotnet test C:\Users\infin\candu\tests\ReactorSim.Golden.Tests\ReactorSim.Golden.Tests.csproj --configuration Release --nologo --no-restore --filter FullyQualifiedName~P4T06G4K --logger "console;verbosity=minimal"
dotnet test C:\Users\infin\candu\tests\ReactorSim.Golden.Tests\ReactorSim.Golden.Tests.csproj --configuration Release --nologo --no-restore --filter FullyQualifiedName~P5T10 --logger "console;verbosity=minimal"
python tools/IndependentReducedAuthority/ProfileDigestAudit.py data/golden/p4-t06-g4k-independent-authority-v1.json
```

Result: G4-R6 consumer 3/3, P5-T10 consumer 2/2, and
`P4_T06_G4K_PROFILE_DIGEST_PASS profiles=6`; zero failures/skips. Artifact
hashes remain `897C0B4DC055F59B21B110F4674903C12E795E9CFAAF2131CB3AB46E042EDE6A`
and `E927A014C15F13341BD4739C59BC8F9C7D8BD4E36A74A0C37DC669AB83098F4B`.

### Formatter verification (T0)

```text
dotnet format C:\Users\infin\candu\ReactorSim.sln whitespace --verify-no-changes --no-restore
```

Result: exit code 0; no formatting changes required.

## Numerical differences

No numerical baseline, equation, tolerance, or approved golden value changed.
The correction adds restore authentication and restart reconstruction only; it
does not alter burnup arithmetic, interpolation, or the approved ReducedModel
artifact.

## Deferred validation

- Same-context independent code review (high) and final G5 disposition remain
  required.
- Direct DONJON5/CANDU reproduction, full-core authority, T6 regeneration,
  Unity/device/mobile, and release checks remain deferred or not applicable to
  this engine-neutral Core correction.

## Blockers, risks, and follow-up

- Blockers: none within the selected ReducedModel/Core implementation scope;
  G5 is closed `PASS` for the bounded scope.
- Risk triggers: lifecycle-authenticated snapshot digests, complete inventory
  history, burnup/refuelling transitions, determinism, save/replay, and public
  schema changes triggered T3 validation and independent high review.
- Risks accepted or deferred: the explicit archive v2 boundary does not
  provide backward compatibility for the incomplete v1 shape; direct external
  authority and full-core evidence remain deferred.
- Follow-up: activate the Phase 6/G6 task-definition chain from the
  implementation plan and frozen RRS specification; direct external authority
  remains separately deferred.

## Token and cost accounting

Per-task root and reviewer allocation telemetry is unavailable. The reused
reviewer receipt is not allocable to P5-T16; no per-worker cost is invented.

| Field | Value | Source/notes |
|---|---:|---|
| Input tokens | `Unavailable` | no allocable per-task receipt |
| Cached input tokens | `Unavailable` | no allocable per-task receipt |
| Cache-write input tokens | `Unavailable` | no allocable per-task receipt |
| Output tokens | `Unavailable` | no allocable per-task receipt |
| Reasoning output tokens | `Unavailable` | no allocable per-task receipt |
| Total tokens | `Unavailable` | no allocable per-task receipt |
| Estimated cost | `Unavailable` | no allocable price source/formula exposed |
| Goal-service total | `2,624,123` | final goal-service telemetry at Phase 5 completion; not added to request totals |

Cost formula/basis: unavailable; no allocable per-task billing receipt or
price source was exposed.

## Next eligible task

`Phase 6 / G6 task-definition chain` - instantiate the seven Phase 6 work
packages from the implementation plan and frozen RRS specification before
execution. Direct external authority remains a separate deferred path.

Source: [`AGENTS.md`](../../AGENTS.md),
[`docs/spec/refuelling-burnup-transitions-v1.md`](../spec/refuelling-burnup-transitions-v1.md),
and [`TASK_REPORT_TEMPLATE.md`](TASK_REPORT_TEMPLATE.md).
