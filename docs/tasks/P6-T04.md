# P6-T04 - bulk moderator poison state and influence-map admission

## Outcome

Status: `COMPLETE / SYNTHETIC TEST-ONLY`

The owner-approved P6-T04 synthetic fixture is implemented in engine-neutral
Core. It validates poison-owned state, explicit moderator volume and derived
concentration, separate rate-limited add and slow withdraw projections,
setup-only Prescribed bounds, a six-target/two-group positive absorption
influence map, deterministic canonical digests, package/manifest binding, and
local state/map scratch rollback. No production CANDU, external-reference, or
golden-data claim is made.

Effectiveness: `SUCCESS`

The earlier authority-only execution was blocked until Kevin Ho supplied the
separate P6-T04 authority. The exact approval is now preserved in
[`P6-T04-OWNER-APPROVAL.md`](P6-T04-OWNER-APPROVAL.md), and this report
records the reopened implementation and validation execution.

## Historical initial authority audit (preserved)

The initial P6-T04 execution was `BLOCKED / PENDING OWNER AUTHORITY`. It found
that earlier G4-R6 ReducedModel poison evidence was not a P6-T04 add/withdraw
authority and did not silently reuse it. The authority packet was recorded in
[`P6-T04-OWNER-APPROVAL-DRAFT.md`](P6-T04-OWNER-APPROVAL-DRAFT.md), with no
runtime source or package admitted in that execution.

That bounded audit read `AGENTS.md`, the complete implementation plan, the
Phase 6 chain, the frozen P2-T04 specification, P1-T08 and its literature
digest, P6-T01/P6-T02/P6-T03 reports, and the task-report template. Applicable
P1-T08 context rows remain `S1-R04`, `S1-R08`, `S1-R10`, `S3-R05`, `S6-R03`,
`S6-R05`, and `S6-R06`; they do not authorize the synthetic map values or a
production/golden claim.

The historical reviewer was `01a020e0-69d3-7c50-88b3-be23c534b331`.
Its initial conditional findings (one Medium report-result wording issue and
one Low historical G4-R6 wording issue) were corrected in that same context,
which returned `PASS` with no remaining findings. Actual reviewer model,
reasoning effort, and receipt telemetry were `UNVERIFIED`.

## Current execution and review evidence

- Role: root implementer.
- Requested implementation model / reasoning: `GPT-5.6 Luna / high`.
- Actual implementation model / reasoning receipt: `UNVERIFIED`; task-level telemetry is not exposed.
- Attempts: one bounded implementation attempt; artifact produced within the task timebox.
- Fresh independent reviewer: `01a0210b-bdfb-7aa1-b285-3a3001db5f46` (`Maxwell`).
- Reviewer request: `GPT-5.6 Luna / high`; actual model/reasoning receipt: `UNVERIFIED`.
- Review rationale: a fresh reviewer was required because the candidate changed from the historical authority-only audit; the same reviewer context is retained for final disposition after corrections.
- Interim review disposition: `CONDITIONAL PASS`, no High findings; the stale-report and package-semantic-coverage findings were corrected in this execution.
- Artifact status: implementation, tests, package, manifest, approval record, and scope handoff produced.

## Files added or changed

- `src/ReactorSim.Core/Domain/Phase6BulkPoisonContracts.cs` - immutable state, derived concentration, setup/action modes, exact mass projection, and action digest.
- `src/ReactorSim.Core/Domain/Phase6BulkPoisonInfluenceMapContracts.cs` - owner-bound six-target/two-group map, canonical topology/reference/map digests, positive overlay, and disabled-zero assertions.
- `tests/ReactorSim.Core.Tests/P6T04BulkPoisonContractsTests.cs` - focused state, accounting, units/bounds, binding, deterministic overlay/rollback, and package/manifest tests.
- `data/packs/p6-t04-synthetic-bulk-poison-map-v1.json` - synthetic-only typed evidence package.
- `data/packs/p6-t04-synthetic-bulk-poison-map-v1.manifest.json` - artifact length/SHA-256 and digest manifest.
- `docs/tasks/P6-T04-OWNER-APPROVAL.md` - exact owner approval supplied for this execution.
- `docs/tasks/P6-T04.md` - this final task report; the initial blocked audit is preserved above.
- `docs/PROJECT_SCOPE.md` - current P6-T04 completion and P6-T05 routing.

The draft approval packet remains historical input. No queue implementation,
scenario package, production data, external-reference data, golden-data
promotion, commit, push, or publication was added. The T4 smoke copied the
validated Core plugin into the existing ignored Unity plugin path as test setup
only; no Unity source was changed.

## Approved authority and implementation boundary

The normative authority is the owner approval plus the frozen P2-T04 bulk-
poison contract and Phase 6 chain. The approved fixture is:

- Fixture: `P6-T04-SYNTHETIC-BULK-POISON-MAP-V1`.
- Owner: `Kevin Ho`.
- Data version: `p6-t04-synthetic-bulk-poison-map-v1`.
- Poison source: `00000000-0000-0000-0000-00000000b405`.
- Map: `00000000-0000-0000-0000-00000000b401`.
- Topology: `00000000-0000-0000-0000-00000000b404`.
- State: `0.5 kg / 5.0 m^3 = 0.1 kg/m^3`; reference concentration `0.0 kg/m^3`.
- Rates: add `0.1 kg/s`; withdraw `0.01 kg/s`.
- Prescribed setup maximum: `1.0 kg`.
- Topology: six explicit `(ChannelId=0..5, BundlePosition=0)` targets.
- Map: two groups and twelve explicit entries, with approved weights `0.08 m^-1 per kg/m^3` and `0.05 m^-1 per kg/m^3`.
- Sign: `P6-T04-SYNTHETIC-POSITIVE-ABSORPTION-V1`; normalization: `None`.

P6-T04 may validate poison-owned state from the complete supplied state and
local state/map scratch rollback. It does not allocate, enqueue, consume,
transition, or roll back shared queue state. Queue allocation/transition/
rollback remains reserved for P6-T06; scenario packages remain reserved for
P6-T07. Hidden chemistry, decay, transport, cleanup, shutdown, trip, safety,
production, external-reference, and golden-data behavior remain excluded.

## Digest and numerical evidence

| Evidence | Value |
|---|---|
| Topology digest | `eaa45936722cb7eafa9e6493918ce1329e90e73b75bcf86a2ea9286db0e3798a` |
| Reference-state digest | `84518339725ff79e6714fa84ca7ae6719544c453647753c7dd874c0d3b64c38e` |
| Map digest | `57fda945e7f24d33d118e3526cdca6401eea8213ee930cb771260c03ec825d19` |
| Initial state digest | `fa37ad317267e00aff656341786789ca090eeb93bb4e3e552af7535e080ac194` |
| Overlay digest | `bb2ca729938811b01c2c6d104a979fd0c744763b9d92a42fa9e48c903a6023f2` |
| Add-action digest at 2 s | `d289b53a11551b0deaddb3d6946ca082e45c9241e0bf924613fbd9a9b0eca53` |
| Map canonical byte length | `1611` |
| State canonical byte length | `224` |
| Package byte length | `6605` |
| Package SHA-256 | `dbed4cfad9ace995282629f20bfa079866612194834022492201fa0cad3b2bdb` |

Numerical checks are synthetic contract evidence only. At `t=2 s`, Add
requests and applies `0.2 kg`, producing `0.7 kg` and `0.14 kg/m^3`. A
Withdraw state at `0.05 kg` over `10 s` requests `-0.1 kg` and applies only
`-0.05 kg`, ending at exactly zero. The initial `0.1 kg/m^3` concentration
above the `0.0 kg/m^3` reference produces group overlays `+0.008 m^-1` and
`+0.005 m^-1`; Disabled produces canonical positive zero for every overlay.
No external or golden numerical difference is claimed.

## Validation commands and results

### T0 - authority and package integrity

The required authority, specification, literature, prerequisite-report, and
template read completed with `P6_T04_AUTHORITY_READ_PASS`. The package and
manifest JSON parse; static whitespace/replacement-character checks pass; and
the independent artifact audit reported:

```text
P6_T04_PACKAGE_STATIC_PASS byte_length=6605 sha256=dbed4cfad9ace995282629f20bfa079866612194834022492201fa0cad3b2bdb
```

The focused package test reconstructs the typed fixture, checks every declared
map entry and unit/sign/normalization/limit field, verifies all package and
manifest identity/digest/boundary fields, and matches manifest artifact
length/SHA-256.

### T1 - focused P6-T04 tests

```powershell
dotnet test C:\Users\infin\candu\tests\ReactorSim.Core.Tests\ReactorSim.Core.Tests.csproj --configuration Release --nologo --no-restore --filter FullyQualifiedName~P6T04BulkPoisonContractsTests --logger "console;verbosity=detailed"
```

Result: exit `0`; `6 passed, 0 failed, 0 skipped`. The detailed run emitted
the digest and canonical-length values recorded above.

### T3 - full Core and approved Golden consumer regression

```powershell
dotnet test C:\Users\infin\candu\ReactorSim.sln --configuration Release --nologo --no-restore
```

Result: exit `0`; Core `132 passed, 0 failed, 0 skipped`; Golden
`19 passed, 0 failed, 0 skipped`.

### T4 - pinned Unity import/compile smoke

Pinned editor: Unity `6000.3.21f1`, revision `c02631ffc030`.

```powershell
& C:\Users\infin\candu\tools\Test-UnityImport.ps1 -UnityEditorPath C:\Users\infin\AppData\Local\Unity\Editors\6000.3.21f1\Editor\Unity.exe -ArtifactsPath C:\Users\infin\AppData\Local\Temp\candu-p6-t04-t4-artifacts-final -LogPath C:\Users\infin\AppData\Local\Temp\candu-p6-t04-t4-import-final.log
```

The invocation from the repository directory first exposed the environment's
missing pinned SDK `10.0.302` (only `10.0.303` is installed). Re-running the
same pinned smoke from `C:\Users\infin`, which is how the successful prior
project checks resolve the SDK, completed with exit `0`: Core Release plugin
build `0 warnings / 0 errors`, and Unity import/compile smoke passed. Prepared
plugin SHA-256:
`6a93ae2a8b9faaddb7a24d2846e7f74e5e638382b66d3c86e420c5339c797daf`.

## Independent review disposition

Fresh reviewer `01a0210b-bdfb-7aa1-b285-3a3001db5f46` performed one bounded
independent high-effort review. The first disposition was `CONDITIONAL PASS`
with no High findings. The stale-report finding and the package semantic
coverage finding were corrected in the same reviewer context. The final
same-context disposition was `PASS` with no High, Medium, or Low findings.
Actual model, reasoning effort, and receipt telemetry are `UNVERIFIED`, so
this is not described as receipt-verified approval.

## Token and cost accounting

Task-level and reviewer-level billing telemetry was not exposed. No
per-worker or per-reviewer allocation is inferred from aggregate goal usage.

| Field | Value | Source/notes |
|---|---:|---|
| Input tokens | `Unavailable` | no task/reviewer receipt exposed |
| Cached input tokens | `Unavailable` | no task/reviewer receipt exposed |
| Cache-write input tokens | `Unavailable` | no task/reviewer receipt exposed |
| Output tokens | `Unavailable` | no task/reviewer receipt exposed |
| Reasoning output tokens | `Unavailable` | no task/reviewer receipt exposed |
| Total tokens | `Unavailable` | no task/reviewer receipt exposed |
| Estimated cost | `Unavailable` | no price source/date or allocable billing formula exposed |
| Goal-service total | `Unavailable` | not exposed; not added to request totals |

Cost formula/basis: unavailable; no provider price or per-request usage
receipt was exposed.

## Deferred tests, risks, and follow-up

- P6-T06 owns shared queue allocation, enqueue, consume, transition, and rollback.
- P6-T07 owns scenario packages and scenario-driven poison recovery evidence.
- No T5 mobile or T6 external/reference-baseline evidence is applicable to this synthetic-only task; those boundaries remain closed.
- Critical blockers for the approved P6-T04 synthetic scope: none.
- Risk trigger fired: public Core state/map contracts, SI units, sign, canonical ordering, deterministic digests, rate accounting, and rollback required T3/T4 escalation and independent review.
- Review-evidence limitation: actual reviewer telemetry is unavailable and remains explicitly `UNVERIFIED`; no receipt-verified approval is claimed.
- The package remains synthetic/test-only and must not be consumed as production, external-reference, or golden authority.

## Next eligible task

`P6-T05` - execute only after its own authority audit and approved input
package. This report stops at P6-T04 as required.

Source: [`AGENTS.md`](../../AGENTS.md),
[`Implementation_plan.md`](../Implementation_plan.md), the frozen P2-T04
specification, and [`TASK_REPORT_TEMPLATE.md`](TASK_REPORT_TEMPLATE.md).
