# G2-C07 - Make the P2-T05 ValueKind mapping mutation-closed

## Outcome

Status: COMPLETE

This bounded corrective task addresses the residual G2-C06 Medium finding from
the failed G2-R2 review. The P2-T05 focused command now asserts the exact
approved `ValueKind` to `PayloadSchemaId` to `QuantityId` mapping for all five
partition rows, not merely the flattened 135-ID set. Independent mutation
evidence shows that a cross-kind reassignment fails closed. The same reviewer’s
final disposition is PASS, with no residual findings; the final scope audit is
recorded below.

## Files created or changed

- `docs/tasks/P2-T05.md` - added the exact approved per-kind/schema mapping and
  mapping assertion to the existing focused command; updated its exact output.
- `docs/tasks/G2-C07.md` - this bounded corrective-task report.

No specification, implementation plan, runtime source, Unity file, gate report,
other historical task report, reference input/output, tolerance, golden value,
or private/raw artifact was changed. Build and Python artifacts remained
external.

## Assumptions and design choices

- The expected mapping is copied only from the already approved committed
  P2-T05 ValueKind partition. It introduces no new physical quantity, schema,
  tolerance, or reference value.
- The check compares each actual row’s exact `PayloadSchemaId` cell and each
  per-schema QuantityId list against the closed expected mapping. The existing
  catalog/crosswalk/partition set, row-shape, vector/component-order,
  serialization, status, applicability, whitespace, fence, and synthetic power
  assertions remain active.
- The correction stays documentation-only and limited to the same focused
  evidence file used by G2-C06. The root made the small review-driven edit
  locally; no overlapping worker edit was introduced.

## Validation commands and results

### Required reading and pre-task scope freeze - T0

```text
Get-Content -Raw AGENTS.md
Get-Content -Raw docs/Implementation_plan.md
Get-Content -Raw docs/gates/G2.md
Get-Content -Raw docs/gates/G2-R1.md
Get-Content -Raw docs/gates/G2-R2.md
Get-Content -Raw docs/tasks/G2-R2.md
Get-Content -Raw docs/tasks/G2-C06.md
Get-Content -Raw docs/tasks/P2-T05.md
Get-Content -Raw docs/spec/observables-validation-methodology-v1.md
Get-Content -Raw docs/tasks/TASK_REPORT_TEMPLATE.md
git status --short --untracked-files=all
```

Result: PASS. The residual Medium finding, failed G2-R2 reports, governing
instructions, complete P2-T05 contract, and current worktree were read. The
pre-task external snapshot was
`C:\Users\infin\AppData\Local\Temp\candu-g2-c07-pre-20260809-2150\git-visible-manifest.tsv`:
731 paths, aggregate SHA-256
`3c57c4cf5f0fa04b6a86226490bd736b2c1d58b8b20cf60382b91df3e5ad3743`.
Allowed files were exactly `docs/tasks/P2-T05.md` and this report.

### Exact focused command rerun - T1

The command block embedded in the amended P2-T05 report was extracted and run
unchanged from the repository root:

```powershell
$report=Get-Content -Raw -LiteralPath 'docs/tasks/P2-T05.md'; $match=[regex]::Match($report,'(?s)```powershell\r?\n@''\r?\n(.*?)''@ \| python -\r?\n```'); if(-not $match.Success){throw 'P2-T05 focused command block not found'}; $match.Groups[1].Value | python -; $exit=$LASTEXITCODE; "G2_C07_T1_EXIT=$exit"; if($exit -ne 0){exit $exit}
```

Exact output:

```text
P2_T05_T1_REPRO_PASS catalog_rows=135 catalog_ids=135 crosswalk_rows=135 crosswalk_ids=135 partition_rows=5 partition_ids=135 partition_mapping=exact vectors=11 component_orders=11 power_total=10W region=5W fraction=0.5 fences=6 trailing_whitespace=0
G2_C07_T1_EXIT=0
```

Result: exit code 0; PASS. The embedded command parses the five actual
ValueKind rows, checks their exact payload-schema cells and all per-schema
QuantityId lists against the approved mapping, and retains the prior exact
three-way set, vector, serialization, fence, and synthetic arithmetic checks.

### Mutation closure check - T1

An external in-memory harness executed the exact embedded validator against
four mutated specification strings and never wrote a repository file. The
mutations were an incomplete crosswalk row, an extra partition ID, a swap of
`inventory.bundle_id_uniqueness` between `Structured` and `Scalar`, and an
unbalanced fence sequence.

```text
G2_C07_MUTATION_PASS malformed_row=REJECTED extra_id=REJECTED partition_reassignment=REJECTED unbalanced_fence=REJECTED
```

Result: exit code 0; PASS. The per-kind/schema assertion rejects the
cross-kind reassignment even though the flattened QuantityId set remains
unchanged.

### Repository format check - T0

```powershell
& .\tools\Check-Format.ps1; "G2_C07_FORMAT_EXIT=$LASTEXITCODE"; if($LASTEXITCODE -ne 0){exit $LASTEXITCODE}
```

Result: exit code 0; PASS (`G2_C07_FORMAT_EXIT=0`).

### Required independent Sol review

The same read-only `gpt-5.6-sol` high reviewer Darwin
(`019fe7f4-ab48-7f52-bd5c-b1005c6e5664`) was reused after the G2-R2 review, in
accordance with the reduced reviewer cadence. The initial C07 review found one
Low wording contradiction: the report said no historical task report changed
while listing the permitted P2-T05 report amendment. The wording was corrected
to “other historical task report,” the focused/format checks remained PASS,
and the same reviewer returned final `PASS` with no Critical, High, Medium, or
Low findings.

## Numerical differences

Not applicable; no numerical behavior, equation, unit, convergence criterion,
tolerance, reference baseline, golden data, or runtime code changed.

## Deferred validation

- G2 rerun - deferred until G2-C07’s final Sol disposition, task report, and
  mechanical scope audit are complete.
- T3/T4 runtime and Unity suites - not triggered by this report-only focused
  evidence correction; prior authorized G2-C01/G2-C04 evidence is unchanged.
- T6 reference rerun and T5 mobile validation - not triggered.

## Blockers, risks, and follow-up

- Blockers: none for G2-C07 after final Sol-high PASS; the next gate rerun is
  separately deferred until this final scope audit is recorded.
- Risk triggers: deterministic serialization/schema evidence and gate
  reproducibility triggered independent Sol review; no runtime numerical
  trigger was introduced.
- Risks accepted or deferred: no tolerance or reference value was invented;
  Golden tests remain undiscoverable in prior T3 evidence; external Unity cache
  paths remain pre-existing and untouched.
- Follow-up: after final C07 PASS, rerun G2 as a new candidate; do not start
  Phase 3 implementation.

## Next eligible task

`G2-R3` - rerun G2 after G2-C07’s final review and scope audit.

## Final mechanical scope audit

The final audit compared every Git-visible path, byte length, and SHA-256
against the frozen pre-task manifest:

`C:\Users\infin\AppData\Local\Temp\candu-g2-c07-pre-20260809-2150\git-visible-manifest.tsv`

The external final manifest is
`C:\Users\infin\AppData\Local\Temp\candu-g2-c07-final-audit-20260809-2205\git-visible-manifest.tsv`.
The result is `pre_count=731`, `after_count=732`, exactly one added path
(`docs/tasks/G2-C07.md`), exactly one changed path
(`docs/tasks/P2-T05.md`), zero removals, and zero new private/raw/cache or
generated-artifact paths. The full scan retains 521 pre-existing
cache/generated/binary-pattern paths and two pre-existing synthetic listing
fixtures; neither set was added by C07. The report omits its own aggregate
manifest hash; the external manifest contains the exact final per-path lengths
and SHA-256 values.

Source: [`docs/Implementation_plan.md`](../Implementation_plan.md),
[`docs/gates/G2-R2.md`](../gates/G2-R2.md), and [`docs/tasks/P2-T05.md`](P2-T05.md).
