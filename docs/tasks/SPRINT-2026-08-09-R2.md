# Sprint 2026-08-09-R2 - G2 corrective closure

## Outcome

Status: COMPLETE

This sprint resumed from the audited G2-C01 safe state and applied the revised
review policy: independent Sol reviews used high reasoning, not max reasoning,
with reduced cadence and reviewer reuse. The sprint completed the eligible
corrective sequence through G2-C07 and reran G2 as G2-R3. G2-R3 passed after the
failed G2-R2 candidate was preserved accurately and its residual mapping defect
was corrected. No Phase 3 implementation was started.

The pre-existing unsuffixed `docs/tasks/SPRINT-2026-08-09.md` was preserved; it
is the historical blocked sprint report. This `-R2` report is the required
same-date report for the resumed sprint.

## Goal policy, restart, and usage

- Sol policy for this run: `gpt-5.6-sol`, high reasoning, read-only review;
  the same reviewer was reused for the G2-R2, G2-C07, and G2-R3 sequence.
- Luna was used with max reasoning for bounded implementation work when viable.
  The G2-C06 Luna attempt produced no checkpoint or artifact within its timebox;
  the root agent completed the bounded correction locally.
- The goal service would not create a revised goal while the existing blocked
  goal record was unfinished. The revised Sol-high policy was nevertheless
  applied to this restart, and the existing tracked goal is closed only after
  this report and its final audit.
- The weekly allowance denominator was not exposed. Weekly-percentage
  consumption is therefore not measurable; no percentage was invented.
- Goal-service telemetry immediately before closure: `112115` tokens and
  `465` elapsed seconds. Per-worker token breakdown was not exposed.

## Task and gate ledger

| ID | Type | Result | Review and evidence |
|---|---|---|---|
| P1-T08 | implementation task | COMPLETE | Final Sol-high PASS; focused check, format, T3, and scope audit PASS. |
| G2-C04 | corrective task | COMPLETE | Final Sol-high PASS; T1, T3, and Unity T4 PASS. |
| G2-C06 | corrective task | COMPLETE | Initial Sol findings corrected; final Sol-high PASS and focused/format checks PASS. |
| G2-R2 | gate attempt | FAIL / INCOMPLETE | Root T0, T3, and T4 passed; Sol-high found one Medium mapping defect and one Low report-status defect. Reports were corrected to FAIL/INCOMPLETE. |
| G2-C07 | corrective task | COMPLETE | Exact ValueKind/PayloadSchemaId mapping and mutation rejection added; final Sol-high PASS. |
| G2-R3 | gate rerun | PASS | T0, T3, T4, format, scope audit, and final Sol-high disposition PASS. |

Implementation-task completion rate is 4/4 (100%), counting P1-T08,
G2-C04, G2-C06, and G2-C07. The two G2 reruns are gate attempts, not
implementation tasks. First-pass rate for this sprint's gate attempts is 1/2
(50%): G2-R2 failed and G2-R3 passed. Final corrected gate status is G2 PASS
at G2-R3; the earlier G2-R2 candidate remains correctly recorded as FAIL.

## Completed work

### P1-T08

The approved manifest/digest contract was completed and its private-path check
remained optional and truthful. The focused result was:

```text
P1_T08_T1_FINAL_PASS sources=6 claim_rows=50 domains=8 private_path_check=PASS
```

The final Sol-high review passed after corrections. T3 passed with Core `2/2`
and Golden `0` discoverable cases; the Golden limitation remains documented as
deferred evidence, not a false pass.

### G2-C04

The I/Xe refuelling contract was closed for atomic transition, batch,
discharge, digest, status, and rollback behavior without inventing reference
values. The focused result was:

```text
G2_C04_T1_REPORT_PASS required_terms=36 enum=P2-T05 tables=clean fences=balanced links=PASS whitespace=clean
```

The final headless T3 run exited `0` with Core `2/2` passed, failed `0`,
skipped `0`, and Golden `0` discoverable tests. The authorized external Unity
T4 evidence passed EditMode, PlayMode, and StandaloneWindows64, each with
`total=1 passed=1 failed=0 skipped=0`; the desktop log recorded a successful
Windows player build. All build and Unity outputs stayed outside the repository.

### G2-C06 and G2-C07

The P2-T05 focused evidence check was made runnable and then hardened after the
G2-R2 review. The final command parses exact catalog, crosswalk, partition,
vector, component-order, field, fence, and whitespace content. G2-C07 added
the exact per-kind and `PayloadSchemaId` mapping assertion and a mutation
harness. Final output was:

```text
P2_T05_T1_REPRO_PASS catalog_rows=135 catalog_ids=135 crosswalk_rows=135 crosswalk_ids=135 partition_rows=5 partition_ids=135 partition_mapping=exact vectors=11 component_orders=11 power_total=10W region=5W fraction=0.5 fences=6 trailing_whitespace=0
G2_C07_MUTATION_PASS malformed_row=REJECTED extra_id=REJECTED partition_reassignment=REJECTED unbalanced_fence=REJECTED
```

The focused and format checks exited `0` after the final review fix. The final
Sol-high disposition was PASS with no Critical, High, Medium, or Low findings.

### G2-R2 and G2-R3

Both reruns used the exact same external Unity evidence from the authorized
G2-C04 run. The G2-R2 root checks passed, but the independent review correctly
rejected the candidate because a cross-kind mapping swap could preserve the
flat ID set. The root changed the G2-R2 gate report to FAIL and task report to
INCOMPLETE before proceeding.

After G2-C07, the root reran G2-R3. Its admission output was:

```text
G2_R3_T0_PASS prerequisites=25 corrective_reports=7 prior_failed_rerun=recorded tolerance_authority=PASS mapping_mutations=PASS t4_evidence=PASS phase3=NOT_STARTED
```

The exact T3 command was:

```powershell
$artifacts='C:\Users\infin\AppData\Local\Temp\candu-g2-r3-t3-20260809-2240'; New-Item -ItemType Directory -Force -Path $artifacts | Out-Null; & .\tools\Test-FullHeadlessSuite.ps1 -ConfirmFullSuite -ArtifactsPath $artifacts; $exit=$LASTEXITCODE; "G2_R3_T3_EXIT=$exit artifacts=$artifacts"; if($exit -ne 0){exit $exit}
```

It exited `0` with Core `2/2` passed and Golden `0`. The final T4 evidence
hashes and Unity editor identity are recorded in the G2-R3 gate report; all
three required XML results passed and the desktop log showed a successful
Windows player. The reused Sol-high reviewer returned final PASS with no
findings.

## Validation and scope evidence

- `& .\tools\Check-Format.ps1` exited `0` for every final report/check where
  it was required.
- T3 artifacts, TRX files, Unity projects, logs, and player outputs were
  written under `C:\Users\infin\AppData\Local\Temp`, not the repository.
- The final G2-R3 audit compared every Git-visible path, byte length, and
  SHA-256. The external final manifest is
  `C:\Users\infin\AppData\Local\Temp\candu-g2-r3-final-audit-20260809-2310\git-visible-manifest.tsv`.
  It recorded 732 paths before the G2-R3 reports and 734 afterward, exactly
  two added reports, no removals, no unrelated changes, and no new artifact
  delta. Its final aggregate hash was
  `dda96df6c08df64dc94c59a4784bb9d78a6ded8e2a2bbe4c4d09e2f356245e55`.
- The sprint report itself was frozen before editing at
  `C:\Users\infin\AppData\Local\Temp\candu-sprint-20260809-r2-pre-20260809-2350\git-visible-manifest.tsv`:
  734 paths, aggregate SHA-256
  `e32298a3bda5fc12c89316e934c8edd4618892e4b1b3de9bac7afb34299c6b43`.
- Pre-existing Unity `Library`/`Temp`/cache paths remain present and were not
  deleted. No new private/raw DRAGON5 or DONJON5 inputs, repository caches, or
  generated artifacts were introduced.

## Estimates, blockers, and deferred work

- The implementation estimate was optimistic for G2-C04 and G2-C06 because
  review-driven contract hardening required multiple correction cycles. G2-C07
  was added only after the independent G2-R2 finding and was completed within
  its bounded scope. G2-R3 was primarily evidence and reporting work.
- Blockers: none for the completed G2 closure. The goal-service limitation on
  replacing an unfinished blocked goal was recorded; it did not prevent the
  revised review policy or repository work.
- Risk triggers: specification/serialization/mapping, deterministic evidence,
  and Unity adapter evidence triggered T3, T4, and independent Sol review.
- Deferred: no Phase 3 implementation begins until the next one-purpose task
  is explicitly prepared and approved. Golden evidence remains limited by the
  previously documented undiscoverable Golden assembly; no false Golden pass
  is claimed. No task loosened a tolerance or replaced golden data.

## Next eligible task

Prepare and approve the next one-purpose Phase 3 task from the post-G2 queue.
Do not begin implementation until its specification, allowed files,
prerequisites, focused check, and gate are explicitly recorded.

## Final mechanical scope audit

After this report was written, the final audit compared every Git-visible path,
byte length, and SHA-256 against the pre-report snapshot. The final manifest is
at:

`C:\Users\infin\AppData\Local\Temp\candu-sprint-20260809-r2-final-audit\git-visible-manifest.tsv`

Expected result: 734 paths before and 735 afterward, exactly this sprint report
added, no removals, no changed pre-existing paths, and no new repository
artifact/cache/private-input paths. The report intentionally omits its own
aggregate manifest hash; the external manifest is the authoritative per-path
record. Goal closure occurs only after this audit passes.

Source: [`docs/Implementation_plan.md`](../Implementation_plan.md),
`AGENTS.md`, and the task/gate reports linked above.
