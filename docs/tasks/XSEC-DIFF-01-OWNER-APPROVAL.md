# XSEC-DIFF-01 owner approval - source diffusion-coefficient route

**Recorded:** 2026-08-29
**Decision:** APPROVED / BOUNDED

## Owner direction

The project owner directed completion of the next XSEC-03 work after
`XSEC-FIELD-01` established that the similarly named `REAC DIFF` output is a
scattering cross section rather than the mapping-required diffusion
coefficient. This approval authorizes one bounded source/deck and mapping
decision to select, prove, or reject an official DRAGON5 method that produces
the required isotropic `DIFF` coefficient for the existing two-state,
whole-cell candidate route.

## Bounded authority

The task may inspect official DRAGON5 primary guidance and pinned Version5
source, create external temporary source variants, run the owner-authorized
offline JEFF/WLUP fixture, and create only the reviewed deck/mapping,
path-free manifest, report, and scope evidence named in its request.

## Non-authorization

This approval does not authorize changing the source case, geometry, material
model, group boundary, source controls, source assertions, runtime equations,
Core/CLI/Unity behavior, runtime schema, data pack, full-core case,
golden/reference baseline, redistribution, publication, or release. Raw
libraries, procedures, captures, listings, source values, and derived values
remain external-only. A later semantic-decode task remains separately
authorized and owns any numerical candidate admission.

## Reserved successor

The concrete successor identifier is `XSEC-03-R4`. This owner direction
authorizes its separate task-definition record after `XSEC-DIFF-01` receives
final review PASS; it does not authorize `XSEC-03-R4` execution within this
task or admit numerical data.
