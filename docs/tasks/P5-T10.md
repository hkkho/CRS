# P5-T10 - approved ReducedModel sequence comparison

## Outcome

Status: COMPLETE

P5-T10 consumed the already-approved G4-R6 `ReducedModel` package and the
project-authored G4J representative sequence definition as a test-only Golden
consumer. It exercised both explicit S4 directions, verified the two refuelling
event records, and checked the exact structural mapping of the existing
project-authored burnup projection, bundle identity, cumulative-energy fields,
insertion time, discharge preservation, and inventory invariants after the two
shifts. It did not independently integrate power into burnup, establish a
numerical drift baseline, create a new runtime contract, equation, tolerance,
golden authority, or direct DONJON5/CANDU claim.

Effectiveness: SUCCESS

## Execution and model evidence

- Role: root implementer; independent code review (high) reviewer.
- Requested model / reasoning: GPT-5.6 Luna / high for bounded implementation and documentation; code review (high) for the final disposition.
- Actual model / reasoning: root execution telemetry is not exposed as a per-task receipt; final reviewer receipt verified `gpt-5.6-sol` / high.
- Execution receipt or telemetry source: reviewer context `01a01217-337b-79e1-9b86-88232ccf91d1`; final P5-T10 review turn `01a0125d-0b07-7731-a67b-fe6f0a461372`; local receipt `C:\Users\infin\.codex\sessions\2026\08\17\rollout-2026-08-17T19-38-25-01a01217-337b-79e1-9b86-88232ccf91d1.jsonl`.
- Attempts: one implementation candidate and one bounded review attempt; root task elapsed time unavailable; reviewer elapsed time `249532 ms` from the receipt.
- Artifact/checkpoint status: produced.
- Review disposition: PASS; no actionable findings.
- Review evidence verification: receipt-verified.
- Reviewer reuse/fresh-review rationale: same reviewer context reused from G4-R6 for the Phase 5 candidate; no fresh reviewer was started.

## Files created or changed

- `tests/ReactorSim.Golden.Tests/P5T10ReducedModelSequenceComparisonTests.cs` - test-only consumer for the approved reduced-model sequence and both S4 directions.
- `docs/tasks/P5-T10.md` - this task report.

Pre-existing G4J/G4K definitions, the corrected G4-R6 approved payload and
manifest, Core contracts, project links, and runtime files were inspected but
not changed by P5-T10. The G4-R6 correction addenda remain the prerequisite
record for the current approved hashes.

## Assumptions and design choices

- The G4-R6 approved artifact is consumed only within its declared
  `RepresentativeReducedModel` / `ReducedModel` scope. Current SHA-256 values
  are artifact `897c0b4dc055f59b21b110f4674903c12e795e9cfaaf2131cb3ab46e042ede6a`
  and manifest `e927a014c15f13341bd4739c59bc8f9c7d8bd4e36a74a0c37dc669ab83098f4b`.
- The G4J sequence definition supplies the project-authored five-case context
  and the explicit equilibrium burnup projection. The refuelled comparison
  maps retained bundles through the approved S4 shifts: ch0 positions 4-11
  originate from positions 0-7, and ch1 positions 0-7 originate from
  positions 4-11; fresh positions have zero burnup and energy.
- The test constructs a post-burnup `BundleInventory` from the explicit
  project-authored projection and verifies the frozen accounting identity
  `CurrentBurnup = InitialBurnup + CumulativeFissionEnergy / HeavyMetalMass`.
  This proves structural mapping and identity/accounting preservation only; it
  is not independent power-to-burnup integration or a numerical drift claim.
- Literature applicability is `S1-R07`, `S4-R03`, `S4-R04`, `S5-R05`, and
  `S6-R05`, reviewed as context/methodology and candidate-design evidence only.
  No literature number becomes a runtime or golden authority here.
- Direct DONJON5/CANDU production authority, full-core coverage, and production
  tolerance approval remain deferred to the existing authority path; this task
  does not relabel the reduced-model package.

## Validation commands and results

### Focused P5-T10 consumer (T1)

```text
dotnet test C:\Users\infin\candu\tests\ReactorSim.Golden.Tests\ReactorSim.Golden.Tests.csproj --configuration Release --nologo --no-restore --filter FullyQualifiedName~P5T10 --logger "console;verbosity=minimal"
```

Result: exit code 0; 2/2 P5-T10 tests passed, zero failures and zero skips.

### Full headless solution suite (T3)

```text
dotnet test C:\Users\infin\candu\ReactorSim.sln --configuration Release --nologo --no-restore
```

Result: exit code 0; Core 103/103 passed and Golden 19/19 passed, zero
failures and zero skips.

### Formatter check (T0 environment limitation)

```text
dotnet format C:\Users\infin\candu\ReactorSim.sln whitespace --verify-no-changes --no-restore
```

Result: unavailable in this environment because the repository pins SDK
`10.0.302` in `global.json` while the installed SDK is `10.0.303`. The pinned
file was not changed. The Release build/test path completed successfully and
the touched test passed analyzer checks during compilation.

### G4-R6 prerequisite evidence

The current G4-R6 corrected artifact/manifest hashes, profile-digest audit,
focused approved consumer, and receipt-verified final review are recorded in
the G4-R6 correction addenda. P5-T10 did not regenerate or alter that package.

## Token and cost accounting

Request-level root telemetry and a per-task reviewer allocation are not
available. The reviewer receipt exposes cumulative usage for the reused
G4-R6/P5-T10 reviewer session, but it is not allocable to this task and is not
added to a request-level total.

| Field | Value | Source/notes |
|---|---:|---|
| Input tokens | `11786281` cumulative | reviewer receipt, reused session; not allocable to P5-T10 |
| Cached input tokens | `11407616` cumulative | reviewer receipt, reused session; not allocable |
| Cache-write input tokens | `0` cumulative | reviewer receipt |
| Output tokens | `39974` cumulative | reviewer receipt, reused session; not allocable |
| Reasoning output tokens | `18720` cumulative | reviewer receipt, reused session; not allocable |
| Total tokens | `11826255` cumulative | reviewer receipt, reused session; not a P5-T10 allocation |
| Estimated cost | `Unavailable` | no applicable price source or per-task allocation exposed |
| Goal-service total | `655135` at report time | goal telemetry; report separately and do not add to request totals |

Cost formula/basis: unavailable; no per-request price or allocable cost
telemetry was exposed.

## Numerical differences

No independent external numerical baseline was changed or admitted. The test
uses exact comparisons against the existing approved reduced-model projection
as a structural consumer and exact contract/accounting identities. It does not
measure independent power-to-burnup integration error or accumulated numerical
drift. No new tolerance or golden value was introduced; direct
external/reference numerical differences remain deferred.

## Deferred validation

- Direct DONJON5/CANDU reference-case reproduction and production/full-core
  authority - deferred to the existing reference-admission and production G4
  path because the required case, data identity, normalization, licensing,
  and reproducibility evidence remain outside this bounded task.
- Unity adapter/device validation - not applicable; this change is isolated to
  a Golden test consumer and does not change Unity, serialization, or runtime
  adapter code.
- T6 reference regeneration - not applicable; no external reference baseline
  or approved golden artifact was changed.

## Blockers, risks, and follow-up

- Blockers: none for the approved bounded `ReducedModel` sequence scope.
- Risk triggers: refuelling, burnup, identity, energy accounting, deterministic
  history, and approved-golden consumption triggered T3 and independent
  code-review (high); both passed.
- Risks accepted or deferred: direct production/external authority, full-core
  coverage, independent burnup-integration evidence, and production thresholds
  remain explicitly deferred and are not implied by this completion.
- Follow-up work: `P5-T11` supplies the missing complete-state burnup commit,
  lookup invalidation, and composed recompute/refuelling evidence before `G5`
  is closed; preserve the direct-authority limitation.

## Next eligible task

`P5-T11` - complete-state burnup commit, lookup invalidation, and composed
recompute/refuelling evidence; then rerun `G5`. Direct production/external
authority remains a separate deferred path.

Source: [`AGENTS.md`](../../AGENTS.md) and
[`TASK_REPORT_TEMPLATE.md`](TASK_REPORT_TEMPLATE.md).
