# XSEC-SPH-03 owner approval - official persistent-capture interface proof

**Decision:** APPROVED / BOUNDED

The project owner has directed creation of a separate authorized solution that
can make `XSEC-03-R2` eligible. This authority authorizes the bounded
`XSEC-SPH-03` task to research, construct, and validate an **officially
supported, executable** DRAGON5/CLE-2000 interface for retaining the exact
post-EDI/pre-SPH TCWUX11 object after normal completion.

The task may use the already authorized external JEFF/JEF/WLUP fixture,
pinned DRAGON5 container, official primary documentation and source examples,
and external temporary candidate inputs/outputs. It may add an external
caller/adapter only when the exact CLE syntax and parameter/object lifecycle
are supported by the pinned release's official documentation or executable
example. It must preserve both original
`EDITION := SPH: EDITION VOLMATF INTLINF ;` calls, all four source assertions,
and every existing mapping field/unit/group/normalization authority.

This is not authority to edit an assertion, source physics, geometry,
materials, group structure, unit, convergence rule, tolerance, mapping,
runtime schema, or source output meaning. It does not authorize accepting an
abnormal termination, a transient/non-retained object, an undocumented CLE
workaround, or an inferred field export. It does not authorize raw-data or
tool redistribution, a runtime pack, Core/CLI/Unity integration, release,
publication, or golden/reference approval.

If no officially supported interface survives the required clean reproductions,
the task must remain BLOCKED and preserve the evidence. `XSEC-03-R2` may run
only after this task's report and independent high review establish a PASS;
that later rerun still owns its separate T6 export, semantic validation, and
admission decision.
