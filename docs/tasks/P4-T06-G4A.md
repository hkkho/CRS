# P4-T06-G4A - G4 comparison admission and representative-case disposition

## Outcome

Status: COMPLETE

`P4-T06-G4A` resolves the G4 evidence-admission block at the approved
reduced/synthetic boundary selected by `P4-T06-R2D`. The three deterministic
static cases already emitted by R5 are admitted as the bounded comparison
basis for the next consumer task:

- `fresh_candidate`;
- `equilibrium_like_candidate` (a uniform mid-table material state, not an
  inferred operating equilibrium); and
- `midcycle_interpolation_candidate`.

This is a path-free scope and provenance decision, not a golden-data or
tolerance approval. The existing R5 output remains `candidate`, all 57 R5
records remain `Deferred`/`Candidate`, and no R5 bytes were rewritten or
relabelled. `P4-T06-G4B` may add consumers for only these three admitted static
cases. A fresh `G4` remains the only authority that may approve spatial
tolerances or an `ApprovedGolden` disposition.

The remaining R5 slots are explicitly dispositioned rather than silently
covered:

| R5 case | G4A disposition | Reason and owning follow-up |
| --- | --- | --- |
| `refuelled_perturbation_candidate` | `DeferredForG4` | R5 has no usable state because the inherited two-iteration policy reported `Nonconverged`. It is not converted to a pass or a last iterate; integrated refuelling/depletion comparison remains with `P5-T10`/`G5` after their prerequisites. |
| `rrs_perturbation_candidate` | `DeferredForG6` | No approved RRS influence map or overlay exists. The case remains visible for the Phase 6/G6 RRS work. |
| `poison_perturbation_candidate` | `DeferredForG6` | No approved poison map, concentration, or overlay exists. The case remains visible for the Phase 6/G6 poison work. |

The user's redistribution-rights confirmation is recorded as owner
authorization for the selected external artifact boundary, but direct
Candu6.x2m fields remain retired from P2-T02 admission. The reduced runtime
scope uses only the existing synthetic fixture, R4 candidate pack, R5
candidate comparison output, and frozen P2 contracts. No reference payload,
new equation, unit conversion, normalization rule, convergence rule,
tolerance, golden value, public schema, or runtime dependency was added.

Effectiveness: SUCCESS

## Execution and model evidence

- Role: root implementer; bounded comparison-admission and coverage decision
- Requested model / reasoning: GPT-5.6 Luna / high; code review (high)
- Actual model / reasoning: `UNVERIFIED`
- Execution receipt or telemetry source: root task context; reviewer
  `01a0080f-dc22-7811-9b10-2a60a9e299d0`; receipt telemetry unavailable
- Attempts: 1 bounded decision-record attempt plus 1 bounded same-context
  review/correction pass; elapsed time `Unavailable`
- Artifact/checkpoint status: produced
- Review disposition: `PASS`; no High, Medium, or Low findings remain
- Review evidence verification: `UNVERIFIED`
- Reviewer reuse/fresh-review rationale: the same reviewer context from G4 and
  R5 was reused for the bounded correction pass; no fresh reviewer was used

## Files created or changed

- `docs/tasks/P4-T06-G4A.md` - records the path-free admission basis, exact
  artifact bindings, case dispositions, non-authorizations, and handoff.
- `docs/tasks/ROUND-2026-08-16-P4-REFERENCE-CHAIN.md` - updates G4A to
  complete and routes the next task to G4B.
- `docs/PROJECT_SCOPE.md` - records the G4A scope decision and the G4B
  frontier.

Pre-existing files inspected but not changed include `AGENTS.md`, the complete
`docs/Implementation_plan.md`, the P2-T01/P2-T02/P2-T05 specifications, the
P1-T08 report and digest, `P4-T06`/R1/R2/R2A/R2B/R2C/R2D/R3/R4/R5 reports,
`docs/gates/G4.md`, the R4 pack/manifest, the R5 comparison output, the P4-T05
synthetic fixture test, and the P4-T08 scenario manifest.

## Authorities and literature coverage

The decision uses the owner authorization recorded in R1 and R2D, the frozen
P2 contracts, the R3 reduced-model boundary, the R4 candidate pack/manifest,
the R5 comparison output, and the G4 findings. Applicable P1-T08 digest rows
are `S1-R02`, `S1-R03`, `S1-R04`, `S1-R06`, `S1-R11`, `S5-R03`, `S5-R04`,
`S5-R09`, `S6-R03`, and `S6-R08`. These are context or candidate-evidence
rows only; none authorizes a runtime equation, source mapping, unit
conversion, normalization, tolerance, or golden value.

## Path-free admission binding

The admitted G4B basis is the existing three-node synthetic contract fixture
and its R5 comparison output. The following identities were checked without
recording a host-local path as evidence:

| Binding | Identity |
| --- | --- |
| Solver/model contract | Existing `ReactorSim.Core` P2-T02 spatial solver and P2-T05 observable contract; no source or equation change in G4A. |
| Offline comparison tool | `ReactorSim.SpatialComparison`, task implementation `P4-T06-R5`; `Program.cs` SHA-256 `0f77d04c0ee58006191510862f807ed555f9086650fa27518fbc952fbd447760`; project SHA-256 `fa20da5c59b28f08417b17e4d8750e5e288061c87f3bb866d5a0a15d71e5ee21`; target `net10.0`; Core project reference is explicit. |
| Topology/geometry | `p4-t05-homogeneous-three-node-static-solve-v1`: one channel, three explicit positions `(0,0)`, `(0,1)`, `(0,2)`, all reflective boundaries, explicit reciprocal edges, and explicit node volumes from the committed P4-T08 synthetic scenario. |
| Material source | `p4-t06-r4-synthetic-input-v1`; SHA-256 `1680a075cd34867b603e1e786002815b19be360d4e8557cd5485978dc32c1694`; evidence class `synthetic`; transform `identity_projection_v1`; data version `synthetic-v1`; units profile `SI-v1`. |
| Reduced pack | `p4-t06-r4-reduced-candidate-v1`; SHA-256 `0de429b27f78b5e2c5fe724916b14ac8ce45ba05560ab73f3fa4805befd11a0e`; manifest SHA-256 `6e3c19d4f596dfd930b339f983a54b46a6a27b9e83b6652f8e40a058c19e2a8b`. |
| Comparison output | `p4-t06-r5-candidate-snapshots-v1`; SHA-256 `13481d139c2b2f2fd1c043725c0a2ced9f396c2813a954cc604a1266bc4c8211`; benchmark manifest SHA-256 `8a53f7519a3b6597d3382c9e91df356474d7d1e72e8ce5d0055e30ef635a1045`. |
| Units and normalization | Existing `SI-v1` table units and P2-T05 observable units. The synthetic caller-owned target power is the existing `0.9 W` P4-T08 input; no external source power or normalization is converted. |
| Reproducibility | R5 focused validation reported `snapshots=3`, `nonconverged=1`, `not_covered=2`, `records=57`; committed output regeneration was byte-identical. |
| Licensing/provenance | Synthetic artifacts are committed candidate evidence. The owner confirmed redistribution rights for the selected external reference set, but no raw/private reference payload is committed or made a runtime dependency; direct Candu6.x2m field admission remains retired by R2D. |

The three admitted cases are therefore a bounded **reduced/synthetic contract
self-consistency basis**. They are not claimed to reproduce a DONJON5 result,
represent a named station, or establish physical accuracy outside the frozen
synthetic domain.

## Non-authorizations and stop conditions

G4A does not:

- change the R3 boundary or any P2 equation, sign, unit, conductance,
  topology, normalization, or convergence policy;
- create or approve a numeric tolerance, `ApprovedGolden` value, or runtime
  data pack;
- change the R5 status, records, hashes, or deferred findings;
- infer refuelling, RRS, poison, equilibrium, source-array identity, or
  external solver mapping from the synthetic cases; or
- admit a direct Candu6.x2m coefficient, node, volume, energy, conductance, or
  normalization field.

If G4B would need a new comparison rule, an unapproved threshold, a missing
map, or a new source-to-Core mapping, it must stop and request a separate
approved specification/task.

## Validation commands and results

### Path-free admission and case-coverage audit (T1)

```text
$ErrorActionPreference='Stop'; $r5=Get-Content -LiteralPath 'data/comparisons/p4-t06-r5-candidate-snapshots-v1.json' -Raw | ConvertFrom-Json; if($r5.status -ne 'candidate' -or $r5.evidence_class -ne 'synthetic' -or $r5.evidence_approval -ne 'Candidate'){throw 'R5 candidate status changed'}; $caseCounts=@{}; foreach($case in @($r5.cases)){ $caseCounts[$case.status]=1 + ($caseCounts[$case.status] | ForEach-Object {[int]$_}) }; if(@($r5.cases | Where-Object {$_.status -eq 'CandidateSnapshot'}).Count -ne 3 -or @($r5.cases | Where-Object {$_.status -eq 'Nonconverged'}).Count -ne 1 -or @($r5.cases | Where-Object {$_.status -eq 'NotCovered'}).Count -ne 2){throw 'R5 case counts changed'}; if(@($r5.records | Where-Object {$_.status -ne 'Deferred' -or $_.evidence_approval -ne 'Candidate'}).Count -ne 0){throw 'R5 record approval/status changed'}; $hashes=@('1680a075cd34867b603e1e786002815b19be360d4e8557cd5485978dc32c1694','0de429b27f78b5e2c5fe724916b14ac8ce45ba05560ab73f3fa4805befd11a0e','6e3c19d4f596dfd930b339f983a54b46a6a27b9e83b6652f8e40a058c19e2a8b','13481d139c2b2f2fd1c043725c0a2ced9f396c2813a954cc604a1266bc4c8211','8a53f7519a3b6597d3382c9e91df356474d7d1e72e8ce5d0055e30ef635a1045'); $paths=@('data/packs/p4-t06-r4-synthetic-input-v1.json','data/packs/p4-t06-r4-reduced-candidate-v1.json','data/packs/p4-t06-r4-reduced-candidate-v1.manifest.json','data/comparisons/p4-t06-r5-candidate-snapshots-v1.json','benchmarks/P4-T08-static-solver-benchmark.json'); for($i=0;$i -lt $paths.Count;$i++){ $actual=(Get-FileHash -Algorithm SHA256 -LiteralPath $paths[$i]).Hash.ToLowerInvariant(); if($actual -ne $hashes[$i]){throw ('Hash mismatch '+$paths[$i]+' '+$actual)} }; $golden=@(Get-ChildItem -LiteralPath 'data/golden' -File -Recurse | Where-Object {$_.Name -ne '.gitkeep' -and $_.FullName -notmatch '\(bin|obj)\'}); $goldenTests=@(rg --files tests/ReactorSim.Golden.Tests -g '*.cs' -g '!bin/**' -g '!obj/**'); if($golden.Count -ne 0 -or $goldenTests.Count -ne 0){throw 'G4A unexpectedly created golden evidence'}; if(@($r5.records | Where-Object {$_.evidence_path -match '^[A-Za-z]:|^/' }).Count -ne 0){throw 'Host path leaked into R5 evidence'}; Write-Output ('P4_T06_G4A_ADMISSION_PASS admitted_static=3 deferred_refuelled=1 deferred_rrs=1 deferred_poison=1 records='+$r5.records.Count+' golden_files='+$golden.Count+' golden_tests='+$goldenTests.Count)
```

Result: exit 0.

```text
P4_T06_G4A_ADMISSION_PASS admitted_static=3 deferred_refuelled=1 deferred_rrs=1 deferred_poison=1 records=57 golden_files=0 golden_tests=0
```

### Path-free artifact binding check (T0)

```text
$paths=@('docs/tasks/P4-T06-G4A.md','data/packs/p4-t06-r4-synthetic-input-v1.json','data/packs/p4-t06-r4-reduced-candidate-v1.json','data/packs/p4-t06-r4-reduced-candidate-v1.manifest.json','data/comparisons/p4-t06-r5-candidate-snapshots-v1.json','benchmarks/P4-T08-static-solver-benchmark.json','tools/SpatialComparison/Program.cs','tools/SpatialComparison/ReactorSim.SpatialComparison.csproj'); $slash=[char]92; $forward=[char]47; $windowsDrivePattern='C:' + $slash + 'Users' + $slash; $posixUsersPattern=$forward + 'Users' + $forward; $posixHomePattern=$forward + 'home' + $forward; $escapedSlash=[regex]::Escape($slash); $uncRegex='(?i)(?:^|[^' + $escapedSlash + '])' + $escapedSlash + '{2}[^' + $escapedSlash + $forward + '\r\n]+' + $escapedSlash; $hostPatterns=@($windowsDrivePattern,$posixUsersPattern,$posixHomePattern); foreach($path in $paths){if(-not (Test-Path -LiteralPath $path -PathType Leaf)){throw "Missing $path"}; $text=Get-Content -LiteralPath $path -Raw; if($text -match '(?m)[ \t]+$'){throw "Trailing whitespace: $path"}; foreach($pattern in $hostPatterns){if($text.IndexOf($pattern,[StringComparison]::OrdinalIgnoreCase) -ge 0){throw "Host path in $path"}}; if($text -match $uncRegex){throw "UNC host path in $path"}}; Write-Output ('P4_T06_G4A_STATIC_PASS files='+$paths.Count)
```

Result: exit 0; `P4_T06_G4A_STATIC_PASS files=8`. The report and existing
candidate artifacts contain no host-local path, trailing whitespace, or
replacement-character failure.

### Scope and round handoff audit (T0)

```text
$ErrorActionPreference='Stop'; $report=Get-Content -LiteralPath 'docs/tasks/P4-T06-G4A.md' -Raw; $round=Get-Content -LiteralPath 'docs/tasks/ROUND-2026-08-16-P4-REFERENCE-CHAIN.md' -Raw; $scope=Get-Content -LiteralPath 'docs/PROJECT_SCOPE.md' -Raw; foreach($text in @($report,$round,$scope)){if($text -match '(?m)[ \t]+$'){throw 'trailing whitespace in routing records'}}; if($round -notmatch '(?s)\| `P4-T06-G4A` \|.*?Current result: COMPLETE.*?Next: `P4-T06-G4B`'){throw 'round does not route G4A complete to G4B'}; if($scope -notmatch '(?s)P4-T06-G4A.*?complete.*?P4-T06-G4B.*?next' -or $scope -notmatch '### `G4` - spatial validation and tolerance gate'){throw 'scope does not route G4A complete to G4B'}; foreach($needle in @('P4-T06-G4A','P4-T06-G4B','three deterministic','static synthetic cases','explicitly deferred','fresh `G4`')){if(-not (($report -match [regex]::Escape($needle)) -or ($round -match [regex]::Escape($needle)) -or ($scope -match [regex]::Escape($needle)))){throw "missing route marker $needle"}}; Write-Output 'P4_T06_G4A_ROUTING_PASS g4a=COMPLETE next=G4B fresh_g4=required'
```

Result: exit 0.

```text
P4_T06_G4A_ROUTING_PASS g4a=COMPLETE next=G4B fresh_g4=required
```

No T6 rerun was required: G4A did not change or regenerate an external
reference baseline. No T3 was required for this documentation-only admission
decision; the unchanged R5 T3 evidence remains 103/103 Core with zero Golden
tests. G4B is responsible for its required T1/T3 consumer validation.

## Token and cost accounting

| Field | Value | Source/notes |
|---|---:|---|
| Input tokens | `Unavailable` | request telemetry unavailable |
| Cached input tokens | `Unavailable` | request telemetry unavailable |
| Cache-write input tokens | `Unavailable` | request telemetry unavailable |
| Output tokens | `Unavailable` | request telemetry unavailable |
| Reasoning output tokens | `Unavailable` | request telemetry unavailable |
| Total tokens | `Unavailable` | request telemetry unavailable |
| Estimated cost | `Unavailable` | no verified price source or billing receipt |
| Goal-service total | `Unavailable` | not exposed; not allocable to this task |

Cost formula/basis: unavailable; no request-level billing receipt or verified
price source was exposed.

## Numerical differences

Not applicable; G4A changes no numerical behavior and adds no numerical data.
The existing R5 static cases remain exact self-consistency candidate evidence;
their `0.0` differences are not promoted to a tolerance or golden result.

## Deferred validation

- G4B golden/comparison consumers - next task; it may consume only the three
  admitted static cases and must keep thresholds deferred.
- Fresh G4 spatial tolerance approval - after G4B; G4 owns the disposition.
- Refuelled usable comparison - deferred because R5 has no usable converged
  state; integrated refuelling/depletion evidence remains with P5-T10/G5.
- RRS and poison comparisons - deferred to the Phase 6/G6 scope because the
  approved maps and overlays do not exist.
- T6 external reference rerun - not applicable because no external baseline
  changed or was admitted by G4A.

## Blockers, risks, and follow-up

- Blockers: none for the bounded G4A decision. The full G4 gate is not yet
  closed; this report intentionally does not claim that result.
- Risk triggers: comparison-scope admission and golden-data boundary were
  reviewed; G4B triggers T3 and code review (high), and fresh G4 retains
  tolerance ownership.
- Risks accepted or deferred: the admitted basis is synthetic contract
  self-consistency, not external solver accuracy; the three deferred case
  classes remain visible and must not be called covered.
- Follow-up work: `P4-T06-G4B`, then fresh `G4`; `P5-T10`/`G5` and Phase 6/G6
  consume their own deferred case evidence.

## Next eligible task

`P4-T06-G4B` - add bounded consumers for the three admitted static comparison
cases, bind the existing manifest/profile identities, and leave numeric
threshold approval to a fresh `G4`.

Source: [`AGENTS.md`](../../AGENTS.md),
[`docs/Implementation_plan.md`](../Implementation_plan.md),
[`docs/spec/reduced-model-interpolation-boundary-v1.md`](../spec/reduced-model-interpolation-boundary-v1.md),
[`docs/spec/observables-validation-methodology-v1.md`](../spec/observables-validation-methodology-v1.md),
and [`TASK_REPORT_TEMPLATE.md`](TASK_REPORT_TEMPLATE.md).
