# G4-R4 - fresh synthetic-only spatial authority disposition

## Outcome

Status: COMPLETE

The fresh G4-R4 gate conditionally passed the new manufactured case for the
bounded `Synthetic` spatial-operator and normalization domain. It created an
explicit `ApprovedGolden` payload and six `1e-10` tolerance profiles for that
domain only. It did not promote the prior G4F/G4D/R5 artifacts, did not change
the frozen equations, and did not close production G4 or unblock P5-T10/G5.

Effectiveness: SUCCESS

## Execution and model evidence

- Role: root implementer; fresh gate execution; final review lane
  `code review (high)`
- Requested model / reasoning: GPT-5.6 Luna / high; code review (high)
- Actual model / reasoning: `UNVERIFIED`
- Execution receipt or telemetry source: `Unavailable`
- Attempts: 1 bounded gate execution; elapsed time: `Unavailable`
- Artifact/checkpoint status: produced
- Review disposition: CONDITIONAL PASS for `Synthetic`; production G4 remains
  `BLOCKED`
- Review evidence verification: `UNVERIFIED`
- Reviewer reuse/fresh-review rationale: same-context high-effort review; no
  prior gate restart and no fresh reviewer because receipt tooling was
  unavailable

Applicable P1-T08 rows are `S1-R02`, `S1-R03`, `S1-R05`, `S1-R11`, `S5-R03`,
`S5-R04`, `S5-R09`, and `S6-R03` as context/methodology or authority-gap
support only. CANDU numeric, burnup, RRS, poison, and kinetics rows are
`NotApplicable` to this static project-authored case; no literature number was
promoted.

## Files created or changed

- `data/golden/p4-t06-g4i-manufactured-authority-v1.json` - approved synthetic
  golden payload with explicit scope and profiles.
- `data/golden/p4-t06-g4i-manufactured-authority-v1.manifest.json` - approved
  payload and definition hash binding.
- `tests/ReactorSim.Golden.Tests/ReactorSim.Golden.Tests.csproj` - approved
  payload links.
- `tests/ReactorSim.Golden.Tests/P4T06G4IApprovedConsumerTests.cs` - approved
  profile and Core consumer checks.
- `docs/gates/G4-R4.md` - this gate's conditional disposition.
- `docs/tasks/G4-R4.md` - this task report.
- `docs/PROJECT_SCOPE.md` - current synthetic approval and production boundary.
- `docs/tasks/ROUND-2026-08-16-G4-BLOCK-RESOLUTION-CHAIN.md` - G4-R4 result and
  current handoff.

Pre-existing G4/G4-R1/G4-R2/G4-R3 reports, G4H report, G4F/G4D/R5 candidate
artifacts, equations, runtime Core code, Unity adapter, and existing deferred
coverage records were inspected and not changed.

## Assumptions and design choices

- The approval is deliberately `Synthetic`/`ApprovedGolden`, not `Direct` and
  not a CANDU physics authority.
- The six approved profiles are `1e-10` absolute/relative for `spatial.k`,
  `spatial.flux`, `spatial.node_power`, `spatial.total_power`, and
  `spatial.equation_residual` as defined by the artifact; convergence metadata
  uses exact discrete matching plus the explicit caller policy.
- The profile is supported by exact manufactured balance, independent
  standalone reproduction, the Core consumer, deterministic repeat, and T3/T6
  evidence. The raw standalone differences remain observations, not an
  unrecorded tolerance derivation.
- The workspace has no Git HEAD. The source-commit limitation is explicit;
  source/definition/artifact/manifest hashes are the synthetic artifact-level
  reproducibility binding. Release/external authority still requires an
  immutable VCS/source snapshot.

## Validation commands and results

### Approved generation and manifest regeneration (T6-style)

```text
powershell -NoProfile -ExecutionPolicy Bypass -File .\tools\ManufacturedSpatialAuthority\Test-P4-T06-G4I.ps1 -Disposition approved
```

Result: exit 0; 0 build warnings, 0 build errors, deterministic generation and
validation pass, 54 outer iterations, repeat equality true. Approved artifact
SHA-256 `9e8ce5534fc59970a3f956e957e8f4ddd78c2f78602712156aa81353cedb43d3`;
manifest SHA-256 `c69176a2b188db45b53ceb5c9dbce63b418c11749336f5e9592671d7f71f935c`.

### G4-R4 authority audit (T1)

```text
$ErrorActionPreference='Stop'; $candidate=Get-Content -Raw data/comparisons/p4-t06-g4i-manufactured-authority-v1.json | ConvertFrom-Json; $approved=Get-Content -Raw data/golden/p4-t06-g4i-manufactured-authority-v1.json | ConvertFrom-Json; if($candidate.golden_status -ne 'NoGolden' -or $candidate.evidence_approval -ne 'Candidate'){throw 'candidate promoted'}; if($approved.golden_status -ne 'ApprovedGolden' -or $approved.evidence_approval -ne 'Approved' -or $approved.coverage_class -ne 'Synthetic'){throw 'approved scope invalid'}; if(@($approved.tolerance.profiles.PSObject.Properties).Count -ne 6){throw 'profile count invalid'}; Write-Output 'G4_R4_AUTHORITY_AUDIT_PASS candidate_preserved=1 approved_profiles=6 synthetic_scope=1'
```

Result: exit 0; candidate preserved, six approved profiles, synthetic scope
only.

### Focused approved consumer (T1)

```text
dotnet test C:\Users\infin\candu\tests\ReactorSim.Golden.Tests\ReactorSim.Golden.Tests.csproj --configuration Release --nologo --no-restore --filter FullyQualifiedName~P4T06G4I
```

Result: exit 0; 4/4 tests passed.

### Full headless regression (T3)

```text
dotnet test C:\Users\infin\candu\ReactorSim.sln --configuration Release --nologo --no-restore
```

Result: exit 0; Core 103/103 and Golden 12/12 passed, with 0 failures and 0
skips.

### Routing and historical-preservation audit (T0)

```text
$ErrorActionPreference='Stop'; $files=@('docs/gates/G4.md','docs/gates/G4-R1.md','docs/gates/G4-R2.md','docs/gates/G4-R3.md','docs/gates/G4-R4.md','docs/tasks/P4-T06-G4H.md','docs/tasks/P4-T06-G4I.md','docs/tasks/G4-R4.md','docs/PROJECT_SCOPE.md','docs/tasks/ROUND-2026-08-16-G4-BLOCK-RESOLUTION-CHAIN.md'); foreach($path in $files){if(-not (Test-Path -LiteralPath $path -PathType Leaf)){throw "missing $path"}; if(@(Select-String -LiteralPath $path -Pattern '\s+$').Count -ne 0){throw "trailing whitespace $path"}}; if((Get-Content -Raw docs/gates/G4-R3.md) -notmatch 'BLOCKED'){throw 'historical gate changed'}; Write-Output 'G4_R4_ROUTING_AUDIT_PASS historical_g4_preserved=1 production_boundary=1'
```

Result: exit 0; historical gate files and production boundary preserved.

## Token and cost accounting

| Field | Value | Source/notes |
|---|---:|---|
| Input tokens | `Unavailable` | No request-level telemetry exposed |
| Cached input tokens | `Unavailable` | No request-level telemetry exposed |
| Cache-write input tokens | `Unavailable` | No request-level telemetry exposed |
| Output tokens | `Unavailable` | No request-level telemetry exposed |
| Reasoning output tokens | `Unavailable` | No request-level telemetry exposed |
| Total tokens | `Unavailable` | No request-level telemetry exposed |
| Estimated cost | `Unavailable` | No verified price source or billing receipt |
| Goal-service total | `Unavailable` | Not exposed; not allocable to this task |

Cost formula/basis: unavailable; no request-level billing semantics or price
receipt was exposed.

## Numerical differences

- Exact manufactured residual relative infinity:
  `3.7436708410613586e-17`.
- Independent standalone eigenvalue absolute error:
  `4.47308856621476e-13`.
- Independent maximum flux absolute error:
  `3.54805074209708e-12`.
- Independent total-power absolute error: `0`.
- Independent residual relative infinity:
  `9.02599039782708e-13`.
- Core approved consumer passed the explicit `1e-10` profiles and exact 54-step
  convergence metadata.

## Deferred validation

- Direct CANDU/DRAGON5/DONJON5 production authority - requires a separate
  complete external or otherwise admissible reference package and rights.
- Refuelled, RRS, and poison coverage - remains with P5/G5 and Phase 6/G6
  owner scopes.
- Release/external source identity - requires a VCS commit or equivalent
  immutable source snapshot because the current workspace has no Git HEAD.
- T4/T5 - not applicable; no Unity, adapter, mobile, or platform behavior
  changed.

## Blockers, risks, and follow-up

- Blockers: production G4, P5-T10, and G5 remain blocked; synthetic approval is
  not representative CANDU approval.
- Risk triggers: golden/tolerance, equation, normalization, and gate changes
  required T3/T6-style validation and code-review (high); actual review
  telemetry remains `UNVERIFIED`.
- Risks accepted or deferred: synthetic-only scope and missing source VCS HEAD
  are explicit and bounded; no hidden tolerance or external claim was added.
- Follow-up: none for the synthetic authority domain. Do not restart previous
  G4 tasks; any production-authority work needs a new owner-authorized task.

## Next eligible task

None for the approved synthetic domain. Production reference work remains
blocked until the owner authorizes a separate external/representative authority
task.

Source: [`AGENTS.md`](../../AGENTS.md),
[`docs/Implementation_plan.md`](../Implementation_plan.md),
[`docs/gates/G4-R4.md`](../gates/G4-R4.md), and
[`docs/tasks/TASK_REPORT_TEMPLATE.md`](TASK_REPORT_TEMPLATE.md).
