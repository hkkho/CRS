# P4-T06 - Minimal DONJON5 comparison and case admission

## Outcome

Status: BLOCKED

The read-only P4-T06 admission audit confirmed that the pinned DONJON5 smoke
descriptor and runner are internally identifiable and syntactically valid, but
the case cannot be admitted as a repository comparison or golden baseline.
The prepared HDF5 group constants have no standalone redistribution license and
carry CEA/AREVA copyright/export metadata. The repository's reference policy
requires an explicit legal/upstream determination before any input, derived
output, or comparison artifact is committed, mirrored, published, or
redistributed. The private smoke output also does not supply the solver
coefficients, geometry mapping, units, or normalization needed to compare it
to the Core two-group solver.

Work stopped before executing or importing a comparison result. No equation,
unit, normalization, tolerance, reference value, or golden data was invented
or changed.

Effectiveness: BLOCKED

## Execution and model evidence

- Role: root implementer; reference-admission audit
- Requested model / reasoning: GPT-5.6 Luna / high
- Actual model / reasoning: `UNVERIFIED`; execution telemetry was unavailable
- Execution receipt or telemetry source: Unavailable for the root request
- Attempts: 1 bounded read-only admission audit; elapsed time: Unavailable
- Artifact/checkpoint status: produced blocker report; no comparison artifact
- Review disposition: PASS for the blocker assessment; no implementation or
  baseline candidate was reviewed
- Review evidence verification: `UNVERIFIED`
- Reviewer reuse/fresh-review rationale: same reviewer context was used for the
  bounded admission assessment, reviewer
  `01a00680-5d6a-7950-af04-9834d8720f07` (Feynman), rather than starting a
  fresh review. The requested lane was code review (high), requested
  GPT-5.6 Terra / high; actual model/effort and receipt telemetry were
  unavailable.

## Files created or changed

- `docs/tasks/P4-T06.md` - records the admission audit and exact blocker.
- `docs/PROJECT_SCOPE.md` - records P4-T06 as blocked and preserves the
  independent-work overlap boundary.

No reference input, raw listing, compact export, HDF5 file, executable,
comparison output, golden file, runtime source, or physics guide was added or
changed. Pre-existing files inspected but not changed include
`reference/README.md`, the P1-T03 case descriptor, the Version5 provenance
manifest, P1-T03/P1-T04/P1-T08 reports, the P1-T08 literature digest, the
P2-T01/P2-T02/P2-T05 specifications, P4-T03/P4-T05 reports, and G3.

## Assumptions and design choices

- The P1-T03 descriptor remains a private-use, offline run contract, not a
  permission to redistribute its selected source-tree inputs or derived raw
  outputs. Its pinned source identity is Version5 `v5.1.0`, commit
  `eee582f8594d3b7d01b65660ca1e8bef00eb6b2a`, tree
  `0d3e8f721c84b3c3b2d03e9898a8a0a9c29287d8`.
- The descriptor's expected DONJON observable (`K-EFFECTIVE`)
  `9.9859273434E-01` is provenance for the private smoke assertion, not a
  Core golden value. The P1 smoke case imports prepared AFA_180/AFA_310 HDF5
  group constants and does not provide a repository-compatible coefficient,
  topology, or normalization mapping.
- The repository's `NOASSERTION / mixed notices` classification, the missing
  standalone library/asset license, and the HDF5 CEA/AREVA export metadata are
  treated as unresolved legal/data prerequisites. No legal conclusion is
  inferred locally.
- Applicable P1-T08 digest rows were `S1-R04`, `S1-R06`, `S1-R11`, `S5-R03`,
  `S5-R06`, `S5-R09`, `S6-R03`, and `S6-R08`. They document candidate
  methodology and the missing tool/build, nuclear-data, geometry, units,
  normalization, reproducibility, and licensing proofs. They do not authorize
  a comparison or golden value.
- G2 remains exactly `FORCED CLOSED / WAIVED`; this audit does not treat the
  waiver as legal, reference, or golden approval.

## Validation commands and results

### T1 - descriptor and runner admission audit

```text
$tokens=$null; $parseErrors=$null; [void][Management.Automation.Language.Parser]::ParseFile((Resolve-Path 'tools\PhysicsData\Run-P1-T03-DonjonAfa180310.ps1'),[ref]$tokens,[ref]$parseErrors); if($parseErrors.Count){$parseErrors|ForEach-Object Message; exit 1}; $case=Get-Content -LiteralPath 'reference\donjon5\P1-T03-AFA_180_310_type1_dual.case.json' -Raw | ConvertFrom-Json; if($case.version5_source.commit -ne 'eee582f8594d3b7d01b65660ca1e8bef00eb6b2a'){throw 'Version5 commit mismatch'}; if($case.execution.program_sha256.Length -ne 64){throw 'Executable hash missing'}; if($case.execution.expected_effective_multiplication_factor -ne '9.9859273434E-01'){throw 'Pinned effective multiplication factor mismatch'}; if($case.execution.convergence.expected_final_outer_iteration -ne 15){throw 'Pinned convergence marker mismatch'}; 'P4_T06_DESCRIPTOR_RUNNER_AUDIT_PASS parser=PASS descriptor_identity=PASS'
```

Result: exit 0; PowerShell parsed with zero errors and the pinned source,
executable-hash, K-effective, and convergence markers matched the descriptor.

### T1 - committed-artifact and legal-boundary audit

```text
$casePath='reference\donjon5\P1-T03-AFA_180_310_type1_dual.case.json'; $case=Get-Content -LiteralPath $casePath -Raw -Encoding utf8 | ConvertFrom-Json; $required=@('Donjon/data/AFA_180_310_type1_dual.x2m','Ganlib/data/testgan4_proc/AFA_180.h5','Ganlib/data/testgan4_proc/AFA_310.h5'); foreach($path in $required){if([string]::IsNullOrWhiteSpace($path)){throw 'Descriptor path missing'}}; $policy=$case.version5_source.license_audit.policy; if($policy -notmatch 'Do not commit, vendor, mirror, publish, or redistribute'){throw 'Private/no-redistribution policy missing'}; if($case.scope.does_not_include -notcontains 'committed upstream input, prepared group constants, raw result, compact export, or golden dataset'){throw 'Descriptor artifact boundary missing'}; if($case.execution.convergence.epsout -ne '1.00E-04'){throw 'Unexpected descriptor convergence setting'}; $repoArtifacts=@(Get-ChildItem -LiteralPath 'reference' -Recurse -File | Where-Object { $_.Extension -in @('.h5','.x2m','.res','.lst','.out') }); if($repoArtifacts.Count -ne 0){throw "Reference input/output artifacts unexpectedly committed: $($repoArtifacts.FullName -join ', ')"}; "P4_T06_ADMISSION_AUDIT_PASS metadata_valid=PASS committed_external_artifacts=0 license_state=PRIVATE_NO_REDISTRIBUTION"
```

Result: exit 0; descriptor metadata passed, no external reference input/output
artifact was committed, and the descriptor preserved the private/no-
redistribution policy.

### T0 - repository format and current-scope checks

```text
$sdkRoot='C:\Users\infin\AppData\Local\Temp\candu-sdk-10.0.302'; $env:DOTNET_ROOT=$sdkRoot; $env:Path="$sdkRoot;$env:Path"; & .\tools\Check-Format.ps1
& .\tools\Check-PhysicsGuideConsistency.ps1
```

Result: format/static checks exited 0. The living physics-guide consistency
check remains PASS: 13 pages, Core static-solver sources present, and the
private PDF synchronized. No guide/PDF update was required because P4-T06 did
not change runtime physics or gate disposition.

### T3/T6 - reference comparison

Not run. The case is not admissible until the legal/data and solver-mapping
prerequisites are resolved; running or copying a private result would not
produce approved comparison evidence.

## Token and cost accounting

| Field | Value | Source/notes |
|---|---:|---|
| Input tokens | `Unavailable` | No request-level telemetry exposed |
| Cached input tokens | `Unavailable` | No request-level telemetry exposed |
| Cache-write input tokens | `Unavailable` | No request-level telemetry exposed |
| Output tokens | `Unavailable` | No request-level telemetry exposed |
| Reasoning output tokens | `Unavailable` | No request-level telemetry exposed |
| Total tokens | `Unavailable` | No request-level telemetry exposed |
| Estimated cost | `Unavailable` | No price/usage receipt exposed |
| Goal-service total | `Unavailable` | Reported separately; not allocable to this task |

Cost formula/basis: unavailable; no provider billing receipt or price basis was
exposed for this request.

## Numerical differences

Not applicable. No Core/reference comparison was executed and no numerical
baseline, tolerance, or golden data changed. The descriptor's
`9.9859273434E-01` is retained only as private upstream assertion metadata.

## Deferred validation

- DONJON5-to-Core comparison is deferred until exact solver coefficients,
  geometry/topology mapping, units, flux/power normalization, output fields,
  and reproducible private inputs are admitted.
- Legal/upstream permission or a documented legal/export-control determination
  is required before any selected HDF5 input or derived reference output can
  be redistributed or committed.
- T6 baseline comparison and non-empty Golden.Tests consumers are deferred to
  the first task after those prerequisites are resolved.
- Representative snapshots and G4 remain deferred; they cannot silently use
  the blocked smoke result.

## Blockers, risks, and follow-up

- Blockers: unresolved licensing/redistribution status for selected HDF5 group
  constants; no committed or authorized solver-ready input package; missing
  traceable geometry/coefficient/unit/normalization mapping from the private
  DONJON smoke case to Core.
- Risk triggers: reference-case admission and potential golden-data use would
  trigger T3/T6 and independent code review (high), but those triggers were
  not entered because the stop condition fired before comparison.
- Risks accepted or deferred: no source-reported K-effective is promoted to a
  golden or runtime value; no external artifacts are copied into the repo.
- Follow-up work: obtain explicit legal/upstream direction and a complete
  solver-ready private case package, then re-open P4-T06 as a fresh bounded
  task. Independent benchmark work may proceed under the gate-overlap policy
  because it does not depend on the blocked reference finding.

## Next eligible task

An independently bounded Phase 4 benchmark task (work package 8; task ID to be
assigned) may proceed without reference inputs. P4-T07/reference snapshots and
G4 remain dependent on the unresolved P4-T06 admission evidence.

Source: [`AGENTS.md`](../../AGENTS.md),
[`docs/Implementation_plan.md`](../Implementation_plan.md),
[`reference/README.md`](../../reference/README.md),
[`reference/donjon5/P1-T03-AFA_180_310_type1_dual.case.json`](../../reference/donjon5/P1-T03-AFA_180_310_type1_dual.case.json),
[`docs/reference/candu-literature-digest-v1.md`](../reference/candu-literature-digest-v1.md),
[`docs/gates/G3.md`](../gates/G3.md), and
[`TASK_REPORT_TEMPLATE.md`](TASK_REPORT_TEMPLATE.md).
