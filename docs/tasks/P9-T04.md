# P9-T04 - Performance-target and optimization decision record

## Outcome

Status: COMPLETE

P9-T04 reviews the completed P9-T01 desktop benchmark, P9-T02 CLI profile,
and P9-T03 Core solver profile and records a deliberate `HOLD` decision. No
numeric performance or allocation target is selected, and no plain-C#,
precision, Burst/Jobs, mobile, thermal, or release change is authorized from
the current synthetic host observations. The reviewed evidence is internally
deterministic and useful for future comparison, but it lacks representative
device evidence, external profiler attribution, and a portable approved target
authority. Phase 10 presentation may proceed independently without treating
this record as a runtime-performance approval.

Effectiveness: SUCCESS

## Execution and model evidence

- Role: root implementer; code review (high) reviewer.
- Requested model / reasoning: GPT-5.6 Luna / high for bounded performance
  decision documentation; code review (high) for target-selection, authority,
  and optimization-scope review.
- Actual model / reasoning: `UNVERIFIED`; execution and reviewer telemetry did
  not expose verifiable model or reasoning metadata.
- Execution receipt or telemetry source: root task goal telemetry; reviewer
  `01a034c9-3041-7dd2-9665-fde9c8c16600` (Meitner), task-specific review
  submission `01a03a59-86a3-7581-b719-a50b7ba1a092`.
- Attempts: one documentation/manifest attempt with one bounded review;
  elapsed time: `Unavailable`.
- Artifact/checkpoint status: produced; owner approval, decision manifest,
  consistency validation, scope handoff, and this report were produced.
- Review disposition: `PASS` after the report/scope administrative handoff;
  no runtime implementation was changed.
- Review evidence verification: `UNVERIFIED` (the receipt exposes the
  disposition but not verifiable model/reasoning telemetry).
- Reviewer reuse/fresh-review rationale: the changed P9-T04 decision candidate
  reused the available Meitner review context from the immediately preceding
  Phase 9 profiling tasks; the candidate is documentation-only and was
  reviewed as a new target-selection decision.

## Files created or changed

- `docs/tasks/P9-T04-OWNER-APPROVAL.md` - bounded decision scope and explicit
  no-optimization exclusions.
- `benchmarks/P9-T04-performance-decision-v1.json` - versioned HOLD decision
  bound to the exact P9-T01/P9-T02/P9-T03 manifests.
- `docs/PROJECT_SCOPE.md` - reconciled decision summary and Phase 9 handoff.
- `docs/tasks/P9-T04.md` - this required task report.

No runtime source, benchmark runner, manifest authority, physics specification,
numerical tolerance, golden/reference artifact, native dependency, Unity asset,
or mobile implementation was changed. The P9-T01/P9-T02/P9-T03 reports and
their exact manifests were inspected and preserved.

## Decision and evidence boundary

The versioned decision artifact is
`benchmarks/P9-T04-performance-decision-v1.json` with SHA-256
`0a2ceb5f0ea2bf906a9fa6e7f2821164d8d2fa8c17783e26b69e9efd432ace9a`. It binds:

- P9-T01 manifest SHA-256
  `63114186fd9768a94e44ff0368001952cda2750610965b48c94cc36e5efb5281`;
- P9-T02 profile manifest SHA-256
  `0c6f547691d52b48a32800a4409eefee68790f73725c215513892b78271187af`; and
- P9-T03 profile manifest SHA-256
  `174f8809837c0bf31bd2562efbd8034bbe7375661b89806f3bca464df42b947f`.

The reviewed observations are:

| Boundary | Observed range | Interpretation |
|---|---:|---|
| P9-T02 complete CLI stream, steady survival | `2778.996–2797.003 us` mean | Host-specific public `CliApplication.Run` observation. |
| P9-T02 complete CLI stream, controlled response | `1884.648–1906.420 us` mean | Host-specific public CLI observation. |
| P9-T02 complete CLI stream, boundary loss | `1869.986–1904.533 us` mean | Host-specific public CLI observation. |
| P9-T03 complete Core solve | `408.622–416.459 us` mean | Host-specific public `SpatialEigenSolve.TrySolve` observation. |
| P9-T03 allocations | `856 bytes/solve` in both runs | Stable for the frozen synthetic case, not a budget. |

Both profiles reported exact deterministic solver/CLI outputs within their
respective preflight checks. P9-T03 also reported two converged solver
iterations and the explicit synthetic topology/flux-state descriptors. These
facts support repeatability, not portable gameplay, mobile, thermal, or release
acceptance.

Decision: `HOLD`.

- `numeric_performance_target`: `null`.
- `allocation_target`: `null`.
- Plain-C# optimization: not authorized by this record.
- Precision change: not authorized.
- Burst/Jobs/native backend: not authorized.
- Mobile/thermal/release claim: not authorized.

The reason is evidence quality and authority, not a failed regression: the
measurements are synthetic and machine-specific, no external profiler is
available to identify internal hotspots, no representative Android device or
`adb` is available, and no approved portable performance-budget authority has
been supplied. A future optimization task must provide its own approved target,
measurement boundary, equivalence evidence, and review scope.

Literature applicability: `NotApplicable`. No equation, constant, unit,
normalization, convergence rule, tolerance, reference case, or golden value was
selected or changed.

## Validation commands and results

### T0 - decision manifest identity and exact upstream hashes

```text
$decision = Get-Content -Raw -LiteralPath 'benchmarks/P9-T04-performance-decision-v1.json' | ConvertFrom-Json; if ($decision.format -ne 'reactorsim.p9-performance-decision/v1' -or $decision.task_id -ne 'P9-T04' -or $decision.decision.disposition -ne 'HOLD') { throw 'Decision manifest identity/disposition mismatch' }; $checks = @(@('benchmarks/P9-T01-cli-gameplay-benchmark-v1.json','63114186fd9768a94e44ff0368001952cda2750610965b48c94cc36e5efb5281'), @('benchmarks/P9-T02-cli-profile-parameters-v1.json','0c6f547691d52b48a32800a4409eefee68790f73725c215513892b78271187af'), @('benchmarks/P9-T03-core-solver-profile-parameters-v1.json','174f8809837c0bf31bd2562efbd8034bbe7375661b89806f3bca464df42b947f')); foreach($check in $checks) { $actual = (Get-FileHash -LiteralPath $check[0] -Algorithm SHA256).Hash.ToLowerInvariant(); if ($actual -ne $check[1]) { throw "Hash mismatch: $($check[0])" } }; git diff --check
```

Result: PASS; the JSON parsed, task/format/disposition matched, all three
upstream SHA-256 bindings matched the actual repository files, and
`git diff --check` passed.

### T0 - report/scope consistency

```text
rg -n "P9-T04|HOLD|plain_csharp_optimization_authorized|numeric_performance_target" docs/tasks/P9-T04.md docs/PROJECT_SCOPE.md benchmarks/P9-T04-performance-decision-v1.json
```

Result: PASS; the report, scope row, owner approval, and decision artifact all
state the same `HOLD` disposition and no-optimization boundary.

### Prior evidence consumed without regeneration

```text
& 'C:\Program Files\dotnet\dotnet.exe' 'benchmarks\ReactorSim.Phase9.Benchmarks\bin\Release\net10.0\ReactorSim.Phase9.Benchmarks.dll' '--profile'
& 'C:\Program Files\dotnet\dotnet.exe' 'benchmarks\ReactorSim.Benchmarks\bin\Release\net10.0\ReactorSim.Benchmarks.dll' '--profile'
```

Result: the completed P9-T02/P9-T03 reports record the two independent profile
runs, exact deterministic preflights, and final Release/T3 evidence. P9-T04
does not rerun or alter those authorities; it only binds and interprets them.

### T3 applicability

No new T3 suite was required: P9-T04 changed only a decision manifest, owner
approval, task report, and scope register. The bound P9-T02/P9-T03 evidence
already records final Release builds and T3 Core `208/208`, CLI `38/38`, Golden
`26/26` with zero failures/skips.

## Token and cost accounting

| Field | Value | Source/notes |
|---|---:|---|
| Input tokens | `Unavailable` | No request-level telemetry exposed. |
| Cached input tokens | `Unavailable` | No request-level telemetry exposed. |
| Cache-write input tokens | `Unavailable` | No request-level telemetry exposed. |
| Output tokens | `Unavailable` | No request-level telemetry exposed. |
| Reasoning output tokens | `Unavailable` | No request-level telemetry exposed. |
| Total tokens | `Unavailable` | No request-level telemetry exposed. |
| Estimated cost | `Unavailable` | Price source/date and per-task allocation were not exposed. |
| Goal-service total | `Unavailable` | Reported separately; not added to request totals. |

Cost formula/basis: unavailable; no provider billing telemetry or allocable
per-task price basis was exposed, so no estimate was invented.

## Numerical differences

Not applicable; no numerical behavior changed. The reviewed timing ranges are
observations already recorded by P9-T02/P9-T03, not new thresholds or reference
comparisons.

## Deferred validation

- Plain-C# optimization, allocation reduction, precision/backend changes, and
  equivalence checks - deferred until a separately authorized task supplies an
  approved target and scope.
- External profiler hotspot/phase allocation attribution - deferred because
  `dotnet-trace`, `dotnet-counters`, `dotnet-gcdump`, and `perfview` are absent.
- Android representative-device baseline and T5/G9 evidence - deferred because
  `adb` and a device are unavailable.
- T4 Unity player/performance smoke - deferred to Phase 10/presentation work;
  this task changed no Unity adapter or runtime behavior.
- T6 reference reproduction/regeneration - not applicable; no reference or
  golden baseline changed.

## Blockers, risks, and follow-up

- Blockers: none for the decision-record objective. Missing target authority,
  external profiler, and device are explicit deferred evidence gaps, not a
  reason to invent a target.
- Risk triggers: performance-target selection, optimization authorization, and
  cross-report hash binding triggered independent code review (high). No
  numerical runtime change was made.
- Risks accepted or deferred: the HOLD decision may leave future optimization
  work pending; this preserves deterministic behavior and prevents host-only
  observations from becoming an unsupported budget.
- Follow-up work: Phase 10 Unity presentation may proceed independently as
  `P10-T01`; a separate Android/device task remains eligible when the external
  environment exists. Re-open optimization only with the required target and
  evidence boundary.

## Next eligible task

`P10-T01` - bounded Unity presentation/input adapter slice over the completed
engine-neutral CLI/Core contracts, with no physics, performance-target, or
mobile-device claim. Android/device evidence remains a separate T5 task.

Source: [`AGENTS.md`](../../AGENTS.md),
[`docs/Implementation_plan.md`](../Implementation_plan.md),
[`docs/tasks/P9-T02.md`](P9-T02.md), [`docs/tasks/P9-T03.md`](P9-T03.md), and
[`TASK_REPORT_TEMPLATE.md`](TASK_REPORT_TEMPLATE.md).
