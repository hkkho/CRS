# XSEC-SPH-01 - source SPH execution and pre-SPH export authority

**Status:** AUTHORIZED TASK DEFINITION

**Authority:** the XSEC workstream owner authorization and the critical
source-stage failure recorded in `XSEC-03-R1.md`.

## Objective

Determine and independently review the exact DRAGON source-stage method that
keeps official TCWUX11 state evolution and all source assertions intact while
capturing only an unambiguous pre-SPH `EDITION` object for candidate semantic
export. Replace v2 authority only if that exact method is source-supported.

## Constraints

- Do not remove, change, bypass, or rebaseline any source assertion.
- Do not admit SPH/ADF-corrected fields to the Core mapping.
- Bind exact source object names, timing, serialized records, and every
  non-mutating external capture action. Prove that later source transitions use
  the original source state.
- Do not choose a new equation, unit, tolerance, runtime schema, pack, or
  golden value. Raw source/output artifacts remain external.
- Require focused source evidence, mapping revision only when necessary, T3,
  and independent code review (high). If source-supported pre-SPH capture is
  unavailable, preserve the blocker rather than constructing an alternate case.

## Next routing

XSEC-03-R2 may run only after this task records an exact reviewed source-stage
authority. It must repeat fresh runs and T6 from clean roots.
