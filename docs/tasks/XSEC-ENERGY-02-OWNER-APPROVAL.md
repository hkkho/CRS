# XSEC-ENERGY-02 owner approval - cross-artifact semantic decode

**Recorded:** 2026-08-29
**Decision:** APPROVED / BOUNDED

## Owner direction

The owner authorized `XSEC-ENERGY-02`, the separately required successor to
`XSEC-ENERGY-01`. It may fresh-rerun the approved deck-v6 route and decode the
approved WCFIELD/SAPHYB source fields externally to admit or reject one offline
candidate semantic result.

## Boundary

The task may create external disposable source roots and a read-only external
decoder; inspect numerical values only outside the repository; retain only
path-free hashes, names, units, dimensions, transformations, and boolean
invariant/comparison outcomes; and create only the task records, a path-free
manifest, report, and scope update named in its request.

No source/derived numerical value, numerical difference, tolerance, runtime
pack, Core/CLI/Unity behavior, full-core/DONJON case, golden/reference
baseline, redistribution, publication, or release is authorized. A failed
invariant, source-preservation defect, nondeterminism, or missing/ambiguous
field must be reported as `BLOCKED`.
