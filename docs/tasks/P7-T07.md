# P7-T07 - Literature-informed candidate comparison cases

## Outcome

Status: COMPLETE

The owner-requested follow-on creates three comparison-ready candidate case
cards from the P1-T08 literature digest: an S5 37-element lattice
cross-check, an S5 constant-power lattice-depletion handoff, and an S1 CANDU-6
full-core/refuelling/xenon history. Each card records source row IDs,
source-reported candidate parameters, intended project observables, and the
complete unresolved admission fields.

These are `LiteratureCandidate` / `Candidate` / `Deferred` / `NoGolden`
artifacts. They are not executable reference decks, runtime inputs, approved
golden data, tolerances, or a CANDU production baseline. External paper PDFs,
DRAGON5/DONJON5 inputs, nuclear-data files, and source outputs remain
external/private.

Effectiveness: SUCCESS

## Execution and model evidence

- Role: root implementer; independent code review (high) reviewer.
- Requested model / reasoning: GPT-5.6 Luna / high for bounded implementation;
  code review (high) for literature/golden/provenance risk.
- Actual model / reasoning: `UNVERIFIED`; no root or reviewer execution receipt
  exposed actual model/effort telemetry.
- Execution receipt or telemetry source: root thread unavailable; same-context
  reviewer `Goodall` (`01a0363e-3a54-7e11-b335-b7b1a68e5063`).
- Attempts: one bounded implementation attempt and one bounded reviewer
  context; elapsed time: `Unavailable`.
- Artifact/checkpoint status: produced; three case cards, manifest, source
  design report, standalone validator, and focused boundary tests are present.
- Review disposition: `CONDITIONAL PASS`; the same reviewer/context found two
  stale evidence strings, the root corrected them, and the reviewer retained
  this final disposition with no remaining authority or implementation issue.
- Review evidence verification: `UNVERIFIED`.
- Reviewer reuse/fresh-review rationale: the changed P7-T07 candidate was sent
  to the existing Goodall context for continuity; no review swarm was started.

## Files created or changed

- `data/comparisons/p7-t07-literature-cases-v1.json` - three candidate lattice/
  full-core case cards with source rows, candidate inputs, observables, and
  admission gaps.
- `data/comparisons/p7-t07-literature-cases-v1.manifest.json` - artifact byte
  length, SHA-256, source-manifest binding, and candidate-only status.
- `docs/research/P7-T07-case-design.md` - case matrix, paper-derived design
  rationale, mapping boundaries, licensing boundary, and admission stop rule.
- `tools/P7T07LiteratureCases/ReactorSim.P7T07LiteratureCases.csproj` -
  standalone validator project with no Core reference.
- `tools/P7T07LiteratureCases/Program.cs` - fail-closed candidate status,
  source-manifest, hash, path, mapping, and no-approval validator.
- `tests/ReactorSim.Golden.Tests/P7T07LiteratureCaseBoundaryTests.cs` - focused
  candidate/deferred boundary and lattice/full-core shape checks.
- `tests/ReactorSim.Golden.Tests/ReactorSim.Golden.Tests.csproj` - copies the
  candidate case artifact into focused test output.
- `docs/tasks/P7-T07.md` - this required report.

Pre-existing files inspected but not changed include `AGENTS.md`, the complete
`docs/Implementation_plan.md`, `docs/PROJECT_SCOPE.md` before this handoff,
the P1-T08 digest and source manifest, P2-T04/P2-T05 specifications and
reports, P7-T01 through P7-T06 reports, G7A, existing runtime code, and the
P7-T06 approved synthetic authority package.

## Assumptions and design choices

- Applicable P1-T08 rows are `S1-R02`, `S1-R03`, `S1-R04`, `S1-R05`, `S1-R06`,
  `S1-R07`, `S1-R09`, `S1-R11`, `S5-R02`, `S5-R03`, `S5-R04`, `S5-R05`,
  `S5-R06`, `S5-R07`, `S5-R08`, and `S5-R09`. Excluded-source notes preserve
  the reason S2, S3, S4, and S6 do not emit a case in this task.
- Source-reported values such as 380 channels, 12 bundles/channel, 8-bundle
  shifts, 4 channels/day, 300 full-power days, 300 days, 31.9713 kW/kg, and
  9.6 GWd/t remain `CandidateCaseDesign` or `CandidateNumericEvidence`. They
  do not select runtime constants, equations, units, normalization, or
  tolerances.
- The candidate cards use the digest's paraphrased claims and source identity;
  no paper text, PDF, private path, raw input, or generated output was copied.
- Potential project mappings are marked `NotProven`; no new `QuantityId`, public
  schema, direct-coverage claim, runtime input, or golden artifact was added.
- P2-T04 remains authoritative for all equations, signs, units, state/event
  order, and I/Xe behavior. P2-T05 remains authoritative for comparison and
  coverage semantics. P7-T06's approved synthetic package is unchanged.
- S5 is recorded as CC BY 4.0 and S1 as CC BY-NC-ND 4.0 in the cards, but all
  source artifacts remain external/private and no derived source run is
  redistributed.

## Validation commands and results

### Candidate artifact and path audit - T0/T1

```text
PowerShell path/JSON assertion over the six P7-T07 artifact, report, tool, and test paths
```

Result: `T0_P7T07_PATH_JSON_PASS files=6 cases=3 status=Candidate/Deferred/NoGolden`.

```text
dotnet C:\Users\infin\candu\tools\P7T07LiteratureCases\bin\Release\net10.0\ReactorSim.P7T07LiteratureCases.dll validate C:\Users\infin\candu\data\comparisons\p7-t07-literature-cases-v1.json C:\Users\infin\candu\data\comparisons\p7-t07-literature-cases-v1.manifest.json
```

Result: `P7_T07_LITERATURE_VALIDATE_PASS cases=3`; artifact size `15080`
bytes; artifact SHA-256
`94f3d87cbbebf4dc09074c19e2f4fc3359b8fe47930bf70e00b2e8ab4ce63ede`; source
manifest SHA-256
`df9b06987dc831b61c2f75daf368628d697b5ab1b0605466134ee0c73bbd25a1`;
status `Candidate/Deferred/NoGolden`.

### Standalone validator build - T0/T1

```text
Push-Location C:\Users\infin; dotnet build C:\Users\infin\candu\tools\P7T07LiteratureCases\ReactorSim.P7T07LiteratureCases.csproj --configuration Release --nologo; Pop-Location
```

Result: exit `0`, `0` warnings, `0` errors. The project has no
`ReactorSim.Core` reference.

### Focused Golden boundary tests - T1

```text
Push-Location C:\Users\infin; dotnet build C:\Users\infin\candu\tests\ReactorSim.Golden.Tests\ReactorSim.Golden.Tests.csproj --configuration Release --nologo; Pop-Location
Push-Location C:\Users\infin; dotnet test C:\Users\infin\candu\tests\ReactorSim.Golden.Tests\ReactorSim.Golden.Tests.csproj --configuration Release --no-build --nologo --filter FullyQualifiedName~P7T07; Pop-Location
```

Result: build exit `0`, `0` warnings/errors; focused tests `2/2` passed,
`0` failed, `0` skipped.

### Full headless regression - T3

```text
Push-Location C:\Users\infin; dotnet test C:\Users\infin\candu\ReactorSim.sln --no-restore --configuration Release --nologo --logger "console;verbosity=minimal" -p:TargetPlatformIdentifier= -p:TargetPlatformVersion=; Pop-Location
```

Result: CLI `5/5`, Core `189/189`, and Golden `24/24` passed with `0` failures
and `0` skips. The Core total was confirmed separately after the parallel
solution output printed the CLI and Golden summaries first.

### Format and whitespace - T0

```text
Push-Location C:\Users\infin; & 'C:\Users\infin\AppData\Local\Temp\candu-sdk-10.0.302\dotnet.exe' format C:\Users\infin\candu\ReactorSim.sln whitespace --verify-no-changes --no-restore --verbosity minimal; Pop-Location
git diff --check
```

Result: pinned format verification exited `0`; `git diff --check` exited `0`
with only the normal LF-to-CRLF warning for the project file.

T6 was not run: no external reference baseline, source input, generated
reference output, or approved golden artifact changed. These cards explicitly
record that a future authority-admission task must run T6 if it obtains a
reproducible source case.

## Token and cost accounting

| Field | Value | Source/notes |
|---|---:|---|
| Input tokens | `Unavailable` | No request-level telemetry exposed. |
| Cached input tokens | `Unavailable` | No request-level telemetry exposed. |
| Cache-write input tokens | `Unavailable` | No request-level telemetry exposed. |
| Output tokens | `Unavailable` | No request-level telemetry exposed. |
| Reasoning output tokens | `Unavailable` | No request-level telemetry exposed. |
| Total tokens | `Unavailable` | No request-level telemetry exposed. |
| Estimated cost | `Unavailable` | No price source or allocation telemetry exposed. |
| Goal-service total | `Unavailable` | No goal-service allocation was available. |

Cost formula/basis: unavailable; no provider usage receipt or price source was
exposed, so no worker/reviewer allocation was invented.

## Numerical differences

No numerical comparison was executed and no numerical behavior changed. The
cards preserve source-reported candidate values and claims only. Absolute and
relative differences, tolerance results, and source-vs-runtime residuals are
`NotApplicable` until an exact source case is admitted and independently run.

## Deferred validation

- Exact DRAGON5/DONJON5/SERPENT case reproduction and source-output comparison -
  deferred to a separately authorized authority-admission task because the
  digest lacks exact inputs, builds, data checksums, outputs, and rights.
- T6 reference regeneration - deferred to that same task if a complete case is
  lawfully available.
- Production/full-core or lattice golden approval - deferred; this task does
  not approve the cards or replace P7-T06 synthetic evidence.
- T4/T5 - not applicable; no Unity, platform, serialization, or runtime change.

## Blockers, risks, and follow-up

- Blockers: none for candidate-case design; exact source comparison remains
  intentionally blocked by the admission gaps recorded in every card.
- Risk triggers: literature/golden-data provenance, units/normalization,
  reference mapping, determinism boundary, and candidate-consumer changes
  triggered T3 and independent code review (high).
- Risks accepted or deferred: a paper-described model may not be reproduced
  from published prose; no candidate value may tune the runtime or become a
  tolerance/golden baseline.
- Follow-up: owner-authorized source-case admission task for one selected case
  after exact inputs, tool/data identity, output history, and rights are
  available.

## Next eligible task

None until a complete, reproducible, and lawfully retained source case is
available for a separately authorized admission task. P7-T06's approved
Synthetic/test-only artifact remains the current bounded comparator.

Source: [`AGENTS.md`](../../AGENTS.md),
[`docs/Implementation_plan.md`](../Implementation_plan.md),
[`docs/reference/candu-literature-digest-v1.md`](../reference/candu-literature-digest-v1.md),
[`docs/tasks/P1-T08.md`](P1-T08.md),
[`docs/tasks/P2-T04.md`](P2-T04.md),
[`docs/tasks/P2-T05.md`](P2-T05.md),
[`docs/tasks/P7-T06.md`](P7-T06.md), and
[`docs/research/P7-T07-case-design.md`](../research/P7-T07-case-design.md).
