# G2-C05 - Resolve plan/tolerance-profile authority conflict

## Outcome

Status: COMPLETE

This user-authorized corrective task resolves G2 finding 5 without inventing
numeric values. Phase 2 requirement 13 now requires quantity-specific
tolerance-profile identity, comparison semantics, threshold state, approval
status, and owner-gate binding. It explicitly keeps numeric thresholds deferred
until evidence-backed approval at the named owner gate. The P2-T05 contract and
the failed G2 gate remain otherwise unchanged. G2 itself remains failed because
the other five findings were not part of this task.

## Files created or changed

- `docs/Implementation_plan.md` - amended requirement 13 and Phase 2 exit
  evidence to align the plan with P2-T05 deferred-profile semantics.
- `docs/tasks/G2-C05.md` - this task report.

No specification, equation, schema, reference/golden artifact, runtime source,
Unity file, gate report, or prior task report was changed. The supplied Unity
environment was inventoried but not exercised in this task.

## Assumptions and design choices

- The user authorization selects the plan-amendment resolution permitted by G2:
  align requirement 13 with P2-T05 rather than inventing thresholds.
- Phase 2 defines profile identity, units/scope/norm, comparison rule, status,
  and ownership. Numeric acceptance values remain the responsibility of G4,
  G5, G6, G7A, or G7B according to the P2-T05 crosswalk.
- The task ID `G2-C05` maps directly to G2 finding 5. No later corrective task
  or G2 rerun was combined with it.
- A fresh empty-context Luna worker was assigned only `docs/Implementation_plan.md`.
  It was stopped after repeated timeboxes without a useful artifact or reading
  checkpoint; the root agent then made the single bounded edit locally. No
  worker edit remained in the repository.

## Validation commands and results

### Pre-task scope freeze (T0)

```text
External snapshot: C:\Users\infin\AppData\Local\Temp\candu-g2-c05-20260808-220950\git-visible-manifest.txt
Git-visible paths: 704
Manifest SHA-256: f07f23d8a24a746ee5850db94b771dff47581e5aecb8c51b925866ed30beb678
Allowed implementation file: docs/Implementation_plan.md
```

Result: PASS. Byte lengths and SHA-256 hashes for all 704 Git-visible paths
were frozen outside the repository. Existing Unity project/generated paths were
preserved as pre-task state.

### Required reading and cross-spec inspection (T0)

```powershell
Get-Content -Raw AGENTS.md
Get-Content -Raw docs\Implementation_plan.md
Get-Content -Raw docs\spec\observables-validation-methodology-v1.md
Get-Content -Raw docs\tasks\P2-T01.md; Get-Content -Raw docs\tasks\P2-T02.md
Get-Content -Raw docs\tasks\P2-T03.md; Get-Content -Raw docs\tasks\P2-T04.md
Get-Content -Raw docs\tasks\P2-T05.md; Get-Content -Raw docs\tasks\G1.md
Get-Content -Raw docs\tasks\G2.md; Get-Content -Raw docs\gates\G2.md
```

Result: PASS. The governing instructions, complete Phase 2 prerequisite
reports/specifications, failed gate, and current task boundary were inspected.

### Focused plan/P2-T05 contract assertion (T1)

```powershell
$plan=Get-Content -Raw docs\Implementation_plan.md; $spec=Get-Content -Raw docs\spec\observables-validation-methodology-v1.md; $required=@('13. Validation observables and quantity-specific provisional tolerance profiles','quantity/unit/scope/norm identity','threshold','approval status','owner gate','must not invent numeric','G4, G5, G6, G7A, or','Quantity-specific tolerance profile identity, ownership','numeric thresholds may remain'); foreach($term in $required){if(-not $plan.Contains($term)){throw "Missing plan term: $term"}}; foreach($term in @('Every numeric profile declared by P2-T05 has `ApprovalStatus=Provisional`','`ThresholdState=Deferred`','G2 approves','does not approve numeric tolerances')){if(-not $spec.Contains($term)){throw "P2-T05 compatibility term missing: $term"}}; if($plan.Contains('AbsoluteTolerance=') -or $plan.Contains('RelativeTolerance=')){throw 'Unexpected numeric tolerance field in plan amendment'}; $baseline='C:\Users\infin\AppData\Local\Temp\candu-g2-c05-20260808-220950\git-visible-manifest.txt'; $row=@(Get-Content $baseline|Where-Object{$_ -like ('docs/spec/observables-validation-methodology-v1.md'+[char]9+'*')}); if($row.Count-ne 1){throw 'P2-T05 baseline row missing or duplicated'}; Write-Output 'PASS: plan/P2-T05 tolerance contract assertions';
```

Result: exit code 0; PASS. The plan amendment contains no numeric threshold and
the P2-T05 compatibility markers and immutable baseline row were present. Three
earlier literal assertions were false negatives caused by Markdown line wraps;
the candidate was unchanged and the normalized final assertion above passed.

### Repository format check (T0)

```powershell
& .\tools\Check-Format.ps1
```

Result: exit code 0; PASS.

### Triggered headless suite (T3)

```powershell
$artifacts=Join-Path $env:TEMP 'candu-g2-c05-t3'; New-Item -ItemType Directory -Force -Path $artifacts | Out-Null; & .\tools\Test-FullHeadlessSuite.ps1 -ConfirmFullSuite -ArtifactsPath $artifacts; if($LASTEXITCODE -ne 0){throw "T3 failed with exit code $LASTEXITCODE"}; Write-Output "T3 PASS: artifacts=$artifacts"
```

Result: exit code 0; `ReactorSim.Core.Tests` passed `2/2`, failed `0`, skipped
`0`; Golden tests discovered `0` tests and had no failures. Build/test output
was external under `C:\Users\infin\AppData\Local\Temp\candu-g2-c05-t3`.
TRX output was external under
`C:\Users\infin\AppData\Local\Temp\candu-full-headless-trx-20c943fc749543a1a4bf8a81850437f9`.

### Independent Sol review

Fresh empty-context `gpt-5.6-sol`, max reasoning, read-only review: no Critical,
High, Medium, or Low findings; final `PASS`. Sol confirmed that the amendment
matches all 85 deferred numeric-profile rows in P2-T05, whose owner gates are
G4, G5, G6, G7A, or G7B, and changes no equations, schemas, reference/golden
data, task ordering, or gate disposition. The review was limited to G2 finding
5; Unity evidence was intentionally not reviewed here.

### Unity environment inventory (read-only, deferred task input)

```text
Unity executable: C:\Users\infin\AppData\Local\Unity\bin\unity.exe
Executable SHA-256: ff9ef81ade1063041d25e2c549cc7ed14e96d446f4204400bf101b389f7b8502
Project editor version: 6000.3.21f1 (c02631ffc030)
```

Result: the authorized executable and project were found. No Unity editor,
EditMode, PlayMode, or desktop-player command was run under G2-C05.

### Final mechanical scope audit (T0)

Result: PASS after this report is written. The audit compared every current
Git-visible path against the 704-path external snapshot: final scope was 705
paths, with exactly one modified path (`docs/Implementation_plan.md`) and one
added path (`docs/tasks/G2-C05.md`); there were no removals, unexpected paths,
or changes to any other file. The artifact scan found 525 pre-existing paths
matching cache/generated/binary patterns, all under the supplied nested Unity
workspace (`unity/ReactorGame/New Unity Project/...`), and found `0` new
artifact paths. They were not touched or deleted. This is a pre-existing
environment hygiene risk for the separate Unity task, not a G2-C05 output.

Final manifest: `C:\Users\infin\AppData\Local\Temp\candu-g2-c05-final-audit\git-visible-manifest.txt`.
The aggregate manifest hash is handed off with the final audit; embedding it in
this Git-visible report would change the manifest it hashes.

## Numerical differences

Not applicable; no runtime behavior, equation, reference baseline, golden data,
or numeric tolerance value changed.

## Deferred validation

- Actual Unity EditMode, PlayMode, and desktop-player smoke evidence - deferred
  to a separate authorized G2 T4 corrective task using the inventoried
  environment.
- Full G2 rerun - deferred until all six findings are closed.
- T6 reference rerun and T5 mobile validation - not triggered by this plan-only
  correction.

## Blockers, risks, and follow-up

- Blockers: G2 remains `FAIL` on findings 1-4 and 6; no production Phase 3 task
  is eligible yet.
- Risk triggers: tolerance/profile semantics and plan/specification authority
  triggered T3 and independent Sol review; both passed for this bounded change.
- Risks accepted or deferred: no numeric threshold was invented or relaxed;
  Unity evidence and the other G2 lifecycle/ordering/rollback/reporting
  findings remain open.
- Follow-up: authorize and execute the separate Unity T4 corrective task, then
  the remaining G2 corrective tasks sequentially before rerunning G2.

## Next eligible task

`G2-C01` - complete actual Unity EditMode, PlayMode, and desktop-player smoke
evidence using the authorized Unity environment, pending its separate bounded
task authorization. Do not combine it with G2-C05.

Source: [`docs/Implementation_plan.md`](../Implementation_plan.md),
[`docs/gates/G2.md`](../gates/G2.md), and the P2-T05 methodology.
