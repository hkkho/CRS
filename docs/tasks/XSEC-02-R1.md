# XSEC-02-R1 - recovered source-mapping T3 revalidation

## Outcome

Status: COMPLETE

The mandatory portable-artifact T3 that blocked XSEC-02 now passes after the
separate test-only `TEST-INFRA-02` recovery. The XSEC-02 mapping specification
and machine record were rechecked but not modified. This completes the
candidate-mapping checkpoint only; it does not run DRAGON/DONJON, create a
runtime pack, admit a full-core case, or call any data golden, authoritative,
licensed for redistribution, release-ready, or Unity-ready.

Effectiveness: SUCCESS

Applicable P1-T08 digest rows: `S1-R04`, `S1-R05`, `S5-R02`, `S5-R03`,
`S5-R04`, `S5-R09`, and `S6-R03`. They remain methodology context; no new
physics or numerical authority was selected.

## Execution and model evidence

- Role: root implementer.
- Requested model / reasoning: GPT-5.6 Luna / high.
- Actual model / reasoning: UNVERIFIED; request-level execution telemetry is
  unavailable.
- Execution receipt or telemetry source: current Codex task; no provider
  request-level receipt was exposed.
- Attempts: one bounded revalidation task; elapsed time: unavailable as an
  allocable task total.
- Artifact/checkpoint status: produced; checkpoint pending after this report
  and scope reconciliation.
- Review disposition: inherited PASS for the unchanged candidate mapping from
  the same reviewer, Popper (`01a03b98-9b86-7f22-9068-76bb1ce6b01e`).
- Review evidence verification: UNVERIFIED. No fresh review was started because
  no reviewed candidate file changed; the prior review was already its final
  same-reviewer disposition.
- Reviewer reuse/fresh-review rationale: reuse of XSEC-02 final review avoids a
  duplicate fresh-review swarm; this task changes only evidence routing.

## Files created or changed

- `docs/tasks/XSEC-02-R1-REQUEST.md` - bounded revalidation authority and
  frozen-input constraints.
- `docs/tasks/XSEC-02-R1.md` - this report.

The XSEC mapping specification, mapping manifest, DRAGON/DONJON inputs,
runtime/Core/CLI/Unity source, schemas, pack bytes, approvals, physics
specifications, tolerances, and golden values were inspected but not changed.

## Assumptions and design choices

- The fresh artifact-wrapper result is valid replacement T3 evidence because
  `TEST-INFRA-02` changed only test-project output declarations and its report
  proves unchanged runtime/data behavior.
- XSEC-02's previously reviewed Version5 commit, two-group identity,
  post-EDI/pre-SPH boundary, and candidate-only status must match exactly;
  revalidation fails closed on a mismatch rather than repairing a mapping.
- The historical XSEC-02 blocked report is immutable evidence of the original
  artifact-layout finding and remains preserved.

## Validation commands and results

### T0 - frozen mapping and whitespace check

```text
$mapping = Get-Content -Raw reference/manifests/xsec-02-mapping-spec-v1.json |
  ConvertFrom-Json
# Assert Version5 commit eee582f8594d3b7d01b65660ca1e8bef00eb6b2a,
# post-EDI_pre-SPH export stage, and not-a-runtime-pack/not-golden status.
git diff --check
```

Result: PASS (`XSEC-02-R1_T0_PASS`); the only initial working-tree change was
this task-definition record.

### T3 - fresh portable full-suite wrapper

```text
$sdkRoot = Join-Path $env:TEMP 'candu-dotnet-sdk-10.0.302'
$env:DOTNET_ROOT = $sdkRoot
$env:PATH = $sdkRoot + ';' + $env:PATH
$artifactsPath = Join-Path $env:TEMP ('candu-xsec-02-r1-t3-' +
  [Guid]::NewGuid().ToString('N'))
.\tools\Test-FullHeadlessSuite.ps1 -ConfirmFullSuite -ArtifactsPath $artifactsPath
```

Result: PASS (exit 0) at fresh artifact root
`candu-xsec-02-r1-t3-bf6aee8d06444a38a8587b4cedb6a5c5`: Core `208/208`,
Golden `26/26`, CLI `38/38`; zero failures and zero skips. This specifically
closes the artifact-layout failure from XSEC-02. No DRAGON/DONJON output or
numerical comparison was generated by this test-only revalidation.

## Token and cost accounting

| Field | Value | Source/notes |
|---|---:|---|
| Input tokens | Unavailable | No task-level telemetry exposed |
| Cached input tokens | Unavailable | No task-level telemetry exposed |
| Cache-write input tokens | Unavailable | No task-level telemetry exposed |
| Output tokens | Unavailable | No task-level telemetry exposed |
| Reasoning output tokens | Unavailable | No task-level telemetry exposed |
| Total tokens | Unavailable | No task-level telemetry exposed |
| Estimated cost | Unavailable | No reliable task-level price allocation |
| Goal-service total | Unavailable | Reported separately; not added to this task |

Cost formula/basis: unavailable. No per-worker or review cost is invented from
aggregate service usage.

## Numerical differences

Not applicable; this task changed no numerical behavior, equation, unit,
normalization, convergence rule, tolerance, source data, schema, pack, or
golden/reference baseline.

## Deferred validation

- T6 DRAGON5 lattice reproduction and repeat-manifest comparison - owned by
  XSEC-03.
- DONJON5 static-core reproduction - deferred to XSEC-04 after the separate
  XSEC-CORE-01 authority completes.
- Converter/pack/Core/CLI/Unity consumption - deferred to XSEC-05 through
  XSEC-07; no runtime pack is admitted yet.

## Blockers, risks, and follow-up

- Blockers: none for the mapping checkpoint. The original artifact-layout
  blocker is closed by the fresh passing wrapper above.
- Risk triggers: source-to-runtime mapping requires T3 and independent high
  review. T3 passed; the final XSEC-02 same-reviewer PASS is reused because the
  candidate was unchanged, with telemetry explicitly UNVERIFIED.
- Risks accepted or deferred: external internal-use permission remains bounded
  by XSEC-RIGHTS-01; raw JEF/JEFF data and generated pack redistribution remain
  prohibited. No source rerun or technical admission can be inferred from this
  checkpoint.
- Follow-up work: XSEC-03 must execute two fresh offline DRAGON lattice runs
  with exact input/tool/library identities, convergence evidence, semantic
  export, and T6 comparison. Unsupported state variants must be reported, not
  fabricated.

## Next eligible task

`XSEC-03` - reproduce the bounded DRAGON5 lattice/depletion candidate and
export its approved semantic source fields with fresh T6 evidence.

Source: [`AGENTS.md`](../../AGENTS.md),
[`Implementation_plan.md`](../Implementation_plan.md),
[`PROJECT_SCOPE.md`](../PROJECT_SCOPE.md),
[`XSEC-02.md`](XSEC-02.md),
[`XSEC-02-R1-REQUEST.md`](XSEC-02-R1-REQUEST.md), and
[`TEST-INFRA-02.md`](TEST-INFRA-02.md).
