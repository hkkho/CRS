# G4-R5 - separately executed reduced-model disposition

## Outcome

Status: BLOCKED

The separately executed G4-R5 review assessed only the owner-authorized G4J
reduced-model candidate. It verified candidate authority-field completeness,
deterministic regeneration, standalone repeat, Core consumption, and the
existing production boundary. The final code-review disposition is
`BLOCKED` / `NO-ADMISSION` for reduced-model tolerance or golden promotion:
the standalone result is implementation-level independent evidence, not an
exact oracle or admissible external authority. The candidate remains
`Candidate` / `Deferred` / `NoGolden` with its diagnostic-only `1e-8` bound.

Effectiveness: BLOCKED

## Execution and model evidence

- Role: root implementer; separate gate execution and evidence disposition.
- Requested model / reasoning: GPT-5.6 Luna / high; code review (high).
- Actual model / reasoning: `UNVERIFIED`.
- Execution receipt or telemetry source: root task context; bounded reviewer
  `01a00dee-bfe2-7140-9e64-8d5b1749de48`; reviewer receipt unavailable.
- Attempts: 1 bounded G4-R5 execution and 1 bounded independent review;
  elapsed time: `Unavailable`.
- Artifact/checkpoint status: produced; final gate and task reports, scope
  routing, and chain handoff were recorded.
- Review disposition: `BLOCKED` / `NO-ADMISSION`; G4J remains candidate-only
  and production G4 remains blocked.
- Review evidence verification: `UNVERIFIED`.
- Reviewer reuse/fresh-review rationale: same bounded reviewer/context was
  used for the final disposition; no fresh-review swarm or prior-task restart
  was warranted or performed.

Applicable P1-T08 digest rows:

- Topology/case design: `S1-R03`, `S1-R06`, `S5-R02`, `S6-R04`.
- Two-group/reduced projection: `S1-R04`, `S1-R05`, `S5-R03`, `S5-R04`,
  `S6-R03`.
- Burnup/refuelling: `S1-R07`, `S4-R03`, `S5-R05`, `S6-R05`.
- RRS/device case design: `S1-R08`, `S2-R05`, `S6-R02`, `S6-R04`, `S6-R05`,
  `S6-R08`.
- Poison/feedback context: `S1-R04`, `S1-R08`, `S1-R10`, `S3-R05`, `S6-R03`,
  `S6-R05`, `S6-R06`.
- Validation/admission methodology: `S1-R04`, `S1-R11`, `S5-R03`, `S5-R06`,
  `S5-R07`, `S5-R09`, `S6-R03`, `S6-R08`.

`S3-R01` through `S3-R08` are `NotApplicable` to this case because they cover
SCWR/CATHENA thermal-hydraulic or safety scope. `S4-R04` and `S6-R07` are
`NotApplicable` because advanced-cycle behavior is out of scope. I-135/Xe-135
and kinetics are explicitly `NotCovered`, not inferred from literature.

## Files created or changed

- `docs/gates/G4-R5.md` - immutable reduced-model gate disposition and
  evidence record.
- `docs/tasks/G4-R5.md` - this task report.
- `docs/PROJECT_SCOPE.md` - current G4-R5 state, evidence gap, and routing.
- `docs/tasks/ROUND-2026-08-16-G4-BLOCK-RESOLUTION-CHAIN.md` - G4-R5 chain
  row and handoff.

The following pre-existing files were inspected and preserved: the G4J
definition, candidate artifact and manifest; the G4J standalone generator and
validator; the candidate Core consumer; the G4I approved synthetic payload;
all frozen specifications; and earlier G4/G4-R4 reports. No production code,
candidate artifact, Golden payload, test source, or project link was retained
as changed by the final G4-R5 disposition. No G4J payload was added under
`data/golden`; the only two files there remain the pre-existing G4I payload and
manifest.

## Assumptions and design choices

- The gate is restricted to the `ReducedModel` domain named by G4J. It does not
  reopen G4-R3, G4-R4, G4H, or any earlier authority task.
- G4J's `1e-8` bound is retained as diagnostic metadata only. It is not a gate
  tolerance and was not promoted from raw observed differences.
- The standalone generator is useful implementation-level cross-checking, but
  its expected values and the Core comparison derive from the same
  project-authored definition/material pack. No independent authority claim
  is made.
- `source_commit=UNAVAILABLE_NO_GIT_HEAD` is preserved. The content hashes
  bind the candidate snapshot but cannot substitute for an immutable release
  or external-source authority.
- The transient provisional promotion evaluated during gate execution was
  rejected by the final independent review and removed before final
  validation; the repository's final state contains no G4J golden payload or
  approved tolerance profile.

## Validation commands and results

### Candidate regeneration and hash-bound validation (T1)

```powershell
powershell -NoProfile -ExecutionPolicy Bypass -File .\tools\RepresentativeReducedAuthority\Test-P4-T06-G4J.ps1
```

Result: exit 0; Release build 0 warnings/0 errors; candidate generation and
validation passed with five scenarios, 48 nodes, 92 edges, 104 boundaries,
`repeat_equal=True`, artifact SHA-256
`4e4cf79f726fc1202a40b01db0cd296f337c3abb087a21a1abd5158483cc05c3`, and
manifest SHA-256
`8b8506b4467debe4ffd9f352b661235823282cb9d48a309cf375bb8a268eb215`.

### Candidate authority audit (T1)

The exact audit recorded in `docs/gates/G4-R5.md` exited 0 with:

```text
G4_R5_CANDIDATE_AUDIT_PASS candidate_sha256=4e4cf79f726fc1202a40b01db0cd296f337c3abb087a21a1abd5158483cc05c3 candidate_manifest_sha256=8b8506b4467debe4ffd9f352b661235823282cb9d48a309cf375bb8a268eb215 definition_sha256=bf227e60bd2c9540c699786c530c079bb31caf4108cf461704f37c81f6758b6e source_pack_sha256=0de429b27f78b5e2c5fe724916b14ac8ce45ba05560ab73f3fa4805befd11a0e scenarios=5 nodes=48 edges=92 boundaries=104 authority_missing=0 s4_events=2 rrs_entries=96 poison_entries=96 golden_payload_files=2 path_hits=0
```

### Focused Core consumer (T1/T2)

```powershell
dotnet test C:\Users\infin\candu\tests\ReactorSim.Golden.Tests\ReactorSim.Golden.Tests.csproj --configuration Release --nologo --no-restore --filter FullyQualifiedName~P4T06G4J
```

Result: exit 0 when run from `C:\Users\infin`; 2/2 passed, 0 failures, and 0
skips. All five candidate scenarios converged in Core within the diagnostic
bound. A run from the repository directory is blocked before test discovery
by the pinned SDK mismatch (`10.0.302` requested, `10.0.303` installed); the
pin was not modified.

### Full headless regression (T3)

```powershell
dotnet test C:\Users\infin\candu\ReactorSim.sln --configuration Release --nologo --no-restore
```

Result: exit 0 when run from `C:\Users\infin`; Core 103/103 and Golden 14/14
passed, with 0 failures and 0 skips. The suite validates repository regression
health and test consumers; it does not promote the G4J candidate.

### Format validation (T0)

```powershell
dotnet format C:\Users\infin\candu\ReactorSim.sln --no-restore --verify-no-changes
```

Result: exit 1 before analysis because `global.json` requests SDK `10.0.302`
with roll-forward disabled and only `10.0.303` is installed. No source or
configuration file was changed to bypass this limitation.

### T6/T4/T5 applicability

T6 was not applicable because no external reference baseline or production
golden data changed. T4 and T5 were not applicable because no Unity adapter,
serialization contract, mobile, or platform code changed.

## Token and cost accounting

| Field | Value | Source/notes |
|---|---:|---|
| Input tokens | `Unavailable` | No request-level telemetry exposed |
| Cached input tokens | `Unavailable` | No request-level telemetry exposed |
| Cache-write input tokens | `Unavailable` | No request-level telemetry exposed |
| Output tokens | `Unavailable` | No request-level telemetry exposed |
| Reasoning output tokens | `Unavailable` | No request-level telemetry exposed |
| Total tokens | `Unavailable` | No request-level telemetry exposed |
| Estimated cost | `Unavailable` | No verified price source or billing receipt |
| Goal-service total | `Unavailable` | Not exposed; not allocable to this task |

Cost formula/basis: unavailable; no request-level billing semantics or price
receipt was exposed.

## Numerical differences

These are candidate reduced-model outputs only; no approved tolerance or
golden comparison exists:

| Scenario | k | Total power | Outer iterations | Residual relative infinity |
|---|---:|---:|---:|---:|
| `fresh_start` | `0.32569333812682566` | `1000 W` | 1823 | `9.935461458409358e-11` |
| `equilibrium_like` | `0.3067650216236955` | `1000 W` | 681 | `9.876534557925246e-11` |
| `refuelled_4_bundle_shift` | `0.31039330409549426` | `1000 W` | 850 | `8.177231151682484e-11` |
| `rrs_tilt_perturbation` | `0.3085369346181452` | `1000 W` | 676 | `9.727510171639472e-11` |
| `bulk_poison_perturbation` | `0.3022545018722577` | `1000 W` | 707 | `9.871078899029496e-11` |

Standalone repeats were byte-identical. Core comparisons passed only the
candidate's diagnostic `1e-8` bound. No numerical difference was promoted to a
gate profile.

## Deferred validation

- Exact manufactured oracle or complete independent authority for the G4J
  reduced-model sequence - deferred to a new owner-authorized task because the
  current candidate lacks admissible expected values and provenance.
- External/production CANDU authority, full-core coverage, I/Xe/kinetics,
  liquid-zone/adjuster runtime sequences, and production histories - deferred
  to their named production gates; `P5-T10`/`G5` remain blocked.
- `dotnet format` - deferred until SDK `10.0.302` is available or an
  separately authorized SDK-pin decision is made.

## Blockers, risks, and follow-up

- Blockers: no admissible tolerance/golden basis for G4J; production G4,
  `P5-T10`, and `G5` remain blocked.
- Risk triggers: numerical/reference/golden admission work triggered T3 and
  code-review (high). Review receipt telemetry remains `UNVERIFIED`.
- Risks accepted or deferred: candidate determinism is not authority;
  source-commit identity is unavailable; the synthetic source pack is not
  external nuclear-data authority; production and advanced-state coverage are
  unresolved.
- Follow-up: authorize a new exact-oracle or complete independent-authority
  task if reduced-model admission is still desired. Do not restart G4J or
  earlier G4 tasks and do not start `P5-T10` from this candidate.

## Next eligible task

None is automatically eligible. A new owner-authorized exact-oracle or
complete independent-authority task may be created; production G4 remains
blocked.

Source: [`AGENTS.md`](../../AGENTS.md),
[`docs/Implementation_plan.md`](../Implementation_plan.md),
[`docs/PROJECT_SCOPE.md`](../PROJECT_SCOPE.md),
[`docs/gates/G4-R5.md`](../gates/G4-R5.md), and
[`docs/tasks/TASK_REPORT_TEMPLATE.md`](TASK_REPORT_TEMPLATE.md).
