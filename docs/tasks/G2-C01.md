# G2-C01 - Complete Unity EditMode, PlayMode, and desktop-player smoke evidence

## Outcome

Status: COMPLETE

The user-authorized G2-C01 corrective task added a real Unity Test Framework
harness and completed the required T4 evidence on the pinned Unity editor:
EditMode, PlayMode, and a StandaloneWindows64 desktop player each ran one
targeted test and passed. The earlier G2 finding 1 is closed by evidence; G2
itself remains FAIL until its other findings are corrected and the gate is
rerun.

## Files created or changed

- `unity/ReactorGame/Packages/manifest.json` - pinned the built-in
  `com.unity.test-framework` package to `1.6.0`.
- `unity/ReactorGame/Packages/packages-lock.json` - Unity-resolved the exact
  test-framework lock entry and its built-in transitive entries.
- `unity/ReactorGame/Assets/ReactorGame.Unity/Tests/Editor/G2C01.EditModeTests.cs`
  and `.asmdef` - opened the real Bootstrap scene and asserted the exact
  approved serialization probe payload.
- `unity/ReactorGame/Assets/ReactorGame.Unity/Tests/PlayMode/G2C01.PlayModeTests.cs`
  and `.asmdef` - yielded one runtime frame, asserted `Application.isPlaying`,
  and asserted the same exact serialization probe payload; the assembly is
  player-eligible.
- Unity-generated `.meta` files for the new test folders and four test files.
- `docs/tasks/G2-C01.md` - this report.

Pre-existing adapter, scene, Core source, project settings, gate reports,
fixtures, and task reports were inspected. The generated Core DLL, Unity logs,
XML results, player, and Unity project cache stayed outside the repository.

## Assumptions and design choices

- The installed pinned editor is `6000.3.21f1 (c02631ffc030)` at
  `C:\Users\infin\AppData\Local\Unity\Editors\6000.3.21f1\Editor\Unity.exe`,
  SHA-256 `bacff9aaacdd990d0725226e39f8ef39e17c20d859d20d2f1e52c96394fd6c98`.
- The local built-in Unity Test Framework metadata reports version `1.6.0`.
  Unity resolved `com.unity.ext.nunit` as built-in `2.0.5` at lock depth 1;
  the parent test-framework package remains exactly `1.6.0`, depth 0, source
  `builtin`.
- The tests exercise only the existing adapter serialization probe and scene;
  no physics, numerical tolerance, reference data, or runtime architecture was
  added.
- The fresh max-reasoning Luna worker owned the six implementation files. It
  initially missed its checkpoint and later returned the completed owned
  artifact; the root agent inspected the final contents and performed all
  integration, validation, scope, and disposition decisions.
- Unity temporarily materialized default values in `ProjectSettings.asset` and
  created `SceneTemplateSettings.json` during validation. Sol classified both
  as transient side effects; the project settings were restored exactly to the
  pre-task bytes and the generated scene-template file was moved externally.

## Validation commands and results

### Required reading and pre-task scope freeze - T0

```text
Get-Content -Raw AGENTS.md
Get-Content -Raw docs\Implementation_plan.md
Get-Content -Raw docs\spec\observables-validation-methodology-v1.md
Get-Content -Raw docs\tasks\P2-T01.md; Get-Content -Raw docs\tasks\P2-T02.md
Get-Content -Raw docs\tasks\P2-T03.md; Get-Content -Raw docs\tasks\P2-T04.md
Get-Content -Raw docs\tasks\P2-T05.md; Get-Content -Raw docs\tasks\G1.md
Get-Content -Raw docs\tasks\G2.md; Get-Content -Raw docs\gates\G2.md
```

External pre-task manifest:
`C:\Users\infin\AppData\Local\Temp\candu-g2-c01-20260808-224430\git-visible-manifest.txt`.
It contained 705 paths and has aggregate SHA-256
`5260909ef4a68c6cd6114d7213fa21d7a0aff8a0cf4ae6f1082aa01719a58f1b`.
The implementation scope was frozen before delegation to the two package files,
four test/assembly files, and their Unity-generated metadata.

### Core plugin preparation - T4 prerequisite

```text
& .\tools\Prepare-UnityCore.ps1 -ArtifactsPath C:\Users\infin\AppData\Local\Temp\candu-g2-c01-t4-20260808-2250
```

Result: exit code 0; Core Release build succeeded with 0 warnings and 0 errors.
The temporary plugin SHA-256 was
`ed958ba7b26a928c8f6910363af2b24abbcaf6bf5cc3844492e6f18f04484e6d`; it was
moved back to the external artifact root after testing.

### EditMode - T4

```text
& $editor -batchmode -nographics -accept-apiupdate -projectPath $project -runTests -testPlatform editmode -testResults C:\Users\infin\AppData\Local\Temp\candu-g2-c01-t4-20260808-2250\editmode.xml -logFile C:\Users\infin\AppData\Local\Temp\candu-g2-c01-t4-20260808-2250\editmode-rerun.log
```

Result: Unity exit code 0; XML result `Passed`, total 1, passed 1, failed 0,
skipped 0. The first exploratory invocation included `-quit`; Unity Test
Framework 1.6.0 logged that `-quit` prevents command-line tests, produced no
XML, and was not counted as evidence. The corrected command above omitted it.

### PlayMode - T4

```text
& $editor -batchmode -nographics -accept-apiupdate -projectPath $project -runTests -testPlatform playmode -testResults C:\Users\infin\AppData\Local\Temp\candu-g2-c01-t4-20260808-2250\playmode.xml -logFile C:\Users\infin\AppData\Local\Temp\candu-g2-c01-t4-20260808-2250\playmode.log
```

Result: Unity exit code 0; XML result `Passed`, total 1, passed 1, failed 0,
skipped 0.

### StandaloneWindows64 desktop-player smoke - T4

```text
& $editor -batchmode -nographics -accept-apiupdate -projectPath $project -runTests -testPlatform StandaloneWindows64 -buildPlayerPath C:\Users\infin\AppData\Local\Temp\candu-g2-c01-t4-20260808-2250\G2C01-ReactorGame.exe -testResults C:\Users\infin\AppData\Local\Temp\candu-g2-c01-t4-20260808-2250\desktop-player.xml -logFile C:\Users\infin\AppData\Local\Temp\candu-g2-c01-t4-20260808-2250\desktop-player.log
```

Result: Unity/Test Framework completed with exit code 0; the log records
`targetPlatform = StandaloneWindows64`, `Build Finished, Result: Success`, an
external `PlayerWithTests.exe`, `Platform: WindowsPlayer`, and test-run exit
code 0. XML result `Passed`, total 1, passed 1, failed 0, skipped 0.

### Static and focused final-candidate checks - T0/T1

```text
& .\tools\Check-Format.ps1
```

Result: exit code 0. An inline PowerShell assertion parsed the manifest and
lock, verified the `1.6.0` package pin/source/depth, verified both probe test
sources, parsed all three result XML files, and asserted the absence of the
generated DLL and scene-template file. Result: PASS for all three `1/1/0/0`
XML counts.

The restored `unity/ReactorGame/ProjectSettings/ProjectSettings.asset` is
exactly 20,483 bytes with SHA-256
`e96014423ef1d2acfb25af6769722b5af8c5e942edb4584660114ded411e5e32`.

### Independent Sol review and final disposition

Fresh empty-context max-reasoning `gpt-5.6-sol` reviewer Feynman was read-only.
The initial review returned no Critical/High/Medium findings and two Low
scope findings for Unity-generated ProjectSettings/SceneTemplate side effects.
Those files were restored or moved externally, affected checks were rerun, and
the frozen candidate was returned to the same reviewer. Final Sol disposition:
`PASS`, with no Critical, High, Medium, or Low findings. Sol confirmed the
external XML evidence, exact scope, package pin, and resolution of both Low
findings.

### Final mechanical scope audit - T0

External final-candidate manifest before this report:
`C:\Users\infin\AppData\Local\Temp\candu-g2-c01-t4-20260808-2250\final-candidate-manifest.txt`.
It contained 716 paths, aggregate SHA-256
`8a40df2417f63c7e17e32f5505fdf6d474b1cba3a2a2eebb043212d4f8e0d7a9`, exactly
11 additions, 2 owned package changes, and 0 removals against the 705-path
pre-task snapshot. No ProjectSettings, scene-template, DLL, private/raw, or
repository-cache path remained in the delta.

After this report was written, the post-report audit was rerun against the same
pre-task snapshot. Its external manifest is recorded in the sprint report and
contains the expected 717 paths: this report is the only additional path beyond
the final-candidate manifest, with the same 11 implementation additions, 2
package changes, and 0 removals.

No commit, push, publication, remote change, or destructive repository
operation was performed.

## Numerical differences

Not applicable; no numerical behavior, equation, tolerance, reference baseline,
or golden data changed. The serialization payload assertion was exact string
equality, not a numerical comparison.

## Deferred validation

- G2 rerun - deferred until the remaining G2 findings are corrected and their
  task reports and required reviews are complete.
- G2 findings 2, 3, 4, and 6 - remain open; this task only closes the complete
  Unity T4 evidence finding.
- T3, T5, and T6 - not triggered by this Unity harness/evidence correction;
  no Core behavior or reference baseline changed.

## Blockers, risks, and follow-up

- Blockers: none for G2-C01. G2 remains blocked by its other four High/Medium
  findings as recorded in `docs/gates/G2.md`.
- Risk triggers: Unity adapter, serialization, package/assembly contract, and
  desktop-player validation triggered T4 and independent Sol review; both
  completed with final PASS.
- Risks accepted or deferred: Unity licensing/network log warnings and the
  unavailable deployment-target logger were nonfatal; the Test Framework still
  built and connected to the Windows player and produced passing XML. Generated
  validation state was kept external or restored.
- Follow-up: obtain explicit authorization for the next bounded G2 corrective
  task, then correct the remaining lifecycle, deterministic RRS ordering,
  I/Xe rollback, and focused-evidence findings before rerunning G2.

## Next eligible task

No later task is eligible under the current authorization. The next item is the
next explicitly authorized bounded G2 corrective task for the remaining findings;
then rerun G2 after all corrective reports and reviews are complete.

Source: [`docs/Implementation_plan.md`](../Implementation_plan.md), sections 5-6,
and [`docs/gates/G2.md`](../gates/G2.md).
