# P4-T06-G4H - owner-authorized numeric baseline and tolerance decision

## Outcome

Status: COMPLETE

P4-T06-G4H completed the bounded authority audit required after G4-R3. No
admissible numeric/reference authority was present in the frozen specifications,
the already admitted G4G/G4F evidence, the repository artifacts, or the
available manifests. Production G4 approval is therefore explicitly declined
for this round, and `G4-R3`/`G4` remain `BLOCKED`.

This is a completed decision task, not a technical PASS for G4. It does not
infer a threshold from G4D/G4F differences, promote any candidate record,
create a golden payload, or change an equation, unit, normalization,
convergence rule, public schema, runtime dependency, website, or PDF.

Effectiveness: SUCCESS

## Execution and model evidence

- Role: root implementer; bounded authority audit and decision documentation
- Requested model / reasoning: GPT-5.6 Luna / high; code review (high)
- Actual model / reasoning: `UNVERIFIED`; execution telemetry did not expose
  the actual root model/effort receipt
- Execution receipt or telemetry source: root task context; reviewer
  `01a00dee-bfe2-7140-9e64-8d5b1749de48`; reviewer receipt telemetry unavailable
- Attempts: 1 bounded audit/documentation attempt and 1 bounded review attempt;
  elapsed time: `Unavailable`
- Artifact/checkpoint status: produced
- Review disposition: PASS; the reviewer found no remaining High, Medium, or
  Low findings
- Review evidence verification: `UNVERIFIED`
- Reviewer reuse/fresh-review rationale: the same bounded reviewer context was
  used for the audit disposition and final report review; no fresh reviewer
  was spawned

Applicable P1-T08 digest rows are `S1-R02`, `S1-R03`, `S1-R04`, `S1-R06`,
`S1-R11`, `S5-R03`, `S5-R04`, `S5-R09`, `S6-R03`, and `S6-R08`. They remain
context, methodology, or candidate-evidence support only. None is an
executable oracle, approved tolerance, or approved golden authority without
the complete case-admission proof.

## Files created or changed

- `docs/tasks/P4-T06-G4H.md` - records the authority audit, explicit decline,
  validation, review, and handoff.
- `docs/PROJECT_SCOPE.md` - refreshes the current Phase 4/G4H frontier and
  preserves the G4 blocker.
- `docs/tasks/ROUND-2026-08-16-G4-BLOCK-RESOLUTION-CHAIN.md` - records the
  named G4H handoff closure.

Pre-existing authority files and evidence were inspected but not changed,
including `AGENTS.md`, the complete implementation plan, the frozen P2
specifications, the P1-T08 report/digest, G4/G4-R1/G4-R2/G4-R3 reports,
P4-T06-R3/R4/R5, P4-T06-G4G, the G4D/G4F/R5 artifacts and manifests, the R4
candidate pack, and all runtime/source files. No file under `data/golden` was
added or changed.

## Authority audit and decision boundary

The audit checked the complete authority fields required by the task and the
P2-T05 case-admission contract:

1. exact source model, solver/tool/build/version and coupling identity;
2. nuclear-data or material identity, version, processing, and checksum;
3. geometry, topology, mesh, group structure, and homogenization mapping;
4. state, initial conditions, depletion/refuelling history, and event order;
5. authoritative units and all boundary conversions;
6. flux/power/reference-state normalization and sign convention;
7. observable identity, schema, ordering, comparison rule, and tolerance
   profile identity;
8. independent reproduction or traceable-equivalence evidence, including
   convergence/statistical information where applicable; and
9. rights to use, retain, and redistribute every input, output, and artifact.

No current artifact supplies all of these fields for an admissible production
baseline. The owner-confirmed redistribution boundary removes the historical
legal uncertainty for the bounded offline path, but it does not supply the
missing numerical provenance, mapping, reproducibility, or gate authority.

### Evidence retained without promotion

| Evidence | Audit result | Disposition |
|---|---|---|
| G4F analytical artifact, SHA-256 `93041fc517029fabdc18e02e48794d1b6e02b4a9d7e2d16b5b637129dd4ae584` | 3 homogeneous mathematical cases; `status=candidate`, `evidence_approval=Candidate`, `comparison_status=Deferred`, `tolerance_status=Deferred`, `golden_status=NoGolden` | Candidate mathematical self-consistency only |
| G4D independent reproduction, SHA-256 `ab456ca38fbd434d2c42af2ffb2f115af964273115e04c09f14945b74e2f5be3` | 57 records; every record remains `Deferred` / `Candidate` | Candidate/deferred evidence only |
| R5 candidate snapshots, SHA-256 `13481d139c2b2f2fd1c043725c0a2ced9f396c2813a954cc604a1266bc4c8211` | 57 candidate records; 3 exact synthetic snapshots, 1 refuelled `Nonconverged`, 2 RRS/poison `NotCovered` | Candidate diagnostic evidence only |
| R4 reduced interpolation pack, SHA-256 `0de429b27f78b5e2c5fe724916b14ac8ce45ba05560ab73f3fa4805befd11a0e` | `evidence_class=synthetic`, `approval_status=candidate`, `runtime_use=candidate_interpolation_input` | Synthetic candidate input only |
| `data/golden` | No payload files; `.gitkeep` only | No approved golden authority |
| P1-T08 digest rows | Context/methodology/candidate support with unresolved case-admission gaps | Not an executable oracle or approval |

The preserved G4F observations (`|Delta k| = 5.80036019215413E-13`, maximum
flux difference `7.9432987964978E-15`, and maximum equation residual
`8.673617379884035E-19`) are not a threshold source. No raw difference was
converted into an absolute or relative tolerance.

### Explicit disposition

- Admissible numeric/reference authority: **none found**.
- Production G4 approval: **explicitly declined** for this round.
- G4-R3 and G4 remain `BLOCKED`; the historical gate reports remain intact.
- G4F/G4D/R5 remain Candidate/Deferred/NoGolden as previously recorded.
- The refuelled case remains `Nonconverged`; RRS and poison remain
  `NotCovered` and deferred to their named owner scopes.
- `P5-T10` was not started and `G5` remains dependent on a future G4
  production disposition.

## Assumptions and design choices

- The owner-authorized G4H task explicitly permits declining production G4
  approval when no admissible authority exists; that decision does not create
  a new tolerance, golden value, or runtime approval.
- The authority conclusion is limited to the frozen repository evidence and
  manifests audited here. It does not claim that an external authority can
  never be supplied in a later, separately authorized task.
- The existing G4-R3 blocker is preserved rather than resolved by assumption,
  because changing the decision would require inventing a threshold, replacing
  golden data, loosening convergence, or accepting an incomplete authority
  record.
- No new task ID is invented. The next eligible work is none until an owner
  supplies an admissible authority or authorizes a new scope decision;
  `P5-T10`/`G5` remain blocked in the meantime.

## Validation commands and results

### Frozen-input hash, status, coverage, and golden-payload audit (T0/T1)

```powershell
$ErrorActionPreference='Stop'
$g4f='data/comparisons/p4-t06-g4f-analytical-reference-v1.json'
$g4fManifest='data/comparisons/p4-t06-g4f-analytical-reference-v1.manifest.json'
$g4d='data/comparisons/p4-t06-g4d-independent-reproduction-v1.json'
$r5='data/comparisons/p4-t06-r5-candidate-snapshots-v1.json'
$pack='data/packs/p4-t06-r4-reduced-candidate-v1.json'
$packManifest='data/packs/p4-t06-r4-reduced-candidate-v1.manifest.json'
$f=Get-Content -Raw $g4f | ConvertFrom-Json
$fm=Get-Content -Raw $g4fManifest | ConvertFrom-Json
$d=Get-Content -Raw $g4d | ConvertFrom-Json
$s=Get-Content -Raw $r5 | ConvertFrom-Json
$p=Get-Content -Raw $pack | ConvertFrom-Json
$pm=Get-Content -Raw $packManifest | ConvertFrom-Json
$g4fSha=(Get-FileHash -Algorithm SHA256 -LiteralPath $g4f).Hash.ToLowerInvariant()
$g4dSha=(Get-FileHash -Algorithm SHA256 -LiteralPath $g4d).Hash.ToLowerInvariant()
$r5Sha=(Get-FileHash -Algorithm SHA256 -LiteralPath $r5).Hash.ToLowerInvariant()
$packSha=(Get-FileHash -Algorithm SHA256 -LiteralPath $pack).Hash.ToLowerInvariant()
if($g4fSha -ne $fm.artifact_sha256 -or $g4fSha -ne '93041fc517029fabdc18e02e48794d1b6e02b4a9d7e2d16b5b637129dd4ae584'){throw 'G4F hash/manifest mismatch'}
if($g4dSha -ne 'ab456ca38fbd434d2c42af2ffb2f115af964273115e04c09f14945b74e2f5be3'){throw 'G4D hash mismatch'}
if($r5Sha -ne '13481d139c2b2f2fd1c043725c0a2ced9f396c2813a954cc604a1266bc4c8211'){throw 'R5 hash mismatch'}
if($packSha -ne '0de429b27f78b5e2c5fe724916b14ac8ce45ba05560ab73f3fa4805befd11a0e'){throw 'pack hash mismatch'}
if($f.status -ne 'candidate' -or $f.evidence_approval -ne 'Candidate' -or $f.comparison_status -ne 'Deferred' -or $f.tolerance_status -ne 'Deferred' -or $f.golden_status -ne 'NoGolden'){throw 'G4F status promoted'}
if(@($f.cases).Count -ne 3 -or @($d.records).Count -ne 57 -or @($d.records | Where-Object {$_.status -ne 'Deferred' -or $_.evidence_approval -ne 'Candidate'}).Count -ne 0){throw 'G4D candidate scope changed'}
if($s.status -ne 'candidate' -or $s.evidence_approval -ne 'Candidate' -or @($s.records).Count -ne 57){throw 'R5 status changed'}
if($p.approval_status -ne 'candidate' -or $p.evidence_class -ne 'synthetic' -or $p.runtime_use -ne 'candidate_interpolation_input' -or $pm.approval_status -ne 'candidate'){throw 'pack status changed'}
if(@(Get-ChildItem -LiteralPath data/golden -File -Recurse | Where-Object {$_.Name -ne '.gitkeep'}).Count -ne 0){throw 'golden payload exists'}
Write-Output ('P4_T06_G4H_INPUT_AUDIT_PASS g4f_sha256=' + $g4fSha + ' g4d_sha256=' + $g4dSha + ' r5_sha256=' + $r5Sha + ' pack_sha256=' + $packSha + ' g4f_cases=' + @($f.cases).Count + ' g4d_records=' + @($d.records).Count + ' r5_records=' + @($s.records).Count + ' golden_payload_files=0')
```

Result: exit 0; G4F/G4D/R5/R4 hashes matched their recorded identities,
candidate/deferred statuses were unchanged, the three-case/57-record scopes
were unchanged, and `golden_payload_files=0`.

### Routing and exact-authority-field audit (T0/T1)

```powershell
$ErrorActionPreference='Stop'
$scope=Get-Content -Raw docs/PROJECT_SCOPE.md
$round=Get-Content -Raw docs/tasks/ROUND-2026-08-16-G4-BLOCK-RESOLUTION-CHAIN.md
$report=Get-Content -Raw docs/tasks/P4-T06-G4H.md
foreach($marker in @('P4-T06-G4H','explicitly declined','G4-R3','BLOCKED','P5-T10','G5','Candidate/Deferred/NoGolden','S1-R02','S6-R08')){if(-not ($report.Contains($marker) -or $scope.Contains($marker) -or $round.Contains($marker))){throw "missing routing/authority marker: $marker"}}
foreach($field in @('model, solver/tool/build/version','nuclear-data or material identity','geometry, topology, mesh','state, initial conditions, depletion/refuelling history','authoritative units','normalization','observable identity','independent reproduction','rights')){if(-not $report.Contains($field)){throw "missing authority field: $field"}}
if($scope -notmatch 'P4-T06-G4H.*COMPLETE' -or $scope -notmatch 'production G4 approval.*declined' -or $scope -notmatch 'P5-T10.*blocked'){throw 'scope frontier mismatch'}
if($round -notmatch 'P4-T06-G4H.*COMPLETE' -or $round -notmatch 'no admissible authority' -or $round -notmatch 'G4 remains `BLOCKED`'){throw 'round handoff mismatch'}
Write-Output 'P4_T06_G4H_ROUTING_AUTHORITY_AUDIT_PASS'
```

Result: exit 0; current routing, explicit decision, required authority fields,
literature-row coverage, and deferred downstream markers were present.

### Review (code review high)

```text
Bounded independent review of P4-T06-G4H evidence and final disposition,
reusing reviewer context 01a00dee-bfe2-7140-9e64-8d5b1749de48
```

Result: PASS; no High, Medium, or Low findings. The review confirmed that no
admissible authority exists, no threshold or status was promoted, refuelled/
RRS/poison coverage remains deferred, hashes match, and T0/T1-only validation
is proportionate because no numerical/reference evidence was admitted. Actual
review model, effort, and receipt telemetry are `UNVERIFIED`.

T3 was not triggered: this task admitted no numerical/reference evidence and
changed no runtime, equation, numerical, schema, tolerance, or golden-data
artifact. T6 was not applicable: no external reference baseline was regenerated.

## Token and cost accounting

| Field | Value | Source/notes |
|---|---:|---|
| Input tokens | Unavailable | request telemetry unavailable |
| Cached input tokens | Unavailable | request telemetry unavailable |
| Cache-write input tokens | Unavailable | request telemetry unavailable |
| Output tokens | Unavailable | request telemetry unavailable |
| Reasoning output tokens | Unavailable | request telemetry unavailable |
| Total tokens | Unavailable | request telemetry unavailable |
| Estimated cost | Unavailable | no verified price source/formula available |
| Goal-service total | 306,623 tokens at final goal completion | `get_goal`; not added to request totals |

Cost formula/basis: unavailable; no request-level billing receipt or verified
price source was exposed.

## Numerical differences

Not applicable; no numerical behavior or numerical/reference artifact changed.
The previously recorded G4F diagnostic differences remain observations only and
were not converted into tolerances.

## Deferred validation

- New external or otherwise admissible numeric authority - deferred until a
  separately authorized task supplies the complete authority record.
- G4 production gate rerun - deferred because production approval was
  explicitly declined and the evidence basis remains insufficient.
- `P5-T10`/`G5` - blocked on a future G4 production disposition and approved
  sequence evidence; P5-T10 was not started.
- Refuelled/RRS/poison comparisons - remain deferred to their named owner
  scopes; no maps, overlays, convergence changes, or replacement evidence were
  synthesized.

## Blockers, risks, and follow-up

- Blockers: no admissible numeric baseline/tolerance authority or complete
  representative production coverage exists; G4 remains `BLOCKED`.
- Risk triggers: gate/reference-authority review was required; the bounded
  review ran and returned PASS with telemetry `UNVERIFIED`. The no-admission
  disposition did not trigger T3 or T6.
- Risks accepted or deferred: G4F/G4D/R5 remain candidate/deferred evidence;
  no approved golden payload exists; the refuelled case is nonconverged and RRS/
  poison are not covered.
- Follow-up work: none is eligible until an owner supplies an admissible
  authority or authorizes a new scope decision. Do not restart G4C/G4D/G4E/G4F
  or start `P5-T10` under this report.

## Next eligible task

None until a new owner-authorized task supplies an admissible numeric/reference
authority or changes the scope decision. `P5-T10` and `G5` remain blocked.

Source: [`AGENTS.md`](../../AGENTS.md),
[`docs/Implementation_plan.md`](../Implementation_plan.md),
[`docs/PROJECT_SCOPE.md`](../PROJECT_SCOPE.md),
[`docs/NEXT_GOAL_PROMPT.md`](../NEXT_GOAL_PROMPT.md), and the frozen
P2/P1/G4 prerequisite records named above.
