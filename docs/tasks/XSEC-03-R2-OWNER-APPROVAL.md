# XSEC-03-R2 owner approval - SPH-preserving DRAGON semantic export

**Recorded:** 2026-08-28  
**Decision:** APPROVED / BOUNDED

## Owner direction

The repository owner explicitly selected XSEC-03-R2 after authorizing the
reviewed mapping v2/deck v3 reconciliation. This record authorizes the
previously deferred fresh DRAGON5 execution, external-only semantic decoding,
determinism comparison, and T6 evidence for candidate
xsec-c6-37-nu-jeff31-4ev-v3-sph-preserved-2k.

The task must retain the official source SPH transitions and assertions exactly
as mapping v2/deck v3 require. It may determine only whether the two selected
external MACROLIB records meet that already-approved candidate mapping.

## Bounded authority

The task may use the owner-authorized external JEFF/WLUP fixture, pinned
offline DRAGON5 container, official source procedures, external temporary
directories, and an offline read-only validator. It may create path-free task
evidence, a run/semantic manifest, report, and scope routing. It may install or
use an exact pinned build/test SDK outside the repository when necessary for
validation.

## Non-authorization

This decision does not authorize a change to source procedures, SPH behavior,
assertions, equations, units, field mapping, group structure, convergence
controls, tolerance, schema, Core/CLI/Unity code, or runtime behavior. It does
not admit a runtime pack, a full-core/DONJON case, golden/reference data,
distribution, release, or Unity-ready status. Raw data, decks, captures,
listings, executables, and decoded cross-section values remain external-only.

Any failed source assertion, capture mismatch, selector ambiguity, forbidden
direct correction subtree, field/unit mismatch, nonfinite value,
nonconvergence, nondeterminism, or licensing discrepancy is a fail-closed
BLOCKED outcome.

