# G2-R2 - Rerun G2 after all corrective closures

## Outcome

Status: INCOMPLETE

The root reran G2 after G2-C01 through G2-C06 were complete, reviewed, and
audited. Root evidence passes the cross-spec admission check, T3 headless
regression, and the required Unity T4 evidence, but the independent Sol-high
review found a remaining Medium gap in the C06 partition-mapping assertion.
This rerun is therefore INCOMPLETE/FAIL at gate level; G2-C07 is the next
bounded corrective task.

No Phase 3 implementation, reference baseline, tolerance value, or runtime
behavior was added.

## Files created or changed

- `docs/gates/G2-R2.md` - this rerun’s gate decision and evidence package.
- `docs/tasks/G2-R2.md` - this task report.

All specifications, source, Unity files, fixtures, corrective reports, and
historical gate reports were inspected but not changed. External build/test/
Unity artifacts remained outside the repository.

## Assumptions and design choices

- `G2-R2` is the next rerun identifier after the historical root-only `G2-R1`.
- The six corrective reports are the authoritative closure records; G2-R2
  does not duplicate or rewrite their implementation scope.
- Existing approved external Unity evidence is admissible because G2-C01
  supplied the real harness and G2-C04 revalidated the final cross-project
  serialization candidate with the pinned editor.
- G2 approval is design/gate approval only. It does not approve numerical
  thresholds, reference values, production solver code, or later Phase 3 work.

## Validation commands and results

### Required reading and pre-rerun scope freeze - T0

```text
Get-Content -Raw AGENTS.md
Get-Content -Raw docs/Implementation_plan.md
Get-Content -Raw docs/gates/G1.md
Get-Content -Raw docs/gates/G2.md
Get-Content -Raw docs/gates/G2-R1.md
Get-Content -Raw docs/tasks/P2-T01.md; Get-Content -Raw docs/tasks/P2-T02.md
Get-Content -Raw docs/tasks/P2-T03.md; Get-Content -Raw docs/tasks/P2-T04.md
Get-Content -Raw docs/tasks/P2-T05.md
Get-Content -Raw docs/tasks/G2-C01.md; Get-Content -Raw docs/tasks/G2-C02.md
Get-Content -Raw docs/tasks/G2-C03.md; Get-Content -Raw docs/tasks/G2-C04.md
Get-Content -Raw docs/tasks/G2-C05.md; Get-Content -Raw docs/tasks/G2-C06.md
git status --short --untracked-files=all
```

Result: PASS. The pre-rerun snapshot was
`C:\Users\infin\AppData\Local\Temp\candu-g2-r2-pre-20260809-2100\git-visible-manifest.tsv`:
729 paths, aggregate SHA-256
`6cbf62a807c624bcbded89f5e776487c2de917488ee388c82d362d60f6ace32f`.
Allowed output files were exactly this task report and
`docs/gates/G2-R2.md`.

### Cross-spec admission check - T0/T1

The exact inline Python command and output are recorded in
`docs/gates/G2-R2.md` under “Cross-spec readiness and corrective admission”.

Result: exit code 0; `G2_R2_T0_PASS prerequisites=19 corrective_reports=6
tolerance_authority=PASS t4_evidence=PASS no_new_runtime=PASS`.

### Headless regression suite - T3

```powershell
$artifacts='C:\Users\infin\AppData\Local\Temp\candu-g2-r2-t3-20260809-2110'; New-Item -ItemType Directory -Force -Path $artifacts | Out-Null; & .\tools\Test-FullHeadlessSuite.ps1 -ConfirmFullSuite -ArtifactsPath $artifacts; $exit=$LASTEXITCODE; "G2_R2_T3_EXIT=$exit artifacts=$artifacts"; if($exit -ne 0){exit $exit}
```

Result: exit code 0; Core `2/2` passed, failed `0`, skipped `0`; Golden `0`
discoverable. External TRX root:
`C:\Users\infin\AppData\Local\Temp\candu-full-headless-trx-fa9b32ef73c64c7793a23590cf9afb3c`.

### Unity T4 evidence audit

The final external G2-C04 evidence was independently re-read from
`C:\Users\infin\AppData\Local\Temp\candu-g2-c04-t4-final-20260809-1630`.
EditMode, PlayMode, and StandaloneWindows64 XML each report `Passed`,
`total=1`, `passed=1`, `failed=0`, `skipped=0`; the desktop log contains
`Build Finished, Result: Success` and `Platform: WindowsPlayer`. XML and log
SHA-256 values are recorded in the gate report. G2-C01’s real Unity harness and
the pinned editor version `6000.3.21f1 (c02631ffc030)` were also confirmed.

### Repository format check - T0

```powershell
& .\tools\Check-Format.ps1
```

Result: exit code 0; PASS.

### Independent Sol review

Fresh empty-context `gpt-5.6-sol` reviewer Darwin
(`019fe7f4-ab48-7f52-bd5c-b1005c6e5664`) at high reasoning was read-only. Final
disposition: `FAIL`, with one Medium finding that the C06 command does not
assert exact per-ValueKind/PayloadSchemaId membership, plus one Low finding that
this report’s earlier COMPLETE status was premature. The report was corrected
to `INCOMPLETE` and the gate report to `FAIL`.

## Numerical differences

Not applicable; no numerical behavior, equation, convergence rule, tolerance,
reference baseline, golden value, or runtime source changed.

## Deferred validation

- T6 reference rerun - not triggered; no raw input, reference output, or
  approved baseline changed.
- T5 mobile validation - not implied by G2 design approval.
- Golden consumer tests - deferred because the current assembly contains zero
  discoverable Golden tests; later gate/task work must supply approved evidence.
- Phase 3 implementation - deferred until the next task queue is explicitly
  prepared and approved after G2.

## Blockers, risks, and follow-up

- Blockers: G2-C06’s exact partition mapping remains incomplete; G2-R2 is
  recorded as `INCOMPLETE` and `FAIL`.
- Risk triggers: cross-spec serialization/versioning, deterministic RRS
  ordering, I/Xe atomicity, tolerance authority, evidence reproducibility,
  and Unity adapter/player validation triggered T3/T4 and Sol review.
- Risks accepted or deferred: no tolerance was loosened, no reference/golden
  value was invented, and pre-existing Unity cache paths were preserved rather
  than destructively removed.
- Follow-up: execute G2-C07, complete its final review and scope audit, then
  rerun G2 as a new candidate; do not start Phase 3 here.

## Next eligible task

`G2-C07` - close the exact ValueKind/PayloadSchemaId partition mapping gap,
then rerun G2. No later task is started by G2-R2.

## Final mechanical scope audit

The final failed-rerun audit compared every Git-visible path, byte length, and
SHA-256 against the frozen pre-rerun manifest:

`C:\Users\infin\AppData\Local\Temp\candu-g2-r2-pre-20260809-2100\git-visible-manifest.tsv`

The external final manifest is
`C:\Users\infin\AppData\Local\Temp\candu-g2-r2-final-audit-20260809-2140\git-visible-manifest.tsv`.
The result is `pre_count=729`, `after_count=731`, with the two allowed report
paths added and then disposition-corrected, zero removals, zero unrelated
changes, and zero new private/raw/cache/generated artifact paths. The full
scan retains 521 pre-existing cache/generated/binary-pattern paths and two
pre-existing synthetic listing fixtures; neither set was added by G2-R2. The
report intentionally omits its own aggregate manifest hash; the external
manifest contains the exact final per-path lengths and SHA-256 values.

Source: [`docs/Implementation_plan.md`](../Implementation_plan.md),
[`docs/gates/G2-R2.md`](../gates/G2-R2.md), and [`docs/gates/G2.md`](../gates/G2.md).
