# G6 - Phase 6 regulating-system review

## Result

`CONDITIONAL PASS` for the approved synthetic/test-only Phase 6 Core and
ReducedModel contract scope.

P6-T01 through P6-T07 are complete within their owner-approved synthetic
boundaries. The aggregate Phase 6 focused suite and final T3 regression pass,
and the scenario package is deterministic, manifest-bound, and explicit about
queue, refuelling, safety, and production boundaries. An applicable approved
P6 RRS comparison package is not present: the earlier G4-R6 approved
ReducedModel payload is a different authority and explicitly marks
liquid-zone/adjuster runtime as `NotCovered`. No P6 fixture is promoted to
production, external-reference, or golden authority. The missing applicable
comparison remains the named condition for an unconditional RRS comparison
disposition.

## Scope and authority reviewed

- Tasks/reports since the previous gate: `P6-T01` through `P6-T07`, including
  the final same-context independent PASS recorded by `P6-T07`.
- Approved specifications and ADRs: `docs/Implementation_plan.md` Phase 6
  and G6; `docs/tasks/ROUND-2026-08-17-P6-RRS-CHAIN.md`; frozen
  `docs/spec/kinetics-xenon-rrs-feedback-v1.md`; P2-T05 observable/validation
  contracts; and the applicable decision-log entries in the implementation
  plan.
- Current-scope lookup: exact `PHASE-6` / `G6` entries in
  `docs/PROJECT_SCOPE.md`.
- Candidate artifacts inspected: the P6-T01 through P6-T05 Core contracts and
  synthetic packages, P6-T06 shared queue contracts, and
  `data/scenarios/p6-t07-synthetic-rrs-scenarios-v1.json` with its manifest;
  no commit or external artifact was used.
- Required validation: aggregate Phase 6 focused T1 suite, full headless T3
  Core and Golden regression, package/boundary authority audit, and the
  independent code review (high) required by the G6 chain row.
- Gate-overlap status: G5 remains `PASS` only for the approved ReducedModel /
  engine-neutral Core scope. G4-R6's approved ReducedModel artifact was
  inspected as a possible comparison authority and rejected as non-equivalent
  for this P6 package; it does not cover liquid-zone/adjuster runtime. The
  conditional result therefore preserves the deferred comparison boundary.

Applicable P1-T08 digest rows are the union used by the Phase 6 task reports:
`S1-R04`, `S1-R08`, `S1-R10`, `S2-R05`, `S3-R05`, `S4-R05`, `S6-R02`,
`S6-R03`, `S6-R04`, `S6-R05`, `S6-R06`, and `S6-R08`. They provide device-role,
regulating-system, adjuster, candidate-design, methodology, or limitation
context only. None is a runtime equation, map, controller constant,
comparison authority, or golden-data authority for this gate.

## Evidence and commands

### Aggregate Phase 6 focused suite (T1)

```text
$sdkPath='C:\Users\infin\AppData\Local\Temp\candu-sdk-10.0.302'; & (Join-Path $sdkPath 'dotnet.exe') test 'C:\Users\infin\candu\tests\ReactorSim.Core.Tests\ReactorSim.Core.Tests.csproj' --configuration Release --nologo --no-restore --filter 'FullyQualifiedName~P6T' --logger 'console;verbosity=minimal'
```

Result: exit code `0`; `44/44` Phase 6 focused Core tests passed, with zero
failures and zero skips. This includes the state/map contracts, controller,
queue transition, scenario replay, timestep partition, saturation, poison
accounting, and refuelling-order evidence.

### Full headless regression (T3)

```text
$sdkPath='C:\Users\infin\AppData\Local\Temp\candu-sdk-10.0.302'; & (Join-Path $sdkPath 'dotnet.exe') test 'C:\Users\infin\candu\ReactorSim.sln' --configuration Release --nologo --no-restore --logger 'console;verbosity=minimal'
```

Result: exit code `0`; Core `154/154` passed and Golden consumers `19/19`
passed, with zero failures and zero skips.

### P6 package, boundary, and comparison-authority audit (T0/T1)

```text
$ErrorActionPreference='Stop'; $scenario=Get-Content -Raw 'data\scenarios\p6-t07-synthetic-rrs-scenarios-v1.json' | ConvertFrom-Json; $manifest=Get-Content -Raw 'data\scenarios\p6-t07-synthetic-rrs-scenarios-v1.manifest.json' | ConvertFrom-Json; $approved=Get-Content -Raw 'data\golden\p4-t06-g4k-independent-authority-v1.json' | ConvertFrom-Json; $bytes=[IO.File]::ReadAllBytes('data\scenarios\p6-t07-synthetic-rrs-scenarios-v1.json'); $hash=(Get-FileHash -LiteralPath 'data\scenarios\p6-t07-synthetic-rrs-scenarios-v1.json' -Algorithm SHA256).Hash.ToLowerInvariant(); if($scenario.status -ne 'SYNTHETIC_TEST_ONLY' -or $manifest.status -ne 'SYNTHETIC_TEST_ONLY' -or $scenario.scenarios.Count -ne 5 -or $manifest.scenario_count -ne 5){throw 'P6-T07 scope/count mismatch'}; if($bytes.Length -ne $manifest.artifact.byte_length -or $hash -ne $manifest.artifact.sha256){throw 'P6-T07 package manifest mismatch'}; if($scenario.boundary.approved_comparisons -ne 'RESERVED_G6' -or $scenario.boundary.production_external_golden_claim -ne 'FORBIDDEN' -or $scenario.boundary.safety_behavior -ne 'FORBIDDEN'){throw 'P6-T07 boundary mismatch'}; if($approved.validation_domain -ne 'ReducedModel' -or $approved.coverage.liquid_zone_adjuster_runtime -ne 'NotCovered' -or $approved.coverage.external_candu_numeric_authority -ne 'Deferred'){throw 'G4-R6 scope mismatch'}; Write-Output ('G6_SCOPE_AUDIT_PASS p6_t07_bytes='+$bytes.Length+' p6_t07_sha256='+$hash+' scenarios='+$scenario.scenarios.Count+' g4r6_domain='+$approved.validation_domain+' g4r6_liquid_zone_adjuster_runtime='+$approved.coverage.liquid_zone_adjuster_runtime+' applicable_p6_comparison=0')
```

Result: exit code `0`; `G6_SCOPE_AUDIT_PASS`, P6-T07 package `4813` bytes,
SHA-256 `ae08d771e9d67454ec1f4131b7f1c2e9aca64bd8a2f21ddf3d1fc0d19dc3f58f`,
five scenarios, and zero applicable P6 comparison artifacts. The audit also
confirmed the G4-R6 authority is `ReducedModel` with
`liquid_zone_adjuster_runtime=NotCovered` and external CANDU authority
`Deferred`.

### T4-T6 applicability

No Core, Unity adapter, serialization contract, mobile toolchain, or external
reference baseline changed in this gate task. T4/T5/T6 are therefore not
applicable to the gate dossier. The prerequisite P6-T01 through P6-T06 reports
record their applicable pinned Unity smoke results; no new Unity behavior is
admitted here.

## Independent review evidence

- Requested lane / reasoning: independent code review (high); requested model
  GPT-5.6 Luna / high reasoning.
- Actual model / reasoning: `UNVERIFIED`; reviewer telemetry is unavailable.
- Receipt or telemetry source: reviewer context
  `01a022b9-0e7a-7092-ab8b-87150c391c05` (Sartre); no verified execution
  receipt exposed.
- Attempts and reviewer continuity: one bounded G6 gate-review attempt; the
  existing reviewer context is reused because the candidate is the completed
  Phase 6 gate dossier and it preserves the preceding P6-T07 review context.
  No fresh review swarm was started.
- Final technical disposition: `CONDITIONAL PASS` limited to the approved
  synthetic/test-only Phase 6 Core/ReducedModel contract scope. No critical,
  high, or low findings remain.
- Evidence verification: `UNVERIFIED`.

## Findings

### Medium: applicable P6 RRS comparison package is absent

- Location: `docs/tasks/ROUND-2026-08-17-P6-RRS-CHAIN.md`, G6 dependency and
  `docs/spec/kinetics-xenon-rrs-feedback-v1.md` reference/golden boundary.
- Evidence and impact: the P6-T07 package explicitly reserves approved
  comparisons for G6, while the only inspected approved ReducedModel payload
  declares `liquid_zone_adjuster_runtime=NotCovered` and uses different
  authority identities. There is no traceable comparison that can validate
  these P6 synthetic maps/controller values against an approved RRS baseline.
- Required action: create a separately owner-authorized, fully manifested RRS
  comparison package with exact model, map, units, normalization, observables,
  tolerances, and provenance, then perform a G6 re-entry review. Until then,
  this gate is closed only as the conditional synthetic/test-only disposition.

### Critical / High / Low findings

None. The independent review confirmed that the focused/T3 results, package
identity, and boundary audit are internally consistent.

## Numerical comparison summary

No approved numerical comparison is applicable to the P6 synthetic fixture, so
no external error or tolerance pass is claimed. The validated synthetic
observables are:

| Observable | Synthetic result | Comparison status |
|---|---:|---|
| Centered controller command | total `0.72`, tilt `0.50`; state `0.50` before and `0.60` after delay | Synthetic evidence only |
| Regional tilt command | total `0.50`, tilt `0.61`; signed local overlay | Synthetic evidence only |
| Zone saturation | final fill `1.0`; `UpperBound` diagnostic | Synthetic evidence only |
| Adjuster motion | insertion `1.0`; withdrawal `0.0` after delayed rate-limited queue motion | Synthetic evidence only |
| Poison recovery | `1.0 kg` after add, `0.9 kg` after withdrawal, `0.18 kg/m^3` recovery concentration | Synthetic evidence only |
| Timestep partition | one-step/two-step canonical evidence bytes equal | Synthetic evidence only |
| Refuelling boundary | frozen same-time rank order validated; no refuelling mutation | Boundary evidence only |

## Boundary review

- Dependency direction: **PASS**. Phase 6 contracts remain in
  `ReactorSim.Core`; the Unity adapter is not changed and no runtime dependency
  on DRAGON5/DONJON5 or OpenMC is introduced.
- Determinism and failure diagnostics: **PASS**. The 44 focused tests and T3
  regression pass; P6-T06 validates queue identity, causal motion, saturation,
  rollback, and replay determinism; P6-T07 validates package-bound replay and
  timestep equality.
- Scope exclusions: **PASS**. No shutdown, scram, trip, accident progression,
  safety-system, thermal-hydraulic, CFD, operator-training, production CANDU,
  external-reference, or hidden-reactivity/chemistry behavior is admitted.
- Licensing/provenance: **PASS** for the synthetic/test-only scope. P6 inputs
  are project-owned fixtures with manifests; external literature is context
  only; no external artifact is promoted.

## Required corrective tasks and deferred evidence

- Applicable RRS comparison and unconditional gate disposition: deferred to a
  separately owner-authorized G6 re-entry/comparison task; no task ID is
  currently activated, so no comparison authority is invented here.
- Production/external CANDU RRS authority, full-core coverage, and approved
  quantity tolerances: remain deferred and outside this conditional scope.
- Full refuelling-state interaction beyond the frozen same-time event-order
  boundary: deferred to the appropriate future refuelling/RRS integration task.
- Safety-system, shutdown/scram/trip, accident, thermal-hydraulic/CFD, and
  hidden chemistry/reactivity behavior: excluded, not corrective work.

## Risks and next eligible work

- Accepted/deferred risks: synthetic map/controller values and scenario outputs
  prove contract behavior and determinism only; they do not establish a named
  reactor or production controller baseline.
- Dependent work blocked: any unconditional G6 comparison claim, production or
  external RRS claim, and any release claim relying on those authorities.
- Independent overlap-eligible work: none is activated by this gate report;
  future work requires its own frozen scope and authorization.
- Next bounded task: owner-authorized RRS comparison-package admission followed
  by G6 re-entry. Phase 7 or later work requires separate task authorization and
  must not treat this conditional result as production RRS evidence.

## Token and cost accounting

Request-level input, cached input, cache-write input, output, reasoning output,
total tokens, price source/date, cost formula, and goal-service totals were not
exposed. Cost is `Unavailable` and no per-worker or per-reviewer allocation is
inferred.

## Sign-off

- Reviewer/model lane: independent code review (high), requested GPT-5.6 Luna /
  high; actual receipt `UNVERIFIED`.
- Gate owner: `Kevin Ho`.
- Date: `2026-08-21`.
- Final disposition: `CONDITIONAL PASS` limited to the synthetic/test-only
  Phase 6 Core/ReducedModel contract scope. An owner-authorized comparison
  package and G6 re-entry are required before any unconditional comparison,
  production, external-reference, or golden authority claim.

## Owner approval record

Received from Kevin Ho on `2026-08-21`:

```text
APPROVED: G6-PHASE-6-SYNTHETIC-RRS-CONDITIONAL-V1
OWNER: Kevin Ho
SCOPE: Conditional Phase 6 synthetic/test-only engine-neutral Core / ReducedModel evidence.
SIGN: Approved as written.
IDENTITY: G6.md; P6-T07 package SHA-256 ae08d771e9d67454ec1f4131b7f1c2e9aca64bd8a2f21ddf3d1fc0d19dc3f58f.
BOUNDARY: No production CANDU, external-reference, golden-data, safety-system, shutdown, scram, trip, or hidden-reactivity claim. An approved RRS comparison package and G6 re-entry are required for unconditional disposition.
DATE: 2026-08-21
```

Source: [`AGENTS.md`](../../AGENTS.md),
[`docs/Implementation_plan.md`](../Implementation_plan.md),
[`docs/PROJECT_SCOPE.md`](../PROJECT_SCOPE.md), and
[`GATE_REPORT_TEMPLATE.md`](GATE_REPORT_TEMPLATE.md).
