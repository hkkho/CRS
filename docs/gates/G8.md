# G8 - Phase 8 text-driven game vertical-slice review

## Result

`PASS` for the approved synthetic, engine-neutral Phase 8 CLI boundary.

The complete text-driven slice is implemented and exercised across commands,
approved scenarios and playback modes, operating-envelope/loss behavior,
scoring and cause/effect explanations, versioned save/load/replay, four
non-player-facing scripted policies, and 64 fresh long-run soak cycles. The
slice is deterministic, responsive at the approved 100 ms control tick and
10x playback factor, consequential through score/action/loss state, and
recoverable through pause/resume plus atomic save/load and verify-only replay.
This result does not promote synthetic gameplay data to a physical or
production authority and does not imply Unity presentation or mobile/release
readiness.

## Scope and authority reviewed

- Tasks/reports since the previous gate: `P8-T01` through `P8-T06`, including
  each owner approval where present and the current P8-T06 handoff.
- Approved specifications and authorities: `AGENTS.md`, the complete Phase 8
  and G8 scope in `docs/Implementation_plan.md`, the approved P8-T02 scenario
  and difficulty parameter pack, P8-T03 scoring pack, P8-T04 replay archive,
  and P8-T05 scripted-policy pack. P8-T06 is bound to the exact upstream
  hashes recorded in its owner approval and report.
- Current-scope lookup: exact `PHASE-8` / `G8` entry and the Phase 8
  non-physics CLI sequence in `docs/PROJECT_SCOPE.md`.
- Candidate/diffs or commits inspected: Phase 8 implementation history
  `b102c65` (P8-T01), `9dcddce` (P8-T02), `869b04f` (P8-T03), `6998894`
  (P8-T04), `6509293` (P8-T05), and `f46a729` (P8-T06); current HEAD is
  `f46a729`.
- Required validation: T3, focused replay and policy suites, P8-T06 long
  soaks, repeated seed/digest smoke, malformed-command smoke, and scripted
  strategy/loss/recovery checks.
- Gate-overlap status: G6 and G7A remain conditional for their independent
  synthetic/reference boundaries. They do not block this non-physics CLI
  gate. No unresolved finding is being used to select a physics equation,
  unit, tolerance, golden value, safety behavior, or production claim.

## Evidence and commands

### Final diff hygiene (T0)

```text
git diff --check
git status --short
```

Result: exit code `0`; no whitespace errors. The implementation candidate was
`HEAD f46a729`; no implementation files were dirty, and only this gate report
and its `docs/PROJECT_SCOPE.md` handoff were pending for the final gate commit.

### Final Release solution build (T0/T3)

```text
& 'C:\Program Files\dotnet\dotnet.exe' 'C:\Program Files\dotnet\sdk\10.0.303\MSBuild.dll' 'ReactorSim.sln' /t:Build /p:Configuration=Release /p:RestoreIgnoreFailedSources=true /v:minimal
```

Result: exit code `0`; all solution projects built with zero warnings and zero
errors. The installed direct 10.0.303 SDK was used because `global.json`
requests 10.0.302, which is unavailable on this machine.

### Full headless regression (T3)

```text
& 'C:\Program Files\dotnet\dotnet.exe' 'C:\Program Files\dotnet\sdk\10.0.303\vstest.console.dll' 'tests\ReactorSim.Core.Tests\bin\Release\net10.0\ReactorSim.Core.Tests.dll' 'tests\ReactorSim.Cli.Tests\bin\Release\net10.0\ReactorSim.Cli.Tests.dll' 'tests\ReactorSim.Golden.Tests\bin\Release\net10.0\ReactorSim.Golden.Tests.dll' '--logger:console'
```

Result: exit code `0`; Core `208/208`, CLI `38/38`, and Golden `26/26`
passed, with zero failures and zero skips.

### Focused CLI vertical-slice suites (T1)

```text
& 'C:\Program Files\dotnet\dotnet.exe' 'C:\Program Files\dotnet\sdk\10.0.303\vstest.console.dll' 'tests\ReactorSim.Cli.Tests\bin\Release\net10.0\ReactorSim.Cli.Tests.dll' '--Tests:CliApplicationTests' '--logger:console'
& 'C:\Program Files\dotnet\dotnet.exe' 'C:\Program Files\dotnet\sdk\10.0.303\vstest.console.dll' 'tests\ReactorSim.Cli.Tests\bin\Release\net10.0\ReactorSim.Cli.Tests.dll' '--Tests:P8T04ReplayArchiveTests' '--logger:console'
& 'C:\Program Files\dotnet\dotnet.exe' 'C:\Program Files\dotnet\sdk\10.0.303\vstest.console.dll' 'tests\ReactorSim.Cli.Tests\bin\Release\net10.0\ReactorSim.Cli.Tests.dll' '--Tests:P8T05BaselinePolicyTests' '--logger:console'
& 'C:\Program Files\dotnet\dotnet.exe' 'C:\Program Files\dotnet\sdk\10.0.303\vstest.console.dll' 'tests\ReactorSim.Cli.Tests\bin\Release\net10.0\ReactorSim.Cli.Tests.dll' '--Tests:P8T06SoakTests' '--logger:console'
```

Result: exit code `0` for each suite; `CliApplicationTests` `8/8`,
`P8T04ReplayArchiveTests` `7/7`, `P8T05BaselinePolicyTests` `6/6`, and
`P8T06SoakTests` `2/2`, all with zero failures and zero skips. Together these
cover command parsing and pacing, malformed input, boundary loss, scoring,
replay canonicality/atomicity/tamper rejection, all four scripted policies,
and two complete repeatable soak plans plus fresh-cycle coverage.

### Replay, loss/recovery, and malformed-command CLI smoke (T1)

```text
$dll = (Resolve-Path 'src/ReactorSim.Cli/bin/Release/net10.0/ReactorSim.Cli.dll').Path
$path = Join-Path ([System.IO.Path]::GetTempPath()) ('reactorsim-g8-smoke-' + [Guid]::NewGuid().ToString('N') + '.json')
$commands = "new run tutorial-equilibrium play-accelerated-10x`nset power target 0.95`nadvance wall 50`nset tilt target 0.1`nadvance wall 50`ninspect score`nsave $path`nreplay $path`nload $path`ninspect core`nquit`n"
$output = $commands | & 'C:\Program Files\dotnet\dotnet.exe' $dll
$saveExit = $LASTEXITCODE
$lossCommands = "new run operating-envelope-boundary play-accelerated-10x`nadvance wall 1000`ninspect score`nquit`n"
$lossOutput = $lossCommands | & 'C:\Program Files\dotnet\dotnet.exe' $dll
$lossExit = $LASTEXITCODE
$badCommands = "new run tutorial-equilibrium`nnot-a-command`nquit`n"
$badOutput = $badCommands | & 'C:\Program Files\dotnet\dotnet.exe' $dll 2>&1
$badExit = $LASTEXITCODE
```

Result: exit code `0` for the replay/save/load smoke, boundary-loss smoke, and
the validating wrapper. The replay path reported `save: written`,
`replay: verified`, and `load: restored` with the same final digest
`c73edda37e51b01db91fab9fd4ccdfc11ec604017a83c18a1bdc45bc8387a8df`; the
boundary path resolved at simulation `10` s / wall `1` s with
`RecordLoss`, `power_above_maximum`, and the explicit `no safety response`
effect; malformed input returned exit code `2` with `CLI.Command.Unknown` on
the diagnostic stream. Temporary replay files were removed after each smoke.

### Repeated seed and digest reproducibility smoke (T1)

Two fresh CLI processes ran the identical centered-power command stream and
saved to independent temporary archive paths. Both exited `0` and reported:

| Observable | First process | Second process | Difference |
|---|---:|---:|---:|
| `replay_seed` | `16939011776919352178` | `16939011776919352178` | exact `0` |
| `final_state_digest` | `87ace7e76ff9ff6d540bddf6e0f6666b77620de7d1e2d7bd10902fb99b7a5e49` | `87ace7e76ff9ff6d540bddf6e0f6666b77620de7d1e2d7bd10902fb99b7a5e49` | exact equality |

### Long-run soak evidence (T1/T3)

The P8-T06 report and focused suite verify all four approved P8-T05 policy
strategies over 16 fresh cycles each: `boundary-observer-challenge`,
`centered-power-standard`, `hold-equilibrium-practice`, and
`regional-tilt-standard`. The monitor checks finite/monotone clocks, exact
100 ms control-tick and state-segment progress, action/event/loss ordering,
pending capacity, approved operating-envelope state, score bounds/quality,
turn-summary identity, terminal loss identity, and exact P8-T05 expected
observables. Two complete plan executions have equal aggregate digests and
equal full per-cycle fingerprints.

## Independent review evidence

- Requested lane / reasoning: independent code review (high), requested
  GPT-5.6 Luna / high reasoning.
- Actual model / reasoning: `UNVERIFIED`; reviewer telemetry is unavailable.
- Receipt or telemetry source: Laplace reviewer
  `01a03986-5000-70f2-a925-1bee4f197ac6`, reused from the P8-T06 technical
  review; final G8 review submission
  `01a03a20-9add-79d1-bcbc-faeebb32607c`.
- Attempts and reviewer continuity: one bounded G8 gate-review attempt; the
  same reviewer/context was used for the final disposition, with no fresh
  reviewer swarm.
- Final technical disposition: `PASS` for the bounded synthetic CLI scope;
  no residual P0, P1, or P2 findings.
- Evidence verification: `UNVERIFIED`.

## Findings

None. The current evidence package has no residual P0, P1, or P2 finding within
the approved synthetic CLI boundary. The gate does not claim external,
production, or physical validation.

## Numerical comparison summary

Not applicable. G8 changes and reviews only synthetic gameplay contracts and
deterministic command/replay observables; it selects no physics equation,
constant, unit, normalization, convergence rule, tolerance, reference case,
or golden value. The exact seed/digest and soak comparisons above are
determinism checks, not reactor numerical-reference comparisons.

## Boundary review

- Dependency direction: **PASS**. The Phase 8 runtime remains an engine-neutral
  Core consumer exercised by the CLI; no Unity or reference-tool dependency
  was introduced.
- Determinism and failure diagnostics: **PASS**. Explicit wall-time input,
  stable 100 ms control ticks, deterministic seeds, canonical replay digests,
  exact two-pass soak equality, malformed-command exit `2`, and fail-closed
  archive/pack validation are evidenced above.
- Choices understandable/consequential/recoverable: **PASS**. Help and
  inspection expose scenario, pacing, score, action timing, turn cause/effect,
  and loss reason; target actions change the scored proxy state and may cross
  the loss envelope; pause/resume, atomic load, and verify-only replay provide
  recovery and diagnosis without autonomous safety action.
- Scope exclusions: **PASS**. No shutdown/scram, safety-system,
  accident-progression, full thermal-hydraulic, CFD, operator-training,
  OpenMC, production CANDU, or player-facing bot behavior was added or
  admitted.
- Licensing/provenance: **PASS** for the project-authored synthetic boundary.
  The gameplay packs are synthetic and hash-bound; no external source bytes or
  production nuclear data are redistributed or promoted.

## Required corrective tasks and deferred evidence

None for the approved synthetic CLI gate. The G8 status/evidence row in
`docs/PROJECT_SCOPE.md` should be updated as the administrative handoff after
this report is accepted; that bookkeeping step is not a technical finding or
blocker for the reviewed implementation.

T4 Unity presentation, T5 mobile/device performance, T6 external/reference
reproduction, and G9 performance/mobile evidence remain deferred to their
separately bounded Phase 9/10 tasks and gates. Their absence does not block
this text-driven Phase 8 gate because no adapter, mobile, or reference
baseline was changed or claimed here.

## Risks and next eligible work

- Accepted/deferred risks: the full vertical slice remains synthetic gameplay
  evidence, not a physical or production balance claim; Unity presentation and
  device performance remain untested. Owner: subsequent Phase 9/10 tasks and
  G9/G10 gates.
- Dependent work blocked: none within the approved text-driven CLI boundary;
  external/production reactor claims remain blocked by their existing
  authority gap.
- Independent overlap-eligible work: none required to close G8.
- Next bounded task: Phase 9 work package 1, versioned benchmark scenarios and
  desktop/mobile baseline authority, with its own task ID/specification before
  implementation.

## Token and cost accounting

Request-level input, cached input, cache-write input, output, reasoning output,
total tokens, price source/date, and per-task goal allocation were not exposed.
Cost is `Unavailable`; no per-reviewer or per-task allocation is inferred.

## Sign-off

- Reviewer/model lane: independent code review (high), requested GPT-5.6 Luna /
  high; actual receipt `UNVERIFIED`.
- Date: `2026-08-25`.
- Final disposition: `PASS`.

Source: [`AGENTS.md`](../../AGENTS.md),
[`docs/Implementation_plan.md`](../Implementation_plan.md), and
[`docs/PROJECT_SCOPE.md`](../PROJECT_SCOPE.md).
