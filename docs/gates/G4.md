# G4 - Phase 4 solver validation

## Result

BLOCKED

G4 cannot approve spatial tolerances or golden comparisons from the current
evidence. R5 is a deterministic synthetic/reduced-model self-consistency
candidate, not an approved solver baseline: its top-level status is
`candidate`, its evidence approval is `Candidate`, its gate status explicitly
defers G4 profiles, all 57 records are `Deferred`, and no record is approved.
The repository contains no committed golden data and the Golden test assembly
has no source tests to discover. Proceeding to a technical PASS would require
inventing or relabeling a reference value, tolerance, or case-admission proof,
which is a critical stop under `AGENTS.md`.

## Scope and authority reviewed

- Tasks/reports since the previous gate: `P4-T01`, `P4-T02`, `P4-T03`,
  `P4-T04`, `P4-T05`, `P4-T08`, `P4-T06-R2D`, `P4-T06-R3`, `P4-T06-R4`, and
  `P4-T06-R5`.
- Approved specifications and ADRs: `docs/Implementation_plan.md` Phase 4
  and G4; P2-T01 topology/indexing/units; P2-T02 two-group solver,
  normalization, and convergence; P2-T05 observables/validation methodology;
  `docs/spec/reduced-model-interpolation-boundary-v1.md`; and
  `docs/gates/G2-FC-01.md`.
- Current-scope lookup: exact key `G4` in `docs/PROJECT_SCOPE.md`, including
  the R5 handoff and the work-required-to-re-enter-G4 section.
- Candidate inspected: `data/comparisons/p4-t06-r5-candidate-snapshots-v1.json`,
  SHA-256
  `13481d139c2b2f2fd1c043725c0a2ced9f396c2813a954cc604a1266bc4c8211`.
- Required validation: current R5 candidate audit at T1 and the full headless
  Core/regression suite at T3. T6 is not applicable because no external
  reference baseline was rerun or changed by this gate.
- Gate-overlap status: G2 remains exactly `FORCED CLOSED / WAIVED`. The R2D
  synthetic/reduced-model boundary is the accepted upstream assumption, but it
  does not authorize a golden baseline or tolerance. Dependent G4/P5-T10 work
  remains stopped; unrelated already-frozen work may overlap only under the
  existing policy.

## Evidence and commands

### R5 candidate and golden-evidence admission audit (T1)

```text
$ErrorActionPreference='Stop'; $output='data/comparisons/p4-t06-r5-candidate-snapshots-v1.json'; $doc=Get-Content -LiteralPath $output -Raw | ConvertFrom-Json; if($doc.status -ne 'candidate' -or $doc.evidence_class -ne 'synthetic' -or $doc.evidence_approval -ne 'Candidate'){throw 'R5 output is not candidate synthetic evidence'}; if($doc.gate_status -notmatch 'deferred G4 profiles'){throw 'R5 gate status is not deferred'}; if(@($doc.records | Where-Object {$_.status -ne 'Deferred' -or $_.evidence_approval -ne 'Candidate'}).Count -ne 0){throw 'R5 contains non-deferred or approved records'}; if(@($doc.cases | Where-Object {$_.status -eq 'CandidateSnapshot'}).Count -ne 3 -or @($doc.cases | Where-Object {$_.status -eq 'Nonconverged'}).Count -ne 1 -or @($doc.cases | Where-Object {$_.status -eq 'NotCovered'}).Count -ne 2){throw 'R5 case disposition counts changed'}; $goldenFiles=@(); if(Test-Path -LiteralPath 'data/golden'){$goldenFiles=@(Get-ChildItem -LiteralPath 'data/golden' -File -Recurse | Where-Object {$_.Name -ne '.gitkeep' -and $_.FullName -notmatch '\\(bin|obj)\\'})}; $goldenTests=@(rg --files tests/ReactorSim.Golden.Tests -g '*.cs' 2>$null); if($goldenFiles.Count -ne 0 -or $goldenTests.Count -ne 0){throw ('Golden consumer evidence unexpectedly present files='+$goldenFiles.Count+' tests='+$goldenTests.Count)}; if(@($doc.records | Where-Object {$_.reference_id -notlike 'p4-t06-r4-synthetic-input-v1'}).Count -ne 0){throw 'R5 reference IDs are not synthetic-only'}; $bytes=[IO.File]::ReadAllBytes((Resolve-Path $output)); $sha=([Convert]::ToHexString([Security.Cryptography.SHA256]::HashData($bytes))).ToLowerInvariant(); if($sha -ne '13481d139c2b2f2fd1c043725c0a2ced9f396c2813a954cc604a1266bc4c8211'){throw ('R5 output hash changed: '+$sha)}; Write-Output ('G4_INPUT_AUDIT_PASS candidate_snapshots=3 nonconverged=1 not_covered=2 deferred_records='+$doc.records.Count+' golden_files='+$goldenFiles.Count+' golden_tests='+$goldenTests.Count+' output_sha256='+$sha)
```

Result: exit 0.

```text
G4_INPUT_AUDIT_PASS candidate_snapshots=3 nonconverged=1 not_covered=2 deferred_records=57 golden_files=0 golden_tests=0 output_sha256=13481d139c2b2f2fd1c043725c0a2ced9f396c2813a954cc604a1266bc4c8211
```

The first inventory probe was overbroad and counted generated `obj` source
files; the final command scopes committed data and source patterns correctly.
It found no golden data or Golden-test source. This was an audit-command scope
correction, not a product finding.

### Full headless Core/regression suite (T3)

```text
$artifacts=Join-Path ([IO.Path]::GetTempPath()) ('candu-g4-t3-'+[Guid]::NewGuid().ToString('N')); $trx=Join-Path ([IO.Path]::GetTempPath()) ('candu-g4-trx-'+[Guid]::NewGuid().ToString('N')); New-Item -ItemType Directory -Path $artifacts -Force | Out-Null; New-Item -ItemType Directory -Path $trx -Force | Out-Null; dotnet test 'C:\Users\infin\candu\ReactorSim.sln' --artifacts-path $artifacts --results-directory $trx --logger 'trx'
```

Result: exit 0 under installed SDK 10.0.303. Core passed 103/103 with 0
failures and 0 skipped tests. The Golden assembly discovered 0 tests and
emitted its existing one-test-platform warning. Artifacts:
`C:\Users\infin\AppData\Local\Temp\candu-g4-t3-ae68945dcd1b4758a13a37c44b2a25db`.
TRX results:
`C:\Users\infin\AppData\Local\Temp\candu-g4-trx-f3d1081d4aa74b00af55ef63d280590e`.

### Routing-record documentation audit (T0)

```text
$ErrorActionPreference='Stop'; $files=@('docs/gates/G4.md','docs/PROJECT_SCOPE.md','docs/tasks/ROUND-2026-08-16-P4-REFERENCE-CHAIN.md'); foreach($file in $files){if(-not (Test-Path -LiteralPath $file -PathType Leaf)){throw "missing $file"}; $trailing=@(Select-String -LiteralPath $file -Pattern '\s+$'); if($trailing.Count -ne 0){throw "trailing whitespace in $file"}}; $g4=Get-Content -LiteralPath 'docs/gates/G4.md' -Raw; $round=Get-Content -LiteralPath 'docs/tasks/ROUND-2026-08-16-P4-REFERENCE-CHAIN.md' -Raw; $scope=Get-Content -LiteralPath 'docs/PROJECT_SCOPE.md' -Raw; foreach($needle in @('BLOCKED','P4-T06-G4A','P4-T06-G4B','fresh `G4`','G4_INPUT_AUDIT_PASS','103/103','UNVERIFIED')){if(-not ($g4.Contains($needle) -or $round.Contains($needle) -or $scope.Contains($needle))){throw "missing evidence marker $needle"}}; if($round -notmatch '\| `P4-T06-G4A` \|' -or $round -notmatch '\| `P4-T06-G4B` \|'){throw 'follow-up task rows missing'}; if($scope -notmatch '### `G4` - spatial validation and tolerance gate' -or $scope -notmatch '### Work required to re-enter `G4`'){throw 'scope G4 sections missing'}; Write-Output ('DOC_SCOPE_AUDIT_PASS files='+$files.Count+' follow_up_tasks=2 gate=BLOCKED reviewer=PASS telemetry=UNVERIFIED')
```

Result: exit 0.

```text
DOC_SCOPE_AUDIT_PASS files=3 follow_up_tasks=2 gate=BLOCKED reviewer=PASS telemetry=UNVERIFIED
```

## Independent review evidence

- Requested lane / reasoning: code review (high)
- Actual model / reasoning: `UNVERIFIED`
- Receipt or telemetry source: same-context reviewer
  `01a0080f-dc22-7811-9b10-2a60a9e299d0`; no receipt telemetry exposed
- Attempts and reviewer continuity: one bounded read-only gate-review attempt;
  same reviewer context reused from R5; no fresh reviewer was spawned
- Final technical disposition: BLOCKED; the reviewer confirmed that this is the
  only defensible technical disposition and that no hidden technical PASS is
  claimed. The reviewer reported no High, Medium, or Low findings.
- Evidence verification: `UNVERIFIED` because no actual model/effort receipt
  telemetry was exposed.

## Findings

### Critical: no approved solver comparison baseline or tolerance evidence

- Location: `data/comparisons/p4-t06-r5-candidate-snapshots-v1.json`; R5
  `reference_id`, `evidence_approval`, and `gate_status` fields.
- Evidence and impact: every R5 reference ID is the committed synthetic input;
  all 57 records are `Candidate`/`Deferred`; the output explicitly says exact
  self-consistency is diagnostic evidence only. P2-T05 requires numeric
  thresholds to remain deferred until G4 receives evidence and approves them.
- Required action: `P4-T06-G4A` must record an owner-approved, path-free
  comparison baseline/case-admission decision that binds the exact model/tool,
  data identity, geometry/topology, units, normalization, observable schema,
  reproducibility, and legal status. It may select neither a new equation nor
  a tolerance and must stop if the proof is unavailable.

### High: required representative coverage is incomplete

- Location: R5 case summary and `docs/tasks/P4-T06-R5.md` numerical summary.
- Evidence and impact: the refuelled perturbation is `Nonconverged` under the
  inherited two-iteration policy, and RRS/poison perturbations are
  `NotCovered`. There is no approved basis to convert these into usable or
  accepted comparisons.
- Required action: `P4-T06-G4A` must either provide approved inputs and
  traceable evidence for the missing cases or record an explicit owner-approved
  scope disposition. It may not synthesize maps, overlays, convergence changes,
  or tolerance values.

### High: no golden consumer tests exist

- Location: `tests/ReactorSim.Golden.Tests` and `data/golden`.
- Evidence and impact: the Golden project has no tracked C# test source and
  `data/golden` contains only `.gitkeep`; T3 reports zero discovered Golden
  tests. A green Core suite cannot establish the G4 golden-comparison gate.
- Required action: after `P4-T06-G4A` admits a valid comparison baseline,
  `P4-T06-G4B` must add bounded golden consumers and their manifest binding,
  without choosing numeric thresholds outside the G4 decision. Run T3 and
  code review (high) for that task, then rerun G4.

## Numerical comparison summary

The three homogeneous R5 cases are exact source-contract/reduced-pack
self-consistency checks with absolute and relative difference `0.0`. They are
not an approved external solver comparison and therefore have no approved
tolerance or PASS disposition. The refuelled case has no usable numerical
state; RRS and poison have no input comparison. No numerical tolerance is
approved by this gate.

## Boundary review

- Dependency direction: pass. R5 remains an offline tool referencing only
  `ReactorSim.Core`; no Unity or reference-tool runtime dependency was added.
- Determinism and failure diagnostics: pass for the current candidate. R5
  regenerated byte-identically, preserved nonconvergence, and the current T3
  suite passed 103/103.
- Scope exclusions: pass. No shutdown/scram, safety behavior, full thermal
  hydraulics, runtime exact reference data, new equation, or public schema was
  introduced.
- Licensing/provenance: pass for the current committed synthetic candidate.
  The user's redistribution confirmation is preserved in R1 and the round
  record, but no external reference payload has been admitted or committed.

## Required corrective tasks and deferred evidence

- `P4-T06-G4A` - resolve G4 evidence admission and case coverage. Produce a
  path-free owner decision and, where authorized, compact comparison inputs
  that bind model/tool/version, data identity, topology/geometry, units,
  normalization, observable mapping, reproducibility, and licensing. Cover the
  required representative cases or explicitly disposition each missing case.
  Do not select or relax tolerances, change equations, or replace the R2D/R3
  boundary. Validate at T0/T1; use T6 only if an external reference baseline is
  actually regenerated; require applicable P1-T08 rows and code review (high).
- `P4-T06-G4B` - add golden consumers for the admitted G4 baseline and bind
  their manifest/profile identities without inventing thresholds. Validate at
  T1/T3 and code review (high), then rerun G4.
- G4 tolerance approval - deferred until both corrective tasks produce
  admissible evidence. No upstream task may approve a numeric threshold.

## Risks and next eligible work

- Accepted/deferred risks: the current candidate is synthetic-only; no
  external CANDU solver baseline is admitted; refuelled/RRS/poison coverage is
  incomplete; and Golden tests are absent. These are visible and not treated
  as PASS evidence.
- Dependent work blocked: `P5-T10`, `G5`, and any reference-dependent Phase 4/5
  gate work remain blocked by this evidence-admission finding. The direct
  Candu6.x2m admission path remains retired by R2D.
- Independent overlap-eligible work: only work whose inputs are already frozen
  and that does not depend on G4's unresolved comparison/tolerance evidence may
  proceed under `AGENTS.md`; no such task is started by this gate report.
- Next bounded tasks: `P4-T06-G4A`, then `P4-T06-G4B`, then a fresh `G4`
  execution. User/owner direction is required if G4A cannot establish an
  approved baseline without changing a frozen contract.

## Token and cost accounting

- Request-level input, cached input, cache-write input, output, reasoning
  output, total, and cost telemetry: `Unavailable`; no allocation or verified
  price source was exposed.
- Goal-service total: record separately when exposed; it is not allocable to
  this gate and is not added to request totals.
- Cost basis: unavailable; no request-level billing receipt or verified price
  source was available.

## Sign-off

- Reviewer/model lane: same-context code review (high), reviewer
  `01a0080f-dc22-7811-9b10-2a60a9e299d0`.
- Date: `2026-08-16`.
- Final disposition: BLOCKED.

Source: [`AGENTS.md`](../../AGENTS.md),
[`docs/Implementation_plan.md`](../Implementation_plan.md), and
[`docs/PROJECT_SCOPE.md`](../PROJECT_SCOPE.md).
