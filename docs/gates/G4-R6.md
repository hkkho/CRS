# G4-R6 - fresh reduced-model G4 disposition

## Result

`PASS` for the selected runtime `ReducedModel` validation domain.

G4-R6 independently audited the P4-T06-G4K package, approved a sibling
`ApprovedGolden` payload and six quantity-specific profiles, activated the
approved Core consumer, and reran the required focused and T3 checks. This
clears G4 for the bounded `RepresentativeReducedModel` scope represented by
the five admitted scenarios. It does not establish direct CANDU, DRAGON5,
DONJON5, production, release, safety, or full-core authority; those boundaries
remain blocked/deferred.

`P5-T10` is eligible to consume this approved ReducedModel authority. `G5`
remains pending until P5-T10 completes its sequence-comparison task and gate.

## Scope and authority reviewed

- Upstream task: [`P4-T06-G4K`](../tasks/P4-T06-G4K.md), technical PASS with
  candidate status preserved.
- Prior blocker: [`G4-R5`](G4-R5.md), `BLOCKED` / `NO-ADMISSION` because G4J
  was implementation-level self-consistency evidence without an admissible
  oracle/tolerance basis.
- Frozen contracts: P2-T01 topology/indexing/units, P2-T02 two-group
  equations/normalization/convergence, P2-T03 refuelling state/event rules,
  P2-T04 feedback branch contracts, P2-T05 observable/profile methodology,
  and the reduced-model interpolation boundary.
- P1-T08 coverage used as context/methodology: `S1-R03`, `S1-R04`, `S1-R05`,
  `S1-R06`, `S1-R07`, `S1-R08`, `S1-R11`, `S2-R05`, `S4-R03`, `S5-R02`,
  `S5-R03`, `S5-R04`, `S5-R05`, `S5-R06`, `S5-R07`, `S5-R09`, `S6-R02`,
  `S6-R03`, `S6-R04`, `S6-R05`, `S6-R08`. No literature number was promoted
  to a runtime or golden value.
- External CANDU/DRAGON5/DONJON5 numeric authority: `NotApplicable` to this
  bounded project-authored ReducedModel approval; no external artifact or
  nuclear-data library was used.

## Approved artifacts and binding

The candidate remains unchanged at:

- Candidate artifact:
  `data/comparisons/p4-t06-g4k-independent-authority-v1.json`
  SHA-256 `a056fb2292450ac251d79716db2091325830d0536fd98d5cbdbe91ae99363fe0`.
- Candidate manifest:
  `data/comparisons/p4-t06-g4k-independent-authority-v1.manifest.json`
  SHA-256 `e6d0ac1ab75329db9caf76961b8ab14050127a54d0d10fcaae84e0add2963067`.

The gate created and approved only the sibling payload:

- Approved artifact:
  `data/golden/p4-t06-g4k-independent-authority-v1.json`
  SHA-256 `d6f3ae2dddf0ffb8791368661c80df097333e736786af2942f4bbe495dfdf227`.
- Approved manifest:
  `data/golden/p4-t06-g4k-independent-authority-v1.manifest.json`
  SHA-256 `7b7e12b20379869e1d4c405b070d0bccf0bfadea62a909c12fed3871f47b4f83`.

The approved manifest binds the candidate artifact, candidate manifest,
definition `bf227e60bd2c9540c699786c530c079bb31caf4108cf461704f37c81f6758b6e`,
source pack `0de429b27f78b5e2c5fe724916b14ac8ce45ba05560ab73f3fa4805befd11a0e`,
independent authority definition
`f0a9e029c6b8b0e030d3470fe1eec952d24397648c2633ffc9ad38a16078cd6d`, and
source snapshot
`3e7a4e47b5bd990c6f8c26d713d01de85ec96c4ae64a2725b36cf840cb6cd2db`.
The approved payload is `approved_golden` / `Approved` /
`ApprovedGolden`, with `coverage_class=RepresentativeReducedModel` and
`validation_domain=ReducedModel`. The original candidate remains
`candidate` / `Candidate` / `Deferred` / `NoGolden`.

## Correction addendum — regenerated exact-profile binding

The artifact and manifest hashes recorded above preserve the original gate
evidence. The final same-context review identified two bounded corrections:
the exact `spatial.convergence` profile must carry absent numeric thresholds,
and the approved scalar `spatial.power` profile must be applied to every node,
not only to the maximum-error diagnostic. The correction made no change to the
approved numeric thresholds, scenarios, or ReducedModel scope.

`Approve.py` now emits `null`/NotApplicable numeric threshold members for the
exact profile, and the approved consumer applies the power profile per node.
The regenerated current bindings are:

- approved artifact SHA-256:
  `897c0b4dc055f59b21b110f4674903c12e795e9cfaaf2131cb3ab46e042ede6a`;
- approved manifest SHA-256:
  `e927a014c15f13341bd4739c59bc8f9c7d8bd4e36a74a0c37dc669ab83098f4b`;

`ProfileDigestAudit.py` passed and the focused consumer passed 3/3. The
superseded hashes remain historical evidence. The same reviewer context
requested this documentation correction; its final disposition is pending the
updated gate/task record.

## Approved tolerance profiles

The gate selected the profiles below. Values are in the authoritative P2-T05
unit. The observed differences are verification evidence only; the profiles
were selected from the frozen P2-T02 policy, the independently measured dense
matrix conditioning/eigenvalue separation, the quantity semantics, and an
explicit cross-method guard band. No threshold was copied from G4J or inferred
as a raw-difference promotion.

| Quantity | Norm/rule | Absolute | Relative | Reference scale | Status |
|---|---|---:|---:|---:|---|
| `spatial.k` | scalar OR | `1e-9` | `1e-8` | `1` | Approved |
| `spatial.flux` | canonical `L_inf` OR | `1e-7 m^-2 s^-1` | `1e-6` | `1` | Approved |
| `spatial.power` | scalar OR | `1e-5 W` | `1e-7` | `1 W` | Approved |
| `spatial.total_power` | scalar OR | `1e-8 W` | `1e-12` | `1000 W` | Approved |
| `spatial.residual_relative_inf` | scalar OR | `1e-9` | `1e-6` | `1` | Approved |
| `spatial.convergence` | exact status/reason/policy | NotApplicable | NotApplicable | NotApplicable | Approved |

The approved profiles carry P2-T05 `ScalarV1`/`VectorV1`/`BytesV1` payload
identities, canonical `SpatialNodeGroup` ordering for flux, `Or`/`Exact`
acceptance, G4 ownership, and recomputed `DataDigest` values. Convergence
iteration counts remain algorithm-specific diagnostics; the approved exact
profile checks convergence status, reason, and frozen policy identity.

## Independent authority audit

The reference expected values were produced by the separate Python/NumPy dense
generalized-eigen path, not by Core/G4J source iteration. The authority covers
five scenarios: `fresh_start`, `equilibrium_like`,
`refuelled_4_bundle_shift`, `rrs_tilt_perturbation`, and
`bulk_poison_perturbation`. It binds 48 nodes, 92 reciprocal edges, 104
boundaries, alternating flow, two explicit S4 shift directions, prescribed RRS
and poison overlays, burnup projection, SI units, target-power normalization,
canonical observables, rights, and the complete state/history/event records.

The independent matrix residual range is
`1.2397838455467547e-15` to `2.4244824322576626e-15`; infinity condition
numbers range from `2.8777862863578236` to `3.539971257405796`; and the
dominant-eigenvalue gap ranges from `0.0025490821331793367` to
`0.008532855632533698`. The source snapshot records
`UNAVAILABLE_NO_GIT_HEAD` for source commit and binds the content-addressed
producer plus NumPy distribution record/build information instead.

## Validation commands and results

### Candidate and approved generation/audit (T0/T1)

```powershell
powershell -NoProfile -ExecutionPolicy Bypass -File .\tools\IndependentReducedAuthority\Test-P4-T06-G4K.ps1
powershell -NoProfile -ExecutionPolicy Bypass -File .\tools\IndependentReducedAuthority\Approve-P4-T06-G4K.ps1
python .\tools\IndependentReducedAuthority\ProfileDigestAudit.py .\data\golden\p4-t06-g4k-independent-authority-v1.json
```

Results: all exit 0. Candidate generation/revalidation, altered source-hash
rejection, six-profile digest audit, approved generation, and approved digest
audit passed. Rerunning approval reproduced the same approved artifact and
manifest hashes.

### Fresh G4-R6 authority audit (T1)

```text
G4_R6_AUTHORITY_AUDIT_PASS candidate_preserved=1 approved_scope=ReducedModel approved_profiles=6 scenarios=5 golden_file_count=4 path_hits=0
```

The audit preserved the candidate boundary, verified both manifest hash chains,
verified all six approved profiles and five scenarios, and found no host paths.
The four golden files include the two pre-existing G4I synthetic files and the
two new G4K ReducedModel files.

### Approved consumer (T1)

```powershell
dotnet test C:\Users\infin\candu\tests\ReactorSim.Golden.Tests\ReactorSim.Golden.Tests.csproj --configuration Release --nologo --no-restore --filter FullyQualifiedName~P4T06G4K --logger "console;verbosity=detailed"
```

Result: exit 0; 3/3 passed, 0 failed, 0 skipped. The approved consumer binds
the approved manifest and profiles and checks all five Core/reference
comparisons against the approved quantity-specific profiles.

### Full T3 regression

```powershell
dotnet test C:\Users\infin\candu\ReactorSim.sln --configuration Release --nologo --no-restore
```

Result: exit 0; Core 103/103 and Golden 17/17 passed, with zero failures and
zero skips. The command ran from `C:\Users\infin` for the repository's pinned
SDK mismatch (`10.0.302` requested; `10.0.303` installed); `global.json` was
not changed.

T6 is `NotApplicable`: no external reference baseline changed. T4/T5 are
`NotApplicable`: no Unity, adapter, mobile, or platform behavior changed.

## Review evidence

- Requested review: independent code review (high) / high effort.
- Final technical disposition: `PASS`, no unresolved High/Medium/Low findings.
- Reviewer: `01a01217-337b-79e1-9b86-88232ccf91d1`.
- Actual model/reasoning receipt: `UNVERIFIED`; telemetry was not exposed.
- This gate records a technical PASS for the bounded ReducedModel evidence and
  does not describe the review as receipt-verified approval.

## Findings, risks, and deferred evidence

- Direct CANDU/DRAGON5/DONJON5 production authority, full 380-channel coverage,
  external nuclear data, I/Xe/kinetics, liquid-zone runtime control, thermal
  behavior, safety behavior, release identity, and mobile evidence remain out
  of scope or deferred.
- No Git HEAD exists in the workspace; the source-commit limitation is explicit
  and content hashes/build provenance bind this bounded project-authored case.
- Review model/effort telemetry remains unavailable and is explicitly
  `UNVERIFIED`; a later release or external-authority claim requires verified
  review/source identity.
- `P5-T10` is the next eligible consumer of the approved ReducedModel
  authority. `G5` remains pending until P5-T10 and its gate complete.

### Review telemetry correction addendum

The original review paragraph above preserves the state observed at the first
handoff. The final same-context review of the corrected records returned
`PASS` with no remaining findings. Its receipt verifies model
`gpt-5.6-sol` and `high` effort for reviewer context
`01a01217-337b-79e1-9b86-88232ccf91d1`, final turn
`01a01254-3951-7d32-8438-16ace632a525`. Cumulative reviewer-session usage was
input `9,999,733`, cached input `9,741,824`, cache-write input `0`, output
`31,441`, reasoning output `14,631`, total `10,031,174`; no per-task allocation
or price/cost basis was exposed.

## Token and cost accounting

Request input, cached input, cache-write input, output, reasoning output, total
tokens, price source/date, cost formula, and goal-service totals were not
exposed. Cost is `Unavailable` and not allocable.

## Sign-off

- Gate owner disposition: `PASS` for `ReducedModel` only.
- Production/external CANDU boundary: preserved as blocked/deferred.
- Next eligible task: `P5-T10`; `G5` remains pending.

Source: [`AGENTS.md`](../../AGENTS.md),
[`docs/Implementation_plan.md`](../Implementation_plan.md),
[`docs/PROJECT_SCOPE.md`](../PROJECT_SCOPE.md),
[`docs/tasks/P4-T06-G4K.md`](../tasks/P4-T06-G4K.md), and
[`docs/gates/GATE_REPORT_TEMPLATE.md`](GATE_REPORT_TEMPLATE.md).
