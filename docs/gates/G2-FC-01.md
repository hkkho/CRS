# G2-FC-01 - Administrative forced closure and overlap authorization

## Result

`FORCED CLOSED / WAIVED`

Date: 2026-08-14
Authority: explicit user direction recorded in `PLAN-C03`.

This addendum closes the current execution status of G2 so the project can
continue. It does not rewrite the historical G2 `FAIL`, G2-R2 `INCOMPLETE`, or
G2-R3 technical `PASS` records. It does not claim that a new code review (high)
was performed. The historical review labels remain historical evidence.

## Scope and effect

- The existing Phase 2 specifications are the implementation inputs:
  topology/indexing/units/boundaries, two-group solver/normalization/convergence,
  refuelling/burnup/interpolation, kinetics/I-Xe/RRS/feedback, and observables/
  validation methodology.
- Phase 3 implementation may continue, including completion of `P3-T03` and
  preparation for G3.
- Phase 4 and later phases may begin when their task inputs are otherwise ready,
  including implementation of the already-written Phase 2 equations and data
  contracts. They must not invent or silently change a physics decision.
- An incomplete non-critical gate may overlap with independent work. The task
  report must name the upstream gate, the overlap assumption, and deferred proof.
- The next active review lane for all former Sol activities is `code review
  (high)`.

## Critical stops that remain active

The waiver does not permit a task to proceed if it would require:

1. inventing or changing an equation, coefficient meaning, unit, sign,
   boundary, convergence rule, normalization, tolerance, golden/reference value,
   or public Core/CLI/Unity contract;
2. hiding nondeterminism, NaN/Inf, nonconvergence, invalid data, failed replay,
   or a failed regression;
3. changing the reference baseline without the dedicated evidence task and T6;
4. crossing unresolved DRAGON5/DONJON5 licensing or nuclear-data authority;
5. adding unapproved native/mobile dependencies, signing/publication behavior,
   or release output; or
6. adding shutdown/scram, safety-system, accident-progression, full
   thermal-hydraulic/CFD, operator-training, OpenMC, or other excluded behavior.

Stop only the dependent task when a critical condition occurs. Continue
independent work that remains within the approved architecture and scope.

## Evidence carried forward

- Historical `G2.md`: technical `FAIL` with six findings before corrective work.
- `G2-R1`: historical `INCOMPLETE`.
- `G2-R2`: historical `INCOMPLETE` after the C06 mapping defect.
- `G2-R3`: technical `PASS` after C07, T3, and T4 evidence, with zero remaining
  root-admitted Critical/High/Medium findings; no Phase 3 implementation was
  started by that rerun.
- Golden consumer tests remain undiscoverable in the recorded G2-R3 evidence.
  Later reference-comparison tasks must supply them before claiming golden
  coverage.

## Required consistency updates

The active rules and scope index are updated together:

- `AGENTS.md` - code-review (high) lane and gate-overlap/critical-blocker policy;
- `docs/Implementation_plan.md` - active review lane, G2 waiver, and overlap
  wording;
- `docs/PROJECT_SCOPE.md` - searchable phase/task/gate map; and
- the private HTML roadmap - matching current disposition and review vocabulary.

## Next eligible task

`P3-T03` - complete explicit simulation clock and command queue contracts, using
the existing Phase 3 specifications and recording the G2 overlap decision.

Source: [`docs/Implementation_plan.md`](../Implementation_plan.md),
[`AGENTS.md`](../../AGENTS.md), and `PLAN-C03`.
