# G2-R1 - Root-only G2 rerun

## Result

`FAIL`

This was an explicitly requested root-only attempt. No subagent or independent
Sol reviewer was used. Existing evidence was revalidated, but the gate cannot
pass while G2-C03 is `BLOCKED` and G2-C04/G2-C06 have no completed corrective
reports.

## Scope reviewed

The rerun reviewed the complete implementation plan, repository instructions,
the historical G2 gate/task reports, P2-T01 through P2-T05 reports, and the
completed G2-C01, G2-C02, G2-C03, and G2-C05 corrective reports. The root also
inspected the current refuelling, lifecycle, observables, and kinetics/RRS
specifications. No historical report was overwritten.

## Evidence and commands

Pre-rerun Git-visible scope was frozen at:

`C:\Users\infin\AppData\Local\Temp\candu-g2-r1-pre-20260809-1015\git-visible-manifest.txt`

It contained `721` paths and aggregate SHA-256
`bcaebf946489c4d0f8012b0ce1a849d76e2b75b44f9963f1688515ab9f99141c`.

| Check | Result |
| --- | --- |
| Root status/contract assertions | PASS for existing contracts; admission FAIL |
| `& .\tools\Check-Format.ps1` | PASS, exit `0` |
| `& .\tools\Test-FullHeadlessSuite.ps1 -ConfirmFullSuite -ArtifactsPath C:\Users\infin\AppData\Local\Temp\candu-g2-r1-t3-20260809-1020` | PASS, Core `2/2`, Golden `0` discoverable, exit `0` |
| Existing Unity EditMode XML | PASS, `1/1/0/0` |
| Existing Unity PlayMode XML | PASS, `1/1/0/0` |
| Existing Unity StandaloneWindows64 XML | PASS, `1/1/0/0` |
| Existing desktop build log | PASS: `Build Finished`, `Result: Success`, `Platform: WindowsPlayer` |

Unity evidence paths were under
`C:\Users\infin\AppData\Local\Temp\candu-g2-c03-t4-20260809-1000`.
The external player was `667136` bytes, SHA-256
`c8b0d73dc40e4f2cddbf656cfb7257fcb8273da22e44e12a8694cd8e275c6fb2`.

## Findings

1. **High - G2-C03 review admission is incomplete.** The candidate's root
   assertions and T1/T3/T4 evidence are positive, but its required independent
   Sol review returned no verdict and its task report remains `BLOCKED`.
   Root-only inspection cannot be recorded as independent review.

2. **High - G2-C04 is not complete.** No corrective report exists for the
   remaining refuelling atomicity issue. The current specification has
   lifecycle counters and rollback language, but no completed corrective task
   and evidence package closes the explicit I/Xe inventory/state inclusion and
   rollback/discharge requirement.

3. **Medium - G2-C06 is not complete.** No corrective report exists to make
   the P2-T05 focused evidence reproducible and independently checkable. The
   original P2-T05 completion report remains valid for P2-T05, but it does not
   close this separate G2 finding.

The previously recorded G2-C01, G2-C02, and G2-C05 findings were rechecked and
remain resolved. No new implementation defect was introduced by this rerun.

## Numerical comparison summary

Not applicable. No numerical baseline, equation, normalization, tolerance, or
reference data changed. The T3 check was a software regression check only.

## Requirements and boundary review

- Dependency direction and Phase 3 exclusion: PASS by inspection; no runtime
  implementation was added.
- Deterministic ordering/lifecycle/schema evidence: candidate assertions and
  available validation PASS, but gate readiness FAIL because C03 review is
  incomplete.
- Failure handling/refuelling atomicity: FAIL at gate level because C04 is
  incomplete.
- P2-T05 focused reproducibility: FAIL at gate level because C06 is incomplete.
- Unity adapter evidence: PASS for the existing external evidence.
- Raw/private DRAGON5/DONJON5 inputs and listings: no new repository artifact
  was found; all generated artifacts stayed external.
- Independent review: NOT RUN by explicit user instruction; this is a blocker,
  not a PASS.

## Required corrective tasks

- Complete G2-C03 with a valid independent Sol review and final disposition.
- Execute the separately authorized G2-C04 correction and its required review.
- Execute G2-C06 focused reproducibility correction and its required review.
- Rerun G2 only after all three corrective task reports and reviews are
  complete.

## Risks accepted or deferred

The gate remains blocked; no risk was accepted as resolved by this root-only
attempt. The missing independent review, I/Xe refuelling atomicity closure, and
P2-T05 evidence reproducibility remain explicit risks. No tolerance was
loosened and no undocumented value was committed.

## Next bounded tasks

The next eligible item is G2-C03 completion after independent review. G2-C04
and G2-C06 remain subsequent bounded corrective tasks; no later task may start
before their prerequisites and reviews are complete.

## Review sign-off

Root agent: `FAIL` disposition for G2-R1. This report records a root-only
attempt requested by the user; it is not an independent Sol sign-off.

## Final scope audit

External final audit:

`C:\Users\infin\AppData\Local\Temp\candu-g2-r1-final-audit-20260809-1030\git-visible-manifest.txt`

The audit compared the final Git-visible scope against the pre-rerun snapshot.
The final manifest contained `723` paths: exactly two additions, the two
allowed reports above, zero unrelated changes, zero removals, and zero new
private/raw or repository-cache paths. The full scan showed `506` pre-existing
Unity `Library` paths, `5` pre-existing `Temp` paths, and `10` pre-existing
non-Library/Temp log artifact-pattern paths; none was added or modified by this
rerun. The final manifest and exact per-path lengths/SHA-256 values are the
mechanical record; the self-including report hash is intentionally not embedded
here.
