# G4-R5 - reduced-model disposition

## Result

BLOCKED

G4-R5 is a separate reduced-model-only disposition for the owner-authorized
`P4-T06-G4J` candidate. The case is deterministic, authority-field complete
for its bounded project-authored scope, and consumed by Core, but it has no
admissible exact oracle, external numerical authority, or approved tolerance
basis. Its `1e-8` comparison bound remains diagnostic-only. The candidate stays
`Candidate` / `Deferred` / `NoGolden`; no G4J golden payload or approved
tolerance profile is admitted. Production G4, `P5-T10`, and `G5` remain
blocked.

## Scope and authority reviewed

- Tasks/reports since the previous gate: `P4-T06-G4J` and this separately
  executed `G4-R5` task.
- Prior gate boundary: `G4-R4` is a conditional pass only for its synthetic
  `Synthetic` case; it does not admit representative reduced-model authority.
- Approved specifications and ADRs: `docs/Implementation_plan.md` Phase 4/G4;
  P2-T01 topology/indexing/units; P2-T02 two-group equations,
  normalization/convergence; P2-T03 burnup/refuelling transitions; P2-T04
  kinetics/I-Xe/RRS/feedback contracts; P2-T05 observable and validation
  methodology; and `docs/spec/reduced-model-interpolation-boundary-v1.md`.
- Current-scope lookup: exact `G4-R5` key in
  `docs/PROJECT_SCOPE.md` and the `G4-R5` row/current handoff in
  `docs/tasks/ROUND-2026-08-16-G4-BLOCK-RESOLUTION-CHAIN.md`.
- Candidate inspected and preserved:
  `data/comparisons/p4-t06-g4j-representative-authority-v1.json`, SHA-256
  `4e4cf79f726fc1202a40b01db0cd296f337c3abb087a21a1abd5158483cc05c3`;
  manifest SHA-256
  `8b8506b4467debe4ffd9f352b661235823282cb9d48a309cf375bb8a268eb215`.
- Candidate definition SHA-256:
  `bf227e60bd2c9540c699786c530c079bb31caf4108cf461704f37c81f6758b6e`.
- Source pack SHA-256:
  `0de429b27f78b5e2c5fe724916b14ac8ce45ba05560ab73f3fa4805befd11a0e`.
- Standalone source SHA-256:
  `dc2dac6a1fd2698b4354d4eb5242d2f7a74a0c8dd425fea263539233095a341c`.
- Candidate boundary inspected: `status=candidate`,
  `evidence_approval=Candidate`, `comparison_status=Deferred`,
  `tolerance_status=Deferred`, `golden_status=NoGolden`,
  `coverage_class=RepresentativeReducedModel`, and
  `validation_domain=ReducedModel`.
- Required validation: T1 candidate authority and focused-consumer checks;
  T3 full headless Core/Golden regression. T6 is not applicable because no
  external reference baseline was changed or regenerated. T4/T5 are not
  applicable because no Unity, adapter, mobile, or platform behavior changed.
- Gate-overlap status: G2 remains exactly `FORCED CLOSED / WAIVED`. G4-R4's
  synthetic-only approval and the G4J candidate are preserved; neither is used
  to close production G4 or to authorize `P5-T10`/`G5`.

Applicable P1-T08 digest rows are carried from the G4J case:

- Topology/case design: `S1-R03`, `S1-R06`, `S5-R02`, `S6-R04`.
- Two-group/reduced projection: `S1-R04`, `S1-R05`, `S5-R03`, `S5-R04`,
  `S6-R03`.
- Burnup/refuelling: `S1-R07`, `S4-R03`, `S5-R05`, `S6-R05`.
- RRS/device case design: `S1-R08`, `S2-R05`, `S6-R02`, `S6-R04`, `S6-R05`,
  `S6-R08`.
- Poison/feedback context: `S1-R04`, `S1-R08`, `S1-R10`, `S3-R05`, `S6-R03`,
  `S6-R05`, `S6-R06`.
- Validation/admission methodology: `S1-R04`, `S1-R11`, `S5-R03`, `S5-R06`,
  `S5-R07`, `S5-R09`, `S6-R03`, `S6-R08`.

`S3-R01` through `S3-R08` are `NotApplicable` to this runtime case because
they cover SCWR/CATHENA thermal-hydraulic or safety scope. `S4-R04` and
`S6-R07` are `NotApplicable` because advanced-cycle behavior is out of scope.
I-135/Xe-135 and kinetics are explicitly `NotCovered` in the candidate;
literature rows are not used to infer those behaviors or any numeric authority.

## Evidence and commands

### Candidate authority, preservation, and boundary audit (T1)

```powershell
$ErrorActionPreference='Stop'; $candidate=Get-Content -Raw data/comparisons/p4-t06-g4j-representative-authority-v1.json | ConvertFrom-Json; $manifest=Get-Content -Raw data/comparisons/p4-t06-g4j-representative-authority-v1.manifest.json | ConvertFrom-Json; $hash=(Get-FileHash -Algorithm SHA256 data/comparisons/p4-t06-g4j-representative-authority-v1.json).Hash.ToLowerInvariant(); $manifestHash=(Get-FileHash -Algorithm SHA256 data/comparisons/p4-t06-g4j-representative-authority-v1.manifest.json).Hash.ToLowerInvariant(); $definitionHash=(Get-FileHash -Algorithm SHA256 data/comparisons/p4-t06-g4j-representative-definition-v1.json).Hash.ToLowerInvariant(); $sourcePackHash=(Get-FileHash -Algorithm SHA256 data/packs/p4-t06-r4-reduced-candidate-v1.json).Hash.ToLowerInvariant(); if($candidate.status -ne 'candidate' -or $candidate.evidence_approval -ne 'Candidate' -or $candidate.comparison_status -ne 'Deferred' -or $candidate.tolerance_status -ne 'Deferred' -or $candidate.golden_status -ne 'NoGolden' -or $candidate.coverage_class -ne 'RepresentativeReducedModel' -or $candidate.validation_domain -ne 'ReducedModel'){throw 'candidate disposition invalid'}; if($manifest.artifact_sha256 -ne $hash -or $manifest.definition_sha256 -ne $definitionHash -or $manifest.source_pack_sha256 -ne $sourcePackHash -or $manifest.disposition -ne 'candidate'){throw 'manifest binding invalid'}; $required=@('source_model_tool_build','nuclear_data','geometry_topology_mesh','state_history_event_order','units_conversions','normalization_sign','observable_schema_order','independent_reproduction','rights'); $missing=@($required | Where-Object {-not ($candidate.authority_requirements.PSObject.Properties.Name -contains $_)}); if($missing.Count -ne 0){throw ('missing authority fields: '+($missing -join ','))}; $events=@($candidate.scenarios | ForEach-Object {$_.event_history} | ForEach-Object {$_} | Where-Object {$_.event_type -eq 'refuel_shift'}); $rrs=@($candidate.scenarios | Where-Object {$_.overlay.kind -eq 'rrs'} | ForEach-Object {$_.overlay.entries}); $poison=@($candidate.scenarios | Where-Object {$_.overlay.kind -eq 'bulk_poison'} | ForEach-Object {$_.overlay.entries}); $golden=@(Get-ChildItem -LiteralPath data/golden -File -Recurse | Where-Object {$_.Name -ne '.gitkeep'}); $pathHits=@(Select-String -LiteralPath data/comparisons/p4-t06-g4j-representative-authority-v1.json,data/comparisons/p4-t06-g4j-representative-authority-v1.manifest.json,data/comparisons/p4-t06-g4j-representative-definition-v1.json -Pattern '(?i)[A-Z]:[\\/]|\\\\[^\\/]+[\\/]'); if($candidate.scenarios.Count -ne 5 -or $candidate.geometry.nodes.Count -ne 48 -or $candidate.geometry.edges.Count -ne 92 -or $candidate.geometry.boundaries.Count -ne 104 -or $events.Count -ne 2 -or $rrs.Count -ne 96 -or $poison.Count -ne 96 -or $golden.Count -ne 2 -or $pathHits.Count -ne 0){throw 'candidate scope/coverage audit failed'}; Write-Output ('G4_R5_CANDIDATE_AUDIT_PASS candidate_sha256='+$hash+' candidate_manifest_sha256='+$manifestHash+' definition_sha256='+$definitionHash+' source_pack_sha256='+$sourcePackHash+' scenarios='+$candidate.scenarios.Count+' nodes='+$candidate.geometry.nodes.Count+' edges='+$candidate.geometry.edges.Count+' boundaries='+$candidate.geometry.boundaries.Count+' authority_missing='+$missing.Count+' s4_events='+$events.Count+' rrs_entries='+$rrs.Count+' poison_entries='+$poison.Count+' golden_payload_files='+$golden.Count+' path_hits='+$pathHits.Count)
```

Result: exit 0; `G4_R5_CANDIDATE_AUDIT_PASS`, with the candidate disposition
preserved, all nine authority-requirement fields present, five scenarios, 48
nodes, 92 edges, 104 boundaries, two S4 refuel events, 96 RRS entries, 96
bulk-poison entries, two existing `data/golden` files belonging to G4I, and
zero path hits.

### Deterministic candidate regeneration and validation (T1)

```powershell
powershell -NoProfile -ExecutionPolicy Bypass -File .\tools\RepresentativeReducedAuthority\Test-P4-T06-G4J.ps1
```

Result: exit 0; Release build had 0 warnings and 0 errors. Generation and
validation passed with `disposition=candidate`, five scenarios, 48 nodes, 92
edges, 104 boundaries, `repeat_equal=True`, artifact SHA-256
`4e4cf79f726fc1202a40b01db0cd296f337c3abb087a21a1abd5158483cc05c3`, and
manifest SHA-256
`8b8506b4467debe4ffd9f352b661235823282cb9d48a309cf375bb8a268eb215`.

### Focused Core consumer (T1/T2)

```powershell
dotnet test C:\Users\infin\candu\tests\ReactorSim.Golden.Tests\ReactorSim.Golden.Tests.csproj --configuration Release --nologo --no-restore --filter FullyQualifiedName~P4T06G4J
```

Result: exit 0 when run from `C:\Users\infin`; 2/2 tests passed, 0 failures,
and 0 skips. All five scenarios converged in the Core consumer and stayed
within the candidate's diagnostic-only `1e-8` bound.

Running the same command from the repository directory is environment-blocked
before test discovery because `global.json` requests SDK `10.0.302` with
roll-forward disabled while this machine has only SDK `10.0.303`. The
repository pin was not changed.

### Full headless Core and Golden regression (T3)

```powershell
dotnet test C:\Users\infin\candu\ReactorSim.sln --configuration Release --nologo --no-restore
```

Result: exit 0 when run from `C:\Users\infin`; Core 103/103 and Golden 14/14
passed, with 0 failures and 0 skips. This is regression evidence for the
candidate consumer and existing approved synthetic consumer, not evidence that
the G4J candidate is an approved authority.

### Format check (T0)

```powershell
dotnet format C:\Users\infin\candu\ReactorSim.sln --no-restore --verify-no-changes
```

Result: exit 1 before format analysis because the repository requests SDK
`10.0.302` and only `10.0.303` is installed. No source or SDK pin was changed
to work around the environment limitation.

### T6 applicability

T6 is not applicable. G4-R5 did not change or regenerate an external reference
baseline, nuclear-data package, or production golden payload. The standalone
repeat and hash checks are T1 candidate determinism evidence only.

## Independent review evidence

- Requested lane / reasoning: code review (high), high effort.
- Actual model / reasoning: `UNVERIFIED`.
- Receipt or telemetry source: bounded reviewer
  `01a00dee-bfe2-7140-9e64-8d5b1749de48`; receipt telemetry unavailable.
- Attempts and reviewer continuity: one bounded independent review attempt;
  same reviewer/context was retained for final disposition; no fresh-review
  swarm or prior-task restart was used.
- Final technical disposition: `BLOCKED` / `NO-ADMISSION` for G4J tolerance
  or golden promotion; candidate determinism and scope checks remain valid.
- Evidence verification: `UNVERIFIED`.

## Findings

### High: no admissible tolerance or golden basis

- Location: `data/comparisons/p4-t06-g4j-representative-authority-v1.json`
  `tolerance` block; `docs/tasks/P4-T06-G4J.md`; P2-T02/P2-T05 and the G4
  admission boundary.
- Evidence and impact: the candidate marks its `1e-8` bound as diagnostic-only,
  with `tolerance_status=Deferred` and `golden_status=NoGolden`. No exact
  manufactured equation oracle, licensed external numerical baseline, or
  separately approved authority supplies a bound or expected result for this
  five-scenario reduced sequence. Promoting the observed bound would invent a
  gate tolerance.
- Required action: retain the candidate-only disposition. A future
  owner-authorized task must provide an exact oracle or complete approved
  authority before reconsidering any reduced-model profile.

### Medium: independence is implementation-level, not authority-level

- Location: G4J standalone generator, candidate artifact
  `expected_independent` values, and
  `tests/ReactorSim.Golden.Tests/P4T06G4JRepresentativeConsumerTests.cs`.
- Evidence and impact: the standalone producer does not reference the Core
  project, but the standalone results and Core comparison both derive from the
  same project-authored definition/material pack and serialized candidate
  artifact. The comparison is a useful deterministic cross-check, not an
  independent golden authority or exact oracle.
- Required action: do not relabel the candidate as `ApprovedGolden` or use it
  to approve a tolerance without an authority with independent expected values
  and complete provenance.

### Medium: production coverage remains outside this disposition

- Location: G4J coverage declaration and the Phase 4/G4 and Phase 5/G5
  handoffs.
- Evidence and impact: the case is 4x12 reduced-model evidence. It does not
  establish external CANDU numeric authority, full 380-channel behavior,
  I/Xe/kinetics, liquid-zone/adjuster runtime sequences, thermal behavior, or
  production histories. It cannot unblock production G4, `P5-T10`, or `G5`.
- Required action: preserve the production blocker and route any new authority
  work as a separate owner-authorized task.

## Numerical comparison summary

The following are candidate outputs only. There is no approved reference,
approved tolerance, or pass/fail gate comparison for them:

| Scenario | Candidate k | Candidate total power | Outer iterations | Residual relative infinity | Gate tolerance |
|---|---:|---:|---:|---:|---|
| `fresh_start` | `0.32569333812682566` | `1000 W` | 1823 | `9.935461458409358e-11` | None admitted |
| `equilibrium_like` | `0.3067650216236955` | `1000 W` | 681 | `9.876534557925246e-11` | None admitted |
| `refuelled_4_bundle_shift` | `0.31039330409549426` | `1000 W` | 850 | `8.177231151682484e-11` | None admitted |
| `rrs_tilt_perturbation` | `0.3085369346181452` | `1000 W` | 676 | `9.727510171639472e-11` | None admitted |
| `bulk_poison_perturbation` | `0.3022545018722577` | `1000 W` | 707 | `9.871078899029496e-11` | None admitted |

Standalone output repeated byte-identically for all five scenarios. Core
consumer comparisons passed the candidate's non-approved `1e-8` diagnostic
bound. Those facts demonstrate deterministic implementation consistency; they
do not establish an approved numerical difference or tolerance.

## Boundary review

- Dependency direction: PASS. The standalone reduced authority tool does not
  depend on Core, Unity, DRAGON5, DONJON5, native libraries, or runtime data;
  the test-only consumer depends on Core in the intended direction.
- Determinism and failure diagnostics: PASS for candidate scope. Generation,
  validation, and standalone repeat were byte-identical; Core converged all
  five scenarios and exposed convergence/residual diagnostics. This does not
  replace independent authority.
- Scope exclusions: PASS. No equation, unit, sign, normalization, public
  schema, runtime dependency, shutdown/scram, accident, thermal-hydraulic,
  controller, or production behavior was added or changed.
- Licensing/provenance: PASS for project-authored candidate scope. No external
  raw artifact or executable is redistributed. The workspace has no Git HEAD,
  so `source_commit=UNAVAILABLE_NO_GIT_HEAD`; content hashes bind the candidate
  snapshot but do not provide release or external-source authority.

## Required corrective tasks and deferred evidence

- Reduced-model authority task - a new owner-authorized task is required if
  reduced-model admission remains desired. It must supply an exact manufactured
  oracle or complete independent authority with source/model/tool/build,
  nuclear/material identity, geometry/topology/mesh, state/history/event order,
  units/conversions, normalization/sign, observable/schema/order, reproduction,
  and rights. It may not derive a tolerance from the current raw differences.
- Production G4 / `P5-T10` / `G5` - remain blocked pending an admissible
  representative external or otherwise approved authority and required
  production coverage.
- `dotnet format` - deferred until an SDK `10.0.302` environment is available
  or the repository owner changes the SDK pin; changing the pin is outside
  this task.
- T4/T5 - not applicable; no Unity adapter, serialization contract, mobile, or
  platform code changed.

## Risks and next eligible work

- Accepted/deferred risks: deterministic candidate evidence is useful for
  implementation regression but is not an authority; source commit identity is
  unavailable; full production and external coverage remain unresolved.
- Dependent work blocked: production G4, `P5-T10`, and `G5`.
- Independent overlap-eligible work: frozen tasks that do not consume the
  unresolved G4 authority may continue under the `AGENTS.md` gate-overlap
  policy.
- Next bounded task: none is automatically eligible. The owner may authorize a
  new exact-oracle or complete independent-authority task; do not restart G4J,
  G4-R4, or earlier G4 admission tasks.

## Token and cost accounting

- Request-level input, cached input, cache-write input, output, reasoning
  output, total, and cost telemetry: `Unavailable`.
- Cost basis: unavailable; no request-level billing receipt or verified price
  source was exposed.
- Goal-service total: unavailable and not allocable to this gate.

## Sign-off

- Reviewer/model lane: code review (high); actual receipt `UNVERIFIED`.
- Date: `2026-08-17`.
- Final disposition: `BLOCKED` / `NO-ADMISSION` for reduced-model tolerance or
  golden promotion; production G4 remains `BLOCKED`.

Source: [`AGENTS.md`](../../AGENTS.md),
[`docs/Implementation_plan.md`](../Implementation_plan.md),
[`docs/PROJECT_SCOPE.md`](../PROJECT_SCOPE.md),
[`docs/reference/candu-literature-digest-v1.md`](../reference/candu-literature-digest-v1.md),
and [`docs/gates/GATE_REPORT_TEMPLATE.md`](GATE_REPORT_TEMPLATE.md).
